% =====================================================================
% RESTRUCTURED FOR THE 12-PAGE A&A FREE LIMIT (2026-07-17)
% Main text spine: complementary morphology estimands.
%   MOVED to appendices: pooled model family + fig (App F), galaxy-level
%     matching details/figs (App F), Sect. 4 environmental diagnostics
%     (App E), full colour analysis (App E), SFMS details (App D),
%     extended size diagnostics (App G), selection diagnostics (App F),
%     figs: sSFR_classification (App B), main_sequence_residuals (App D),
%     crowding, tidal, phase-space-by-distance, matched effects/balance,
%     colour robustness (App E/F).
%   CUT outright (resurrect from git if wanted): Tables 3-5 (medians),
%     fig galaxies_morph_sfr, fig Dom_vs_NonDom_XOR, Table 9
%     (result hierarchy; replaced by tab:inferential_framework +
%     tab:morph_robustness).
%   NEW: tab:inferential_framework (Sect. 2.5), tab:morph_robustness
%     (Sect. 3.5), estimand-resolution text (Sect. 4.1, Conclusion),
%     fixed-Holm-family statement (Sect. 2.5).
%   Search "% BUDGET:" for toggles if the compile lands over/under.
%   All Jinja variable keys unchanged; verdict prose gated on the same
%   data fields as before wherever feasible.
% =====================================================================
\documentclass{aa}
%
\usepackage{graphicx}
\usepackage[varg]{txfonts}
\usepackage{natbib,twoopt}
\usepackage[breaklinks=true]{hyperref} %% to avoid \citeads line fills
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
    pdftitle={Are galaxies in compact groups special?},
    pdfpagemode=FullScreen,
    }

\bibpunct{(}{)}{;}{a}{}{,}             %% natbib format for A&A and ApJ

\makeatletter
  \newcommandtwoopt{\citeads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citealp[#1][#2]{#3}}}
  \newcommandtwoopt{\citepads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citep[#1][#2]{#3}}}
  \newcommandtwoopt{\citetads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citet[#1][#2]{#3}}}
  \newcommandtwoopt{\citeyearads}[3][][]%
    {\href{http://adsabs.harvard.edu/abs/#3}
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citeyear[#1][#2]{#3}}}
\makeatother

\usepackage{graphicx}	% Including figure files
\usepackage{color}
\usepackage{array}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{ae,aecompl}
\usepackage[utf8]{inputenc}
\usepackage{url}
% \usepackage{autonum} % only cited equations are numbered

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{placeins}


\usepackage{listings}
\usepackage{xcolor}

\newcommand{\diazg}{D\'{\i}az-Gim\'enez}



\newcommand{\CB}{Control\textsubscript{4B}}
\newcommand{\CC}{Control\textsubscript{4C}}
\newcommand{\CG}{CG\textsubscript{4}}
\newcommand{\RG}{RG\textsubscript{4}}
\newcommand{\kms}{\,\mathrm{km\,s}^{-1}}
\newcommand{\gap}{$\Delta R_{1,2}$}
\newcommand{\msun}{\mathrm{M}_\odot}

\definecolor{darkgreen}{rgb}{0.0,0.5,0.0}
\definecolor{darkred}{rgb}{0.75,0.,0.2}
\definecolor{magenta}{rgb}{0.8,0,0.8}
\definecolor{purple}{rgb}{0.5,0,0.5}
\definecolor{gray}{rgb}{0.5,0.6,0.7}
\newcommand{\hide}[1]{\textcolor{gray}{#1}}
\newcommand{\red}[1]{\textcolor{red}{#1}}
\newcommand{\blue}[1]{\textcolor{blue}{#1}}
\newcommand\boldblue[1]{\textcolor{blue}{\mathbf{#1}}}
\newcommand\boldred[1]{\textcolor{darkred}{\mathbf{#1}}}
\newcommand{\gam}[1]{\textcolor{darkred}{\bf Gary: #1}}
\newcommand{\gm}[1]{\textcolor{darkred}{#1}}
\newcommand{\referee}[1]{\textcolor{red}{\bf Referee: #1}}
\newcommand{\MT}[1]{\textcolor{darkgreen}{\bf Matthieu: #1}}
\newcommand{\mt}[1]{\textcolor{darkgreen}{#1}}



\lstset{
  language=SQL,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue}\bfseries,
  stringstyle=\color{red},
  commentstyle=\color{gray}\itshape,
  morekeywords={SpecObj,PhotoObj,galSpecExtra,galSpecLine},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=5pt,
  showstringspaces=false,
  tabsize=4,
  breaklines=true,
  frame=single,
  captionpos=b
}

\graphicspath{{<<SUBFIGURES_PATH>>}}


\begin{document}
<% set es = extended_specialness if extended_specialness else {} %>
<% set sz = es.get('size_analysis', {}) %>
<% set szv = sz.get('verdicts', {}) %>
\nolinenumbers
\title{Are galaxies in compact groups special?}

   \author{Matthieu Tricottet\inst{1}\thanks{ORCID: \url{https://orcid.org/0009-0008-0062-4450}}
           \and Gary A. Mamon\inst{2}\thanks{ORCID: \url{https://orcid.org/0000-0001-8956-5953}}
           \and Eugenia \diazg\inst{3,4}\thanks{ORCID: \url{https://orcid.org/0000-0001-5374-4810}}
           }

   \institute{
                80, rue d'Al\'esia, 75014 Paris, France\\
                \email{matthieu.tricottet@gmail.com}
                \and Institut d'Astrophysique de Paris (UMR 7095: CNRS \& Sorbonne Universit\'e), 98 bis boulevard Arago, 75014 Paris, France
                \and CONICET. Instituto de Astronom\'ia Te\'orica y Experimental (IATE), Laprida 854, X5000BGR, C\'ordoba, Argentina
                \and Universidad Nacional de C\'ordoba (UNC). Observatorio Astron\'omico de C\'ordoba (OAC), Laprida 854, X5000BGR, C\'ordoba, Argentina
            }

  \date{}

<# The size and within-host sentences in the Results block are static text
   asserting the current verdicts (no pooled adjusted size offset; matched
   pairs control-dependent; no primary within-host detection). If either
   analysis changes, revisit the abstract. #>
  \abstract
  {Compact groups combine high projected densities with low velocity dispersions, but many are embedded in larger structures. Their significance depends on whether their galaxies remain distinct from comparable regular-group members, not only from field galaxies.}
  {We test whether galaxies in compact groups of four members (\CG) remain special against regular groups of four members (\RG), the four brightest galaxies of richer regular groups (\CB), and projected compact cores formed by the brightest group galaxy and its three nearest projected companions (\CC).}
  {From our previous samples, we retain <<CG4_Groups_nonsplit_N>> \CG{} systems after removing split systems. We combine Galaxy Zoo~1 vote fractions, Sloan Digital Sky Survey stellar masses and specific star-formation rates, optical colours, emission-line diagnostics, and seeing-corrected half-light radii. Inference proceeds through four complementary analyses: per-control covariate-adjusted galaxy-level models, galaxy-level matching, group-level matching of satellite composition, and within-host stratified comparisons.}
  {The main residual signal is morphological and estimand-dependent. \CG{} galaxies, especially satellites, have higher elliptical-vote and lower spiral-vote fractions than their regular-group analogues. The adjusted individual-galaxy excess is strong against \CB{} and \RG{} but not detected against \CC{}; in contrast, the matched satellite-composition contrast is positive against all three controls under the stated per-control convention, including \CC{}. No additional within-host excess is detected with the primary conditional estimator. The morphology result is stable to the vote-fraction threshold, a threshold-free vote-fraction model, a S\'ersic-index cross-check, and the exclusion of the closest projected pairs. Conditioning on a projected pairwise tidal-density proxy attenuates the galaxy-level coefficient. Star-formation, colour, active-galactic-nucleus, magnitude-gap, \(\mathrm{H}\alpha\), and fixed-mass size diagnostics do not show comparably robust adjusted or matched \CG{} offsets.}
  {\CG{} systems host a population shifted towards early-type-like morphologies, consistent with accumulated transformation in dense group cores and/or preprocessing rather than proof of a uniquely isolated present-day quenching mode.}

   \keywords{galaxies: groups: general -- galaxies: evolution -- galaxies: interactions -- galaxies: star formation -- galaxies: elliptical and lenticular, cD -- methods: statistical}

   \maketitle

<% macro p_from_fmt(fmt, label='p') -%><% set text = (fmt|string)|replace('$', '')|trim -%>\(<< label >><% if text.startswith('<') %><< text >><% else %> = << text >><% endif %>\)<%- endmacro %>
<% macro pval_text(x, nd=3) -%><% if x is none -%>\(p\) unavailable<% elif x < 0.0001 -%>\(p<10^{-4}\)<% elif x < 0.001 -%>\(p = << gu.latex_number(x, precision=2, math_mode=False) >>\)<% else -%>\(p = << x|fmt(nd) >>\)<% endif -%><% endmacro %>
<% macro pholm_text(x, nd=3) -%><% if x is none -%>Holm-corrected \(p\) unavailable<% elif x < 0.0001 -%>Holm-corrected \(p<10^{-4}\)<% elif x < 0.001 -%>Holm-corrected \(p = << gu.latex_number(x, precision=2, math_mode=False) >>\)<% else -%>Holm-corrected \(p = << x|fmt(nd) >>\)<% endif -%><% endmacro %>
<% macro qlabel_short(q) -%><% if q.key == 'sSFR' -%>\(\log_{10}\mathrm{sSFR}\)<% elif q.key == 'M_r' -%>\(M_r\)<% else -%>\(\log M_\star\)<% endif -%><% endmacro %>

\section{Introduction}\label{sec:intro}

Given their high apparent densities and low velocity dispersions \citep{Hickson+92}, compact groups appear to be ideal sites for the frequent tidal encounters and mergers that alter galaxy masses, luminosities, sizes, morphologies, and star-formation rates (e.g., \citealt{Mamon92}). \citet{MendesdeOliveira&Hickson94} provided the classic report of an excess of early-type galaxies in compact groups relative to the field, in line with the broader morphology--density relation \citep{Dressler80}, and multi-wavelength studies of compact-group galaxies likewise report altered infrared, UV, star-formation, and stellar-population properties relative to looser environments \citep{Johnson+07,Tzanavaris+10,Bitsakis+11,Coenda+12}.

The interpretation of such contrasts is nevertheless ambiguous. Projected selection can include chance alignments, and many Hickson-like systems need not be isolated bound entities: apparent compact groups may be transient alignments, dense substructures, or cores embedded in looser groups and richer haloes \citep{Mamon86,DiazGimenez&Mamon10,DiazGimenez+20,Zheng&Shen21,Taverna+23}. The relevant observational question is therefore not only whether compact-group galaxies differ from field galaxies, but whether they remain different from galaxies occupying regular-group analogues selected with comparable luminosity, redshift, and membership constraints. Recent structural studies point to transformation in progress in compact groups, with stronger signatures in non-isolated systems and host structures \citep{Montaguth2023,Montaguth2025,Montaguth2026MassSize,Montaguth2026NonIsolated}, but they do not by themselves answer this control question.

In our previous work \citep{Tricottet+25}, we found that, once the direct consequences of the compact-group selection are accounted for, compact groups of four galaxies have group-scale properties broadly similar to those of regular groups of four galaxies and to the cores of richer regular groups. Here we ask the complementary question: even if compact groups are not strongly distinct as systems, are their member galaxies individually different? We compare \CG{} member galaxies with three regular-group analogues -- \RG{}, \CB{}, and \CC{} (Sect.~\ref{sec:samples}) -- controlling for stellar mass, redshift, galaxy rank, and available group-scale properties, and use complementary individual-galaxy, group-composition, and shared-host contrasts to identify which observable, if any, carries a residual compact-group signal.

Section~\ref{sec:data} describes the samples, the classifications, and the statistical design. Section~\ref{sec:results} presents the population contrasts, the matched and within-host comparisons, and the robustness and secondary diagnostics. Section~\ref{sec:discussion} discusses the physical interpretation, and Section~\ref{sec:conclusion} concludes.

\section{Data and classifications}\label{sec:data}
\subsection{Samples}\label{sec:samples}
We use the compact-group and control samples constructed in our previous study \citep{Tricottet+25}, whose construction we briefly recall. Galaxy properties are taken from the SDSS DR12 catalogue of \citet{Tempel+17}, while regular groups are drawn from the SDSS DR7 group catalogue of \citet{Lim+17}; the two catalogues were matched through SDSS object identifiers to define the Lim--Tempel working catalogue. Compact groups are selected from the modified Hickson compact-group catalogue of \citet{DiazGimenez+18}: reliable HMCGs with exactly four members, all present in the Lim--Tempel catalogue, satisfying the volume- and luminosity-complete limits $0.005 \le z_{\rm BGG} \le 0.0452$ and $M_{r,\rm BGG}\leq -21.81$, with all four galaxies lying within 3 magnitudes of the brightest group galaxy (BGG). In contrast to our previous study, we remove CGs classified as \emph{split} following \citet{Zheng&Shen21}, which reduces the compact-group sample from <<CG4_Groups_withsplit_N>> to <<CG4_Groups_nonsplit_N>> groups.

The regular-group comparison samples are built from Lim--Tempel groups satisfying the same redshift and BGG-luminosity limits, with at least 4 members and at least 3 satellites within 3 magnitudes of the BGG. The three controls serve different physical purposes. \RG{} contains regular groups with exactly four members (excluding systems identical to a \CG) and is the most direct equal-membership comparison. \CB{} contains the four brightest galaxies of each richer regular group and tests whether the \CG{} population resembles the luminous core of an ordinary group. \CC{} contains the BGG plus its three closest projected companions and is the most compact-core-like ordinary-group analogue; it is regenerated for this paper directly from the 765 parent groups\footnote{Our previous paper quotes 61 excluded projected-core groups; the archived parent catalogue reproducibly yields 60. The difference traces to an earlier revision of the parent catalogue and does not affect any comparison in this paper.}. Every control group containing at least one \CG{} galaxy is excluded from the control samples, so no galaxy appears on both sides of any comparison<% if r.get('cg4_pc_quartet_overlap', {}) %>; the extent of this compact-group/projected-core overlap is quantified in Appendix~\ref{app:cg_core_overlap}<% endif %>. We also remove the systems associated with Lim group 3688, which appears to be spurious: its single outlying-redshift member ($z=0.048$, against $0.032$--$0.034$ for the other six) is selected into both our original \CB{} and \CC{} samples and inflates the group velocity dispersions to $\simeq 1980 \kms$. After these removals, the control samples contain << Control4B_Groups_N >> \CB{}, << Control4C_Groups_N >> \CC{}, and << RG4_Groups_N >> \RG{} groups (<< Control4B_Gals_N >>, << Control4C_Gals_N >>, and << RG4_Gals_N >> galaxies); the \CB{} and \CC{} counts differ because the luminosity-ranked and projected-distance-ranked core definitions select different galaxies and therefore retain different Lim groups after the compact-group exclusion.

We use SDSS DR<<DATA_RELEASE>> \citep{Ahumada+20} to retrieve stellar masses, star formation rates \citep[SFRs;][]{Brinchmann+04,Kauffmann+03b}, and Galaxy Zoo morphologies \citep{Lintott+08,GalaxyZoo1}; the exact columns, table joins, and query are given in Appendix~\ref{app:sdss_query}. Throughout, `SDSS selection' denotes the reference sample returned by that query: all spectroscopic galaxies with \(<<Z_MIN>> \le z \le <<Z_MAX>>\), extinction-corrected Petrosian \(r \le <<R_MAX>>\), a valid MPA-JHU stellar mass, and Galaxy Zoo classifications. It serves as a field-dominated reference population, not as a control sample. All projected distances and derived quantities adopt the Planck 2015 cosmology \citep{Planck15}, consistently with our previous paper.

\subsection{Star-formation and morphology classes}\label{sec:ssfr_classification}
We classify galaxies from SDSS total sSFRs. In the MPA-JHU catalogue, values of \(-9999\) in the sSFR fields are sentinel values indicating that no valid sSFR estimate is available, rather than physical measurements; the same missing-value convention is used for MPA-JHU DR8 target quantities by \citet{Zeraatgari+24}. We therefore treat \texttt{specsfr\_tot\_p50} \(= -9999\) as \emph{missing data}: such galaxies are excluded from the star-formation classification and from every sSFR-based figure and fraction, and are reported as counts only (Table~\ref{tab:sSFR_status}). All quenched and star-forming fractions quoted in this paper are computed among classified galaxies only. The missing-sSFR incidence is markedly higher in \CG{} (<< CG4_Gals_NNosSFR_pc|fmt(0) >>\% of members, and << CG4_BGG_NNosSFR_pc|fmt(0) >>\% of BGGs) than in the field selection (<< SDSS_NNosSFR_pc|fmt(0) >>\%), plausibly reflecting crowded-field spectroscopy and deblending (Sect.~\ref{sec:decoupling}) rather than a physical population. Galaxies with a measured sSFR are split into quenched and star-forming classes with a two-component Gaussian mixture model (GMM) in the \(\log M_\star\)--\(\log\mathrm{sSFR}\) plane; BPT/AGN handling and the exact GMM boundary definition are given in Appendix~\ref{app:ssfr_method} (Fig.~\ref{fig:sSFR_classification}).

Morphologies are drawn from Galaxy Zoo 1 \citep{Lintott+08,GalaxyZoo1}, a single-question classification in which volunteers assigned each galaxy image to one of several morphological categories. The resulting debiased vote fractions are provided in the \texttt{zooSpec} table through the columns \texttt{p\_el\_debiased} (fraction voting ``elliptical'') and \texttt{p\_cs\_debiased} (fraction voting combined-spiral; Lintott et al.\ 2011). We classify a galaxy as elliptical if \texttt{p\_el\_debiased} exceeds 0.5, as spiral if \texttt{p\_cs\_debiased} exceeds 0.5, and as ``uncertain'' otherwise. These are vote-fraction classes, not a full structural decomposition. In particular, the catalogue does not provide a clean E/S0 separation, so references below to elliptical or early-type galaxies should be read as Galaxy Zoo 1 elliptical-vote proxies for early-type morphology rather than as a bulge-disc structural classification.

<% if sz.get('status') == 'ok' %>
<% set size_cuts = sz.get('quality_cuts', {}) %>
<% set size_avail = sz.get('availability_audit', {}) %>
<% set size_totals = size_avail.get('totals', {}) %>
\subsection{Galaxy sizes}\label{sec:size_data}

Our primary galaxy-size measure is the circular half-light radius of the pure S\'ersic fit in the $r$ band, \(R_{\rm chl,r}\), from the bulge+disk decomposition catalogue of \citet{Simard2011}. These are PSF-convolved GIM2D fits with re-derived sky levels and deblending designed with crowded fields in mind -- the relevant regime for compact-group photometry -- so the fitted sizes are corrected for seeing. The catalogue is keyed to SDSS DR7 identifiers; we bridge our DR<<DATA_RELEASE>> objects to their DR7 counterparts through the SkyServer \texttt{<< r.get('size_dr7_bridge', 'SpecDR7') >>} cross-match table. Two guards protect this bridge: rows in which the Simard redshift differs from the catalogue redshift by more than << size_cuts.get('z_match_tolerance')|fmt(3) >> are treated as wrong matches and their structural values dropped (<< size_totals.get('z_mismatch') >> rows), and rows in which two distinct catalogue galaxies of the same group resolve to a single DR7 identifier are treated as blended detections and dropped (<< size_totals.get('shred_merge') >> rows). The Simard sizes are tabulated in kpc under the catalogue's own cosmology; we convert them back to angular sizes with the catalogue plate scale and then to proper kpc with the same Planck 2015 conversion as every other projected quantity in this paper.

As a secondary size measure we use the SDSS Petrosian radii \texttt{petroR50\_r} and \texttt{petroR90\_r}, which also define the concentration index \(C = R_{90,r}/R_{50,r}\). Petrosian radii are not corrected for seeing, and the SDSS Petrosian aperture misses a profile-dependent fraction of the flux, roughly 20\% for an \(n=4\) profile, which biases \(R_{50,r}\) low for concentrated galaxies \citep{Blanton2001,Graham2005} -- precisely the elliptical/early-type-like population in which \CG{} is enhanced. The Petrosian measure is nevertheless retained because it is model-independent, essentially fully available, and provides the like-for-like bridge to the compact-group size study of \citet{Coenda+12}. All sizes are required to lie between << size_cuts.get('size_min_kpc')|fmt(1) >> and << size_cuts.get('size_max_kpc')|fmt(0) >> kpc after re-conversion; S\'ersic fits pegged at the GIM2D bounds (\(n_g \le << size_cuts.get('ng_peg_low')|fmt(2) >>\) or \(n_g \ge << size_cuts.get('ng_peg_high')|fmt(1) >>\); << size_totals.get('n_pegged') >> galaxies) are excluded from the primary sample, and Petrosian entries must satisfy \(R_{50,r}>0\), \(R_{90,r}>R_{50,r}\), and a finite positive uncertainty. The completeness of both measures, and its difference between \CG{} and the controls, is audited in Sect.~\ref{sec:size_results}.
<% endif %>

\subsection{Statistical design}\label{sec:stats}

Descriptive baseline comparisons use Fisher or Barnard exact tests for two-sample categorical tables, and rank-sum or bootstrap comparisons for distributional and median differences. Spearman and Pearson coefficients are used only as diagnostics of monotonic or linear trends; when the coefficients are small, we interpret them as weak even if the p-values are formally small.

The primary inference is organized around four complementary analyses (Table~\ref{tab:inferential_framework}) that separate individual-galaxy, group-composition, and shared-host contrasts: (i) covariate-adjusted binomial models fit \emph{separately} for each \CG{}--control pair (Sect.~\ref{sec:primary_contrasts}); (ii) a one-to-one galaxy-level matched comparison; (iii) a group-level matched comparison of satellite composition; and (iv) a within-host stratified comparison of \CG{} members against the other members of the same host group. These analyses do not form a monotonic sequence of increasingly strong adjustment: they change the statistical unit, matched population, conditioning variables, outcome, effect scale, and interpretation. Throughout, the independence unit is the \emph{physical} group: standard errors are clustered by Lim group (unifying the overlapping control definitions, with \CG{} systems assigned to their host Lim group), and all resampling is blocked at the group level, since the four galaxies of one group are not independent observations. Pooled models across the three controls are reported only as secondary summaries, after deduplicating the overlapping control samples to one row per physical galaxy (Appendix~\ref{app:pooled_models}). Bootstrap p-values use the add-one empirical convention \(p = (k+1)/(B+1)\) with the number of resamples \(B\) reported, so no resampling p-value can fall below its Monte-Carlo resolution; displayed p-values are floored at \(10^{-4}\).

Holm or Benjamini--Hochberg corrections are applied within pre-defined families of related tests. These families are defined over the full set of computed diagnostics -- including those reported only in the appendices -- and are \emph{not} recomputed over the subset of results shown in the main text. For the per-control group-level satellite-composition matches, the three control definitions are treated as separate scientific estimands because \CB{}, \CC{}, and \RG{} answer different control questions; their p-values are therefore not adjusted across control definitions, and the pooled match is a secondary summary rather than a fourth confirmatory comparison. The paired sign-flip permutation p-value is the primary paired test for these group-level matches; the bootstrap sign-crossing p-value and interval are used for robustness and effect-size uncertainty. A Holm sensitivity calculation for the three per-control bootstrap and permutation p-value sets is reported in Appendix~\ref{app:pooled_models}. Colours, AGN-like fractions, \(\mathrm{H}\alpha\) equivalent widths, magnitude gaps, crossing-time trends, projected phase space, and the pairwise tidal index are secondary diagnostics: the conclusions below rely on signals that survive the adjusted, matched, and within-host analyses, not on isolated exploratory tests.

% BUDGET: tab:inferential_framework is droppable (fold analyses i-iv into prose) if the main text lands slightly over 12 pages.
\begin{table}
\centering
\caption{Complementary inferential framework. Each analysis answers a different question about the \CG{} population; uncertainties are clustered or resampled by physical group.}
\label{tab:inferential_framework}
\small
\tabcolsep=3pt
\begin{tabular}{lll}
\hline\hline
Analysis & Unit & Estimand \\
\hline
Adjusted, per control & galaxy & \makecell[l]{individual conditional association\\at fixed galaxy and group covariates} \\
Matched, galaxy & galaxy & \makecell[l]{matched individual-galaxy contrast\\on one-to-one pairs} \\
Matched, group & group & \makecell[l]{difference in satellite composition\\between matched systems} \\
Within host & member & \makecell[l]{\CG{} members versus non-\CG{}\\co-members of the same host} \\
\hline
\end{tabular}
\end{table}

\section{Results}\label{sec:results}
\subsection{Raw population contrasts}\label{sec:raw}

The sSFR status and Galaxy Zoo morphology mix of each sample are shown in Tables~\ref{tab:sSFR_status} and \ref{tab:morphologies}. For the morphologies, Barnard's two-sided exact tests give p-values of <<p_from_fmt(pval_Control4B_Elliptical_vs_CG_pc)>> for ellipticals and <<p_from_fmt(pval_Control4B_Spiral_vs_CG_pc)>> for spirals versus the \CB{} sample, <<p_from_fmt(pval_Control4C_Elliptical_vs_CG_pc)>> and <<p_from_fmt(pval_Control4C_Spiral_vs_CG_pc)>> versus \CC{}, and <<p_from_fmt(pval_RG4_Elliptical_vs_CG_pc)>> and <<p_from_fmt(pval_RG4_Spiral_vs_CG_pc)>> versus \RG{}. The galaxies in \CG{} systems, which we identified as frequent cores of richer groups in \citet{Tricottet+25}, show a clear raw excess of elliptical classifications and a deficit of spiral classifications. Restricted to BGGs, \CG{} BGGs have a higher elliptical fraction (<<CG4_BGG_fracpc_Elliptical>>\%) than \RG{} BGGs (<<RG4_BGG_fracpc_Elliptical>>\%); the p-values obtained with <<BGG_morph_tests>> are <<BGG_morph_pvalue_Control4B_vs_CG4>> against \CB, <<BGG_morph_pvalue_Control4C_vs_CG4>> against \CC{} and <<BGG_morph_pvalue_RG4_vs_CG4>> against \RG. Restricted instead to star-forming galaxies, the elliptical/spiral mix of \CG{} is statistically consistent with that of every control (<<Morph_sSFR_test>>: <<p_from_fmt(pval_Control4B_Starforming_vs_CG_pc)>> against \CB, <<p_from_fmt(pval_Control4C_Starforming_vs_CG_pc)>> against \CC, and <<p_from_fmt(pval_RG4_Starforming_vs_CG_pc)>> against \RG): we detect neither an excess nor a deficit of \emph{star-forming} elliptical galaxies, so the raw morphology excess is not carried by star-forming ellipticals.

\begin{table}
    \centering
    \caption{Number of galaxies in each sSFR class for each sample.
    Galaxies without a valid sSFR estimate (`No sSFR') are excluded from the
    classification: their percentages are quoted with respect to all galaxies
    of the (sub)sample, while the quenched and star-forming percentages are
    quoted with respect to classified galaxies only.}
    \label{tab:sSFR_status}
    \tabcolsep=3pt
    \begin{tabular}{lccc}
        \hline\hline
            \textbf{Sample} & \textbf{  No sSFR  } & \textbf{  Quenched  } & \textbf{Star-forming} \\
            \hline
<% for tex, s in [('\\CG', 'CG4'), ('\\CB', 'Control4B'), ('\\CC', 'Control4C'), ('\\RG', 'RG4')] %>
<% set all_M = r[s ~ '_BGG_NNosSFR'] + r[s ~ '_Sat_NNosSFR'] %>
<% set all_Q = r[s ~ '_BGG_NQuenched'] + r[s ~ '_Sat_NQuenched'] %>
<% set all_SF = r[s ~ '_BGG_NStarforming'] + r[s ~ '_Sat_NStarforming'] %>
<% set all_C = all_Q + all_SF %>
<% set all_N = all_C + all_M %>
            << tex >> &  <<all_M>>  (<< (100 * all_M / all_N)|fmt(0) >>\%) &  <<all_Q>>  (<< (100 * all_Q / all_C)|fmt(0) >>\%) &  <<all_SF>>  (<< (100 * all_SF / all_C)|fmt(0) >>\%) \\
             \quad \emph{BGG} &  \emph{<< r[s ~ '_BGG_NNosSFR'] >>  (<< r[s ~ '_BGG_NNosSFR_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_BGG_NQuenched'] >>  (<< r[s ~ '_BGG_NQuenched_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_BGG_NStarforming'] >>  (<< r[s ~ '_BGG_NStarforming_pc']|fmt(0) >>\%)} \\
             \quad \emph{Satellites} &  \emph{<< r[s ~ '_Sat_NNosSFR'] >>  (<< r[s ~ '_Sat_NNosSFR_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_Sat_NQuenched'] >>  (<< r[s ~ '_Sat_NQuenched_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_Sat_NStarforming'] >>  (<< r[s ~ '_Sat_NStarforming_pc']|fmt(0) >>\%)} \\
<% endfor %>
         \hline
            \emph{SDSS selection} &  \emph{<<SDSS_NNosSFR>> (<<SDSS_NNosSFR_pc|fmt(0)>>\%)} &  \emph{<<SDSS_NQuenched>>  (<<SDSS_NQuenched_pc|fmt(0)>>\%)} &  \emph{<<SDSS_NStarforming>>  (<<SDSS_NStarforming_pc|fmt(0)>>\%)}\\
        \hline
    \end{tabular}
\end{table}

\begin{table}
    \centering
    \caption{Number of galaxies in each Galaxy Zoo vote-fraction morphology proxy for each sample.}
    \label{tab:morphologies}
    \small
    \tabcolsep=2pt
    \begin{tabular}{lccc}
        \hline\hline
            \textbf{Sample} & \textbf{Elliptical} & \textbf{Spiral} & \textbf{Uncertain} \\
            \hline
            \CG &  <<CG4_Gals_N_Elliptical>>  (<<CG4_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<CG4_Gals_N_Spiral>>  (<<CG4_Gals_fraction_Spiral_pc|fmt(0)>>\%)&  <<CG4_Gals_N_Uncertain>>  (<<CG4_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \CB &  <<Control4B_Gals_N_Elliptical>>  (<<Control4B_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<Control4B_Gals_N_Spiral>>  (<<Control4B_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<Control4B_Gals_N_Uncertain>>  (<<Control4B_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \CC &  <<Control4C_Gals_N_Elliptical>>  (<<Control4C_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<Control4C_Gals_N_Spiral>>  (<<Control4C_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<Control4C_Gals_N_Uncertain>>  (<<Control4C_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \RG &  <<RG4_Gals_N_Elliptical>>  (<<RG4_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<RG4_Gals_N_Spiral>>  (<<RG4_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<RG4_Gals_N_Uncertain>>  (<<RG4_Gals_fraction_Uncertain_pc|fmt(0)>>\%)\\
            \emph{SDSS selection} &  \emph{<<SDSS_N_Elliptical>> (<<SDSS_fraction_Elliptical_pc|fmt(0)>>\%)} &  \emph{<<SDSS_N_Spiral>>  (<<SDSS_fraction_Spiral_pc|fmt(0)>>\%)} &  \emph{<<SDSS_N_Uncertain>>  (<<SDSS_fraction_Uncertain_pc|fmt(0)>>\%)}\\
        \hline
    \end{tabular}
\end{table}

The star-formation contrast is weaker and rank-dependent. In the raw all-galaxy comparison, Fisher's exact test gives star-forming-fraction p-values of <<p_from_fmt(pval_Control4B_Starforming_vs_CG)>> versus the \CB{} sample, <<p_from_fmt(pval_Control4C_Starforming_vs_CG)>> versus \CC, and <<p_from_fmt(pval_RG4_Starforming_vs_CG)>> versus \RG. For satellites, the corresponding p-values obtained with <<BGG_sSFR_tests>> are <<p_from_fmt(Sat_star_forming_pvalue_Control4B_vs_CG4)>> against \CB, <<p_from_fmt(Sat_star_forming_pvalue_Control4C_vs_CG4)>> against \CC{} and <<p_from_fmt(Sat_star_forming_pvalue_RG4_vs_CG4)>> against \RG; for BGGs, they are <<p_from_fmt(BGG_star_forming_pvalue_Control4B_vs_CG4)>>, <<p_from_fmt(BGG_star_forming_pvalue_Control4C_vs_CG4)>>, and <<p_from_fmt(BGG_star_forming_pvalue_RG4_vs_CG4)>> (same control order). The satellite deficit is clearest against \RG{}. We treat all raw tests as descriptive, because star-formation class and morphology are entangled with stellar mass and luminosity; this motivates the adjusted analyses of Sect.~\ref{sec:primary_contrasts}.

For the star-forming subset, we fit the star-forming main sequence with polynomials of order 1--4, adopting order << r.get('Main_Sequence_polyfit_selected_order', 2) >> as the lowest-order model with the smallest five-fold cross-validated RMS scatter (<< r.get('Main_Sequence_polyfit_selected_cv_rms', 0)|fmt(5) >> dex; Appendix~\ref{app:baseline_details}). The median \CG{}-minus-control residual offsets about this relation are small, control-dependent, and change in strength after matching (Appendix~\ref{app:baseline_details}), so we treat main-sequence residuals as a secondary diagnostic rather than as evidence for a robust compact-group star-formation offset.

<% set sm = es.get('specialness_models', {}) %>
<% set pcx = es.get('primary_contrasts', {}) %>
<% if pcx.get('status') == 'ok' %>
\subsection{Per-control adjusted contrasts (primary)}\label{sec:primary_contrasts}

The three control samples answer different questions, so our primary galaxy-level inference fits \CG{} against each control \emph{separately}: against \CB{} (is the \CG{} population special compared with the luminous population of richer ordinary groups?), against \CC{} (compared with BGG-centred projected cores?), and against \RG{} (compared with true four-member ordinary groups?). Each contrast fits the same family of adjusted binomial models (stellar mass, redshift, rank, group luminosity, and velocity dispersion where sufficiently complete), with standard errors clustered by \emph{physical} group: the Lim group for controls, and for \CG{} systems their host Lim group where one exists, so that the same physical system never enters as more than one cluster. Holm correction is applied within each contrast across its model family.

<% for control, tex in [('Control4B', '\\CB'), ('Control4C', '\\CC'), ('RG4', '\\RG')] %>
<% set contrast = pcx.get('contrasts', {}).get(control, {}) %>
<% set ell = contrast.get('elliptical_all', {}) %>
<% set qnc = contrast.get('quenched_all', {}) %>
<% if ell.get('status') == 'ok' %>
Against << tex >>{}, the elliptical odds ratio is \(<< ell.get('cg4_odds_ratio')|fmt(2) >>\) (95\% CI \([<< ell.get('cg4_ci95')[0]|fmt(2) >>, << ell.get('cg4_ci95')[1]|fmt(2) >>]\); << pholm_text(ell.get('cg4_p_adj')) >>)<% if qnc.get('status') == 'ok' %>, and the quenched odds ratio is \(<< qnc.get('cg4_odds_ratio')|fmt(2) >>\) (<< pholm_text(qnc.get('cg4_p_adj')) >>)<% endif %>.
<% endif %>
<% endfor %>

The asymmetry between the contrasts is itself informative. The adjusted individual-galaxy morphology excess is strong against the luminosity-selected cores of richer groups and against true four-member groups, but is not detected against the BGG-centred \emph{projected} cores of ordinary groups, the control definition that most closely mimics a compact projected configuration. Evidence against \CC{} therefore depends on the estimand: the adjusted individual-galaxy contrast is not detected here, whereas the group-level satellite-composition contrast below is positive. This pattern anticipates the local projected-density interpretation of Sect.~\ref{sec:tidal_index}<% if pcx.get('figure') %> (Fig.~\ref{fig:primary_contrasts})<% endif %>.

<% if pcx.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< pcx.get('figure') >>}
    \caption{Adjusted \CG{} odds ratios from the three separate per-control contrasts (colour-coded by control sample) for the all-member elliptical, spiral and quenched outcomes. Bars show 95\% confidence intervals; the vertical line marks equal odds.}
    \label{fig:primary_contrasts}
\end{figure}
<% endif %>
<% endif %>

<% if sm.get('status') == 'ok' %>
<% set sm_ell_all = sm.get('elliptical_all', {}) %>
<% set sm_qnc_all = sm.get('quenched_all', {}) %>
<% set ell_sat = sm.get('elliptical_satellites', {}) %>
<% set spi_sat = sm.get('spiral_satellites', {}) %>
<% set ell_bgg = sm.get('elliptical_bgg', {}) %>
As a compact secondary summary, a pooled model over the deduplicated control catalogues (one row per physical galaxy, cluster-robust by physical Lim group; full family in Appendix~\ref{app:pooled_models}) gives the same qualitative picture<% if sm_ell_all.get('status') == 'ok' %>: the pooled elliptical odds ratio is \(<< sm_ell_all.get('cg4_odds_ratio')|fmt(2) >>\) (<< pholm_text(sm_ell_all.get('cg4_p_adj')) >>)<% endif %><% if sm_qnc_all.get('status') == 'ok' %>, and the pooled quenched odds ratio is \(<< sm_qnc_all.get('cg4_odds_ratio')|fmt(2) >>\) (<< pholm_text(sm_qnc_all.get('cg4_p_adj')) >>)<% endif %>. <% if ell_sat.get('status') == 'ok' and spi_sat.get('status') == 'ok' %>The adjusted morphology signal is primarily carried by satellites rather than BGGs: the satellite-only models give an elliptical odds ratio of \(<< ell_sat.get('cg4_odds_ratio')|fmt(2) >>\) and a spiral odds ratio of \(<< spi_sat.get('cg4_odds_ratio')|fmt(2) >>\), whereas <% if ell_bgg.get('status') == 'ok' %>the BGG elliptical coefficient is not detected after adjustment (OR \(<< ell_bgg.get('cg4_odds_ratio')|fmt(2) >>\); << pholm_text(ell_bgg.get('cg4_p_adj')) >>)<% else %>the BGG morphology model does not provide a detected adjusted counterpart<% endif %><% if sm.get('figure') %> (Fig.~\ref{fig:specialness_logistic})<% endif %>.<% endif %>

<% if sm.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sm.get('figure') >>}
    \caption{Adjusted \CG{} odds ratios for star-formation and morphology diagnostics from the pooled secondary models, including all-galaxy, satellite-only, and BGG-only morphology rows. Bars show 95\% confidence intervals; the vertical line marks equal odds.}
    \label{fig:specialness_logistic}
\end{figure}
<% endif %>
<% endif %>

<% set matched = es.get('matched_controls', {}) %>
<% if matched.get('status') == 'ok' %>
\subsection{Matched comparisons}\label{sec:matched}

<% set m_group = matched.get('group_level', {}) %>
<% set m_pc = matched.get('group_level_per_control', {}) %>
<% if m_group.get('status') == 'ok' %>
\paragraph{Group-level matched satellite-composition contrast (primary).} Because the four galaxies of a compact group are not independent, the group-level matched estimand treats each quartet as the statistical unit: for each group we count its elliptical-vote satellites (\(N_{\rm ell,sat} \in \{0,1,2,3\}\)) and match \CG{} groups one-to-one to control groups on redshift, BGG stellar mass, total quartet luminosity, and velocity dispersion (propensity caliper 0.2\,SD of the logit). We run the matching three times with the pool restricted to a single control type (one quartet per physical Lim group within each type), and once with the pool drawn from all three types combined. For the per-control matches, the paired sign-flip permutation p-value is the primary p-value; the bootstrap sign-crossing probability and bootstrap interval quantify robustness and effect-size uncertainty.

<% set comp = m_group.get('matched_control_composition', {}) %>
The pooled control match covers << m_group.get('n_cg4_groups_available') >> \CG{} groups and << m_group.get('n_control_groups_available') >> control groups, yielding << m_group.get('n_matched_groups') >> matched pairs, drawn predominantly from \CB{} (<% for key, val in comp.items() %><< val >> << key >><% if not loop.last %>, <% endif %><% endfor %>, reflecting the pool composition after deduplication). This pooled match is a secondary summary because the control catalogues overlap. Its elliptical-vote satellite fraction difference is \(\Delta = << m_group.get('delta_smooth_satellite_fraction')|fmt(3) >>\) (95\% bootstrap interval \([<< m_group.get('ci95')[0]|fmt(3) >>, << m_group.get('ci95')[1]|fmt(3) >>]\); bootstrap << pval_text(m_group.get('p')) >><% if m_group.get('p_permutation') is not none %>, permutation \(p = << m_group.get('p_permutation')|fmt(3) >>\)<% endif %>; \(B = << m_group.get('n_boot') >>\)).

The per-control matches (Table~\ref{tab:group_match_per_control}) give a consistent picture under the stated multiplicity convention: the \CG{}-minus-control fraction difference is positive and nominally detected against all three control types. <% if m_pc.get('Control4C', {}).get('status') == 'ok' %><% set pc4c = m_pc.get('Control4C', {}) %>\CC{} is the physically most stringent comparison, since its projected-core definition most closely mirrors the \CG{} compactness criterion; against \CC{}, the match yields << pc4c.get('n_matched_groups') >> pairs with \(\Delta = << pc4c.get('delta_smooth_satellite_fraction')|fmt(3) >>\) (95\% CI \([<< pc4c.get('ci95')[0]|fmt(3) >>, << pc4c.get('ci95')[1]|fmt(3) >>]\); primary permutation \(p = << pc4c.get('p_permutation')|fmt(3) >>\), bootstrap << pval_text(pc4c.get('p')) >>)<% if pc4c.get('p_permutation') is not none and pc4c.get('p_permutation') < 0.05 %>, so the satellite-composition contrast remains nominally detected even against projected ordinary-group cores<% endif %>.<% endif %> The galaxy-level match below estimates a different individual-galaxy contrast rather than a weaker version of this group-composition result.

\begin{table}[h]
\centering
\caption{Per-control group-level matched satellite-composition contrasts. \(\Delta\) is the matched mean difference in elliptical-vote satellite fraction (\CG{} minus control). The paired sign-flip permutation p-value is primary; the bootstrap p-value is the add-one sign-crossing probability used as a robustness check, with \(B = 9999\) resamples. P-values are not adjusted across the three control definitions; Appendix~\ref{app:pooled_models} reports the Holm sensitivity. The pooled row is secondary.}
\label{tab:group_match_per_control}
\begin{tabular}{lrrrr}
\hline\hline
Control & Pairs & \(\Delta\) & 95\% CI & \(p_{\rm perm}\) / \(p_{\rm boot}\) \\
\hline
<% for ctrl_label in ['Control4B', 'Control4C', 'RG4'] %>
<% set pc = m_pc.get(ctrl_label, {}) %>
<% if pc.get('status') == 'ok' %>
\<< ctrl_label.replace('Control4B','CB').replace('Control4C','CC').replace('RG4','RG') >>{} & << pc.get('n_matched_groups') >> & \(<< pc.get('delta_smooth_satellite_fraction')|fmt(3) >>\) & \([<< pc.get('ci95')[0]|fmt(3) >>, << pc.get('ci95')[1]|fmt(3) >>]\) & \(<< pc.get('p_permutation')|fmt(3) >>\) / \(<< pc.get('p')|fmt(3) >>\) \\
<% else %>
\<< ctrl_label.replace('Control4B','CB').replace('Control4C','CC').replace('RG4','RG') >>{} & --- & --- & --- & --- \\
<% endif %>
<% endfor %>
Pooled & << m_group.get('n_matched_groups') >> & \(<< m_group.get('delta_smooth_satellite_fraction')|fmt(3) >>\) & \([<< m_group.get('ci95')[0]|fmt(3) >>, << m_group.get('ci95')[1]|fmt(3) >>]\) & \(<< m_group.get('p_permutation')|fmt(3) >>\) / \(<< m_group.get('p')|fmt(3) >>\) \\
\hline
\end{tabular}
\end{table}
<% endif %>

<% set cg4_audit = es.get('selection_diagnostics', {}).get('sample_size_audit', {}).get('CG4', {}) %>
<% set cg4_total_available = cg4_audit.get('total_galaxies', matched.get('n_cg4_matched')) %>
\paragraph{Galaxy-level matched contrast (secondary).} We matched << matched.get('n_cg4_matched') >> of the << cg4_total_available >> available \CG{} galaxies one-to-one to << matched.get('n_control_matched') >> ordinary-group galaxies <% if not matched.get('replacement') %>without<% else %>with<% endif %> replacement, requiring common propensity-score support, a << matched.get('propensity_caliper_logit_sd')|fmt(1) >>-standard-deviation caliper and exact rank strata; the control pool is first deduplicated by SDSS objid, no control may share an objid with a \CG{} galaxy, and no control galaxy is used more than once. The propensity model uses \(\log M_\star\), redshift, rank, \(\log L_\mathrm{group}\), and \(\sigma_v\); projected distance is retained as a phase-space and interaction diagnostic rather than matched away, because it partly defines compactness and has limited overlap. The largest absolute standardized mean difference changed from << matched.get('max_abs_smd_before')|fmt(2) >> before matching to << matched.get('max_abs_smd_after')|fmt(2) >> afterwards; the residual imbalance is carried by \(\log L_\mathrm{group}\) and lies slightly above the conventional 0.1 threshold, which is why we read the matched contrasts together with the adjusted models rather than in isolation. Matched differences are quoted with group-blocked bootstrap intervals: pairs are resampled by the treated galaxy's compact group (never galaxy by galaxy), with \(B = << matched.get('effects', {}).get('elliptical_fraction', {}).get('n_boot', 9999) >>\) resamples and add-one empirical p-values. Pool composition, provenance, and balance details are given in Appendix~\ref{app:pooled_models}.

<% set m_ell = matched.get('effects', {}).get('elliptical_fraction', {}) %>
<% set m_quenched = matched.get('effects', {}).get('quenched_fraction', {}) %>
<% set m_resid = matched.get('effects', {}).get('residual_sSFR_starforming', {}) %>
<% if m_ell.get('status') == 'ok' %>The matched \CG{}-minus-control difference in elliptical fraction is \(<< m_ell.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_ell.get('ci95')[0]|fmt(3) >>, << m_ell.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_ell.get('p_adj')) >><% if m_ell.get('p_adj') is not none and m_ell.get('p_adj') >= 0.05 %>: directionally consistent with the adjusted models, but not detected as an individual-galaxy matched contrast once the four galaxies of a group are resampled together<% endif %>. <% endif %><% if m_quenched.get('status') == 'ok' %>The matched quenched-fraction difference is \(<< m_quenched.get('delta_cg4_minus_control')|fmt(3) >>\) (\([<< m_quenched.get('ci95')[0]|fmt(3) >>, << m_quenched.get('ci95')[1]|fmt(3) >>]\); << pholm_text(m_quenched.get('p_adj')) >>)<% if m_quenched.get('p_adj') is not none and m_quenched.get('p_adj') < 0.05 %>, supporting an enhanced quenched fraction in \CG{} after matching<% elif m_quenched.get('p_adj') is not none and m_quenched.get('p_adj') < 0.10 %>, suggestive but not formally significant after correction<% else %>, so the matched data do not establish an enhanced quenched fraction after correction<% endif %>; the star-forming and spiral fractions are exact complements of these outcomes on their complete-case subsets and are reported in Appendix~\ref{app:pooled_models}. <% endif %><% if m_resid.get('status') == 'ok' %>Among matched star-forming galaxies, the residual-sSFR difference is \(<< m_resid.get('delta_cg4_minus_control')|fmt(3) >>\) (\([<< m_resid.get('ci95')[0]|fmt(3) >>, << m_resid.get('ci95')[1]|fmt(3) >>]\); << pval_text(m_resid.get('p')) >>).<% endif %>

<% if matched.get('holm_correction_note') %>
The matched-outcome Holm correction is intentionally conservative: quenched/star-forming and elliptical/spiral diagnostics are retained in the same family even though they are exact complements on their complete-case subsets. Using a non-redundant family would not weaken the morphology conclusion.
<% endif %>

The matched individual-galaxy comparison is therefore compatible with the adjusted models but less decisive for morphology than the group-composition estimand; the quenched and star-forming-fraction contrasts are more model-dependent.
<% endif %>

<% set host = es.get('host_controlled', {}) %>
<% if host.get('status') == 'ok' %>
\subsection{Compact-group members versus other members of the same host group}\label{sec:host_controlled}

For the Embedded and Predominant \CG{} systems, whose quartets live inside richer Lim parent groups, the sharpest available test of ``compact subconfiguration versus shared host environment'' compares the \CG{} member galaxies with the \emph{non-\CG{} members of the same host group}. The primary estimator is conditional logistic regression stratified by host group: the stratum-level intercept is conditioned out in the partial likelihood, so every host-level confounder shared by all members of the same host (halo mass, richness, redshift, large-scale environment) is removed by design. As a robustness check we also fit a binomial GLM with host-group fixed-effect dummies and cluster-robust standard errors by host (FE-GLM). The design starts from << host.get('n_hosts') >> host groups (<< host.get('n_members') >> member galaxies, of which << host.get('n_cg_members') >> belong to a \CG); the models include stellar mass, host luminosity rank, and projected host-centric radius as galaxy-level covariates.
<% set host_ell = host.get('models', {}).get('elliptical', {}) %>
<% set host_qnc = host.get('models', {}).get('quenched', {}) %>
<% set host_ell_cl = host_ell.get('conditional_logit', {}) %>
<% set host_ell_fe = host_ell.get('fe_glm', {}) %>
<% set host_qnc_cl = host_qnc.get('conditional_logit', {}) %>
<% if host_ell.get('status') == 'ok' %>
For the elliptical-vote outcome, the complete-case conditional-logit sample contains << host_ell_cl.get('n') >> galaxies in << host_ell_cl.get('n_strata_total') >> host strata; the remaining design host has no usable Galaxy Zoo elliptical/spiral classification after the model complete-case filters. <% if host_ell_cl.get('n_concordant_strata', 0) > 0 %><< host_ell_cl.get('n_concordant_strata') >> concordant strata (all-0 or all-1 outcomes within one host) are automatically excluded, leaving << host_ell_cl.get('n_informative_strata') >> informative strata.<% else %>All << host_ell_cl.get('n_informative_strata', host.get('n_hosts')) >> complete-case host strata are informative; no concordant strata are dropped.<% endif %> The elliptical-vote odds ratio for \CG{} membership is \(<< host_ell.get('cg_member_odds_ratio')|fmt(2) >>\) (95\% CI \([<< host_ell.get('cg_member_ci95')[0]|fmt(2) >>, << host_ell.get('cg_member_ci95')[1]|fmt(2) >>]\); << pholm_text(host_ell.get('cg_member_p_adj')) >>)<% if host_qnc.get('status') == 'ok' %>, and the quenched odds ratio is \(<< host_qnc.get('cg_member_odds_ratio')|fmt(2) >>\) (<< pholm_text(host_qnc.get('cg_member_p_adj')) >>; << host_qnc_cl.get('n') >> complete cases in << host_qnc_cl.get('n_strata_total') >> host strata)<% endif %>.<% if host_ell_fe.get('status') == 'ok' %> The FE-GLM yields a similar elliptical-vote point estimate but a smaller estimated uncertainty and nominal significance: OR \(<< host_ell_fe.get('is_CG_member_odds_ratio')|fmt(2) >>\) (95\% CI \([<< host_ell_fe.get('is_CG_member_ci95')[0]|fmt(2) >>, << host_ell_fe.get('is_CG_member_ci95')[1]|fmt(2) >>]\); nominal \(p = << host_ell_fe.get('is_CG_member_p')|fmt(3) >>\))<% if host_ell_fe.get('separation_flag') %>, though partial separation is flagged<% endif %>. We retain the conditional-logit estimator as primary because it conditions out the host-specific intercept directly.<% endif %>
<% if host_ell.get('cg_member_p_adj') is not none and host_ell.get('cg_member_p_adj') >= 0.05 %>
At fixed stellar mass, rank, and host-centric radius, no residual excess is detected for compact-subconfiguration membership \emph{within} a shared host under the primary conditional estimator. The confidence interval nevertheless permits a range of moderate effects, so this result does not establish equality. It is consistent with the per-control asymmetry of Sect.~\ref{sec:primary_contrasts}: the \CG{} excess appears more clearly against galaxies of \emph{other} groups than against co-members of the same host, pointing to dense projected-core location rather than compact-group membership alone.
<% endif %>
<% endif %>
<% endif %>

<% set crowd = es.get('morphology_robustness', {}) %>
<% set sweep = es.get('morphology_threshold_sweep', {}) %>
<% set sersic = sweep.get('sersic_early_late', {}) %>
<% if crowd.get('status') == 'ok' or sweep.get('status') == 'ok' %>
\subsection{Morphology robustness}\label{sec:morph_crowding}\label{sec:morph_threshold}

Because Galaxy Zoo morphology is image-based and the adopted vote-fraction cut is one of several defensible choices, we subject the morphology contrast to a battery of robustness tests, summarized in Table~\ref{tab:morph_robustness}.
<% if crowd.get('status') == 'ok' %>
<% set crowd_rows = crowd.get('fractions_after_excluding_close', []) %>
<% set crowd_cg_list = crowd_rows|selectattr('sample', 'equalto', 'CG4')|list %>
<% set crowd_c4c_list = crowd_rows|selectattr('sample', 'equalto', 'Control4C')|list %>
<% set crowd_cg = crowd_cg_list|first if crowd_cg_list|length > 0 else {} %>
<% set crowd_c4c = crowd_c4c_list|first if crowd_c4c_list|length > 0 else {} %>
<% set crowd_tests = crowd.get('exact_tests_after_excluding_close', []) %>
<% set crowd_c4c_ell_list = crowd_tests|selectattr('control', 'equalto', 'Control4C')|selectattr('outcome', 'equalto', 'elliptical')|list %>
<% set crowd_c4c_ell = crowd_c4c_ell_list|first if crowd_c4c_ell_list|length > 0 else {} %>
<% set crowd_model_ell = crowd.get('adjusted_models_with_close_flag', {}).get('elliptical', {}) %>
<% set crowd_close_term = crowd_model_ell.get('terms', {}).get('close_neighbour_55arcsec', {}) %>
After excluding galaxies whose nearest projected neighbour within the catalogue group lies within << crowd.get('threshold_arcsec')|fmt(0) >> arcsec (retaining << crowd_cg.get('n_after_excluding_close') >> \CG{} and << crowd_c4c.get('n_after_excluding_close') >> \CC{} galaxies), the \CG{} elliptical fraction \emph{rises} relative to the raw full-sample value in Table~\ref{tab:morphologies}, rather than falling, and the \CG{}--\CC{} contrast remains nominally significant in this diagnostic. This argues against the simplest interpretation that the elliptical excess is produced solely by crowding in the closest pairs. <% if crowd_close_term %>The close-neighbour flag is itself associated with elliptical-vote morphology (OR \(<< crowd_close_term.get('odds_ratio')|fmt(2) >>\); << pval_text(crowd_close_term.get('p')) >>), so this test does not prove that Galaxy Zoo classifications are immune to crowding; it rules out the closest-pair artefact as the sole origin of the result.<% endif %>
<% endif %>
<% if sweep.get('status') == 'ok' %>
Across classification thresholds of 0.4--0.8 on the deduplicated pooled sample, the adjusted \CG{} elliptical odds ratio varies only mildly<% if sweep.get('qualitatively_stable') %> and never changes sign<% endif %> (Table~\ref{tab:morph_robustness}; the unclassified fraction grows from << sweep.get('thresholds', {}).get('0.4', {}).get('fraction_unclassified')|pct(0) >>\% to << sweep.get('thresholds', {}).get('0.8', {}).get('fraction_unclassified')|pct(0) >>\% over the sweep). A threshold-free check models the debiased elliptical vote fraction \(p_E\) itself, and a structural cross-check independent of Galaxy Zoo uses the \citet{Simard2011} pure-S\'ersic index (early type: \(n_g > 2.5\)<% if sersic.get('cg4_odds_ratio') %>; << sersic.get('coverage')|pct(0) >>\% coverage<% endif %>); both confirm the direction of the contrast<% if sersic.get('cg4_odds_ratio') %>, the latter subject to the differential Simard completeness discussed in Sect.~\ref{sec:size_results}<% endif %>.
<% endif %>

\begin{table}
\centering
\caption{Robustness of the \CG{} morphology contrast. Odds ratios (OR) are adjusted \CG{} coefficients; the continuous row is the adjusted offset in the debiased elliptical vote fraction \(p_E\). P-values in this compact diagnostic table are unadjusted unless explicitly labelled.}
\label{tab:morph_robustness}
\small
\tabcolsep=2pt
\begin{tabular}{ll}
\hline\hline
Test & Result \\
\hline
<% if crowd.get('status') == 'ok' %>
\makecell[l]{Exclude neighbours\\within 55 arcsec} & \makecell[l]{elliptical fraction << crowd_cg.get('elliptical_fraction_after_excluding_close')|pct(1) >>\% (\CG) vs\\<< crowd_c4c.get('elliptical_fraction_after_excluding_close')|pct(1) >>\% (\CC); << pval_text(crowd_c4c_ell.get('p_value')) >>} \\
<% if crowd_model_ell.get('status') == 'ok' %>
\makecell[l]{Close-neighbour-flag\\model} & \makecell[l]{OR \(<< crowd_model_ell.get('cg4_odds_ratio')|fmt(2) >>\) \([<< crowd_model_ell.get('cg4_ci95')[0]|fmt(2) >>, << crowd_model_ell.get('cg4_ci95')[1]|fmt(2) >>]\);\\<< pval_text(crowd_model_ell.get('cg4_p')) >>} \\
<% endif %>
<% endif %>
<% if sweep.get('status') == 'ok' %>
Threshold sweep 0.4--0.8 & OR \(<< sweep.get('or_range')[0]|fmt(2) >>\)--\(<< sweep.get('or_range')[1]|fmt(2) >>\) \\
Continuous \(p_E\) offset & \makecell[l]{\(<< sweep.get('continuous', {}).get('cg4_coefficient')|fmt(3) >>\) \([<< sweep.get('continuous', {}).get('cg4_ci95')[0]|fmt(3) >>, << sweep.get('continuous', {}).get('cg4_ci95')[1]|fmt(3) >>]\);\\<< pval_text(sweep.get('continuous', {}).get('cg4_p')) >>} \\
<% if sersic.get('cg4_odds_ratio') %>
S\'ersic \(n_g > 2.5\) cross-check & OR \(<< sersic.get('cg4_odds_ratio')|fmt(2) >>\); << pval_text(sersic.get('cg4_p')) >> \\
<% endif %>
<% endif %>
\hline
\end{tabular}
\end{table}
<% endif %>

<% set tidal = es.get('tidal_indices', {}) %>
<% if tidal.get('status') == 'ok' %>
\subsection{Pairwise tidal index}\label{sec:tidal_index}

The pairwise analysis reconstructs projected separations and velocity differences for << tidal.get('n_galaxies_with_pairs') >> galaxies and refits the adjusted models with and without the pairwise tidal index \(\sum_j M_{\star,j}/R_{ij}^3\) (in \(\msun\,\mathrm{kpc}^{-3}\)). The two model variants estimate \emph{different quantities}. The model without the index estimates the \emph{total} association between compact-group membership and the outcome at fixed stellar mass, redshift, rank, and group-scale covariates; the model with the index estimates the association \emph{at fixed projected local density}. Because the index is built from \(R^{-3}\)-weighted projected separations, it is mathematically entangled with the compact-group selection itself (compact groups are selected to have small projected separations), so conditioning on it partially conditions on the exposure. A reduction of the \CG{} coefficient when the index is added is thus \emph{conditional attenuation}, not mediation: it does not show that local density accounts for the morphology contrast, and it cannot, because the causal ordering of selection, density, and morphology is not identified by projected snapshots. The baseline models in this subsection are refit on the pairwise-analysis complete-case sample, so their coefficients need not be numerically identical to the corresponding coefficients in Sect.~\ref{sec:primary_contrasts} or Appendix~\ref{app:pooled_models}; because \(R_{ij}\) is projected, the term is a local projected-density and immediate-neighbour proxy rather than a direct tidal-history measurement.

<% set tidal_ell = tidal.get('models', {}).get('elliptical', {}) %>
<% set ell_base = tidal_ell.get('baseline', {}) %>
<% set ell_tidal = tidal_ell.get('with_tidal_index', {}) %>
<% if ell_base.get('status') == 'ok' and ell_tidal.get('status') == 'ok' %>
For elliptical morphology, the baseline \CG{} odds ratio is << ell_base.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(ell_base.get('cg4_p')) >>), whereas the model including the tidal index gives a \CG{} odds ratio of << ell_tidal.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(ell_tidal.get('cg4_p')) >>); the tidal-index term itself has odds ratio << ell_tidal.get('terms', {}).get('log_tidal_index', {}).get('odds_ratio')|fmt(2) >> (<< pval_text(ell_tidal.get('terms', {}).get('log_tidal_index', {}).get('p')) >>). The projected tidal-density proxy therefore \emph{attenuates} the \CG{} coefficient; given the selection entanglement described above, this conditional attenuation must not be read as the index accounting for the effect, and the result remains an exploratory projected-pair diagnostic.
<% endif %>
<% set tidal_quenched = tidal.get('models', {}).get('quenched', {}) %>
<% set quenched_base = tidal_quenched.get('baseline', {}) %>
<% set quenched_tidal = tidal_quenched.get('with_tidal_index', {}) %>
<% if quenched_base.get('status') == 'ok' and quenched_tidal.get('status') == 'ok' %>
For quenched status, the baseline \CG{} odds ratio is << quenched_base.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(quenched_base.get('cg4_p')) >>), while the model including the tidal index gives << quenched_tidal.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(quenched_tidal.get('cg4_p')) >>), with a tidal-index term of odds ratio << quenched_tidal.get('terms', {}).get('log_tidal_index', {}).get('odds_ratio')|fmt(2) >> (<< pval_text(quenched_tidal.get('terms', {}).get('log_tidal_index', {}).get('p')) >>).
<% endif %>
<% endif %>

\subsection{Secondary outcomes}\label{sec:secondary}

\paragraph{Star formation.} The adjusted and matched analyses above already bound the star-formation contrast: the per-control quenched odds ratios are control-dependent (Sect.~\ref{sec:primary_contrasts}), the matched quenched and star-forming fractions show no detected offset after correction (Sect.~\ref{sec:matched}), and the main-sequence residuals are a secondary diagnostic (Sect.~\ref{sec:raw}).
<% set ssfr_alt = r.get('sSFR_robustness', {}) %>
<% if ssfr_alt.get('status') == 'ok' and ssfr_alt.get('primary_comparison') %>
The interpretation does not rely on the exact GMM division of the sSFR plane: repeating the key satellite comparison with a fixed threshold, << ssfr_alt.get('threshold_label') >>, gives \CG{} and \RG{} satellite star-forming fractions of << ssfr_alt.get('cg4', {}).get('starforming_fraction_fmt') >>\% and << ssfr_alt.get('primary_comparison', {}).get('control', {}).get('starforming_fraction_fmt') >>\%, respectively, a difference of \(<< ssfr_alt.get('primary_comparison', {}).get('delta_starforming_fraction_pct_fmt') >>\) percentage points (<< p_from_fmt(ssfr_alt.get('primary_comparison', {}).get('fisher_p_fmt')) >>). The qualitative hierarchy is unchanged: the morphology result is independent of the sSFR threshold, whereas the quenched/star-forming contrast remains more sensitive to modelling choices.
<% endif %>

<% set recent = es.get('recent_quenching', {}) %>
<% if recent.get('status') in ['ok', 'limited'] %>
\paragraph{\texorpdfstring{\(\mathrm{H}\alpha\)}{Ha} emission.}
<% set strong_halpha_threshold = recent.get('classification_thresholds', {}).get('strong_halpha_emission')|replace('_', '\\_')|replace('\\,\\AA', '\\,\\text{\\AA}') %>
<% if recent.get('status') == 'limited' %>
Our retrieved catalogues do not include the age-sensitive indices \(D_n4000\) and H\(\delta_A\) (available in the MPA-JHU \texttt{galSpecIndx} product), so we defer a post-starburst classification to follow-up work and perform only a limited \(\mathrm{H}\alpha\)-equivalent-width comparison, using the SDSS convention in which negative equivalent width denotes emission and classifying strong \(\mathrm{H}\alpha\) emission by \(<< strong_halpha_threshold >>\).
<% elif recent.get('status') == 'ok' %>
The available \(D_n4000\), H\(\delta_A\), and \(\mathrm{H}\alpha\) measurements permit a direct recently-quenched classification.
<% endif %>
<% if recent.get('status') == 'limited' %>
The strong-emission fraction is <% for sample, item in recent.get('fractions_by_sample', {}).items() -%><< item.get('strong_emission_fraction')|pct(1) >>\% in << sample|replace('CG4', '\CG{}')|replace('Control4B', '\CB{}')|replace('Control4C', '\CC{}')|replace('RG4', '\RG{}') >><% if not loop.last %>, <% else %>.<% endif %><% endfor %>
<% for comp_name, comp in recent.get('comparisons', {}).items() %>
<% if comp.get('status') == 'ok' %>
Relative to << comp_name|replace('CG4_vs_', '')|replace('Control4B', '\CB{}')|replace('Control4C', '\CC{}')|replace('RG4', '\RG{}') >>, the \CG{} strong-emission fraction differs by \(<< comp.get('strong_emission_fraction_difference')|fmt(3) >>\) (<< pholm_text(comp.get('p_adj')) >>).
<% endif %>
<% endfor %>
These limited results indicate a lower strong-emission fraction in \CG{} than in each control, but they do not provide a direct post-starburst or recently-quenched classification.
<% endif %>
<% endif %>

<% set agn = es.get('agn_environment', {}) %>
<% if agn.get('status') in ['ok', 'limited'] %>
\paragraph{Emission-line and AGN-like populations.}\label{sec:agn_env}
The emission-line analysis classifies BPT star-forming, composite, and AGN-like populations for galaxies with the required diagnostic line ratios: composite galaxies lie between the \citet{Kauffmann+03a} and \citet{Kewley+01} demarcations in the [N\,II] BPT plane, and AGN-like galaxies lie above the \citet{Kewley+01} line; galaxies with a pre-existing AGN flag but no BPT line ratios are also counted as AGN-like, and BPT-unclassified galaxies are excluded from the AGN-like fraction denominator rather than treated as confirmed non-AGN. Among BPT-classified \CG{} galaxies, the AGN-like fraction is << agn.get('fractions_by_sample', {}).get('CG4', {}).get('agn_like_fraction')|pct(1) >>\%.
<% set agn_model = agn.get('mass_adjusted_models', {}).get('all', {}) %>
<% if agn_model.get('status') == 'ok' %>
After mass and environment adjustment, the \CG{} AGN-like odds ratio is << agn_model.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(agn_model.get('cg4_p')) >>)<% if agn_model.get('cg4_p') is not none and agn_model.get('cg4_p') < 0.05 %>: AGN-like activity remains associated with compact-group membership after adjustment<% else %>: AGN-like activity does not provide a robust independent compact-group discriminator<% endif %>.
<% endif %>
<% endif %>

<% set fossil = es.get('fossilness', {}) %>
<% if fossil.get('status') == 'ok' %>
\paragraph{Magnitude gaps and fossil-like assembly.}
We define \(\Delta m_{12}=M_{r,2}-M_{r,1}\) and \(\Delta m_{14}=M_{r,4}-M_{r,1}\), so that a more dominant BGG has a positive, larger gap.
<% set dm12 = fossil.get('sample_comparisons', {}).get('Delta_m12', {}) %>
<% if dm12.get('status') == 'ok' %>
For \(\Delta m_{12}\), the \CG{} and control medians are \(<< dm12.get('median_cg4')|fmt(2) >>\) and \(<< dm12.get('median_control')|fmt(2) >>\) mag, respectively (<< pholm_text(dm12.get('p_adj')) >>)<% endif %><% set dm14 = fossil.get('sample_comparisons', {}).get('Delta_m14', {}) %><% if dm14.get('status') == 'ok' %>; for \(\Delta m_{14}\), they are \(<< dm14.get('median_cg4')|fmt(2) >>\) and \(<< dm14.get('median_control')|fmt(2) >>\) mag (<< pholm_text(dm14.get('p_adj')) >>)<% endif %>.
<% if not fossil.get('magnitude_gap_significant') %>
The compact-group morphology signal is therefore not accompanied by a detected excess in these simple fossil-like magnitude-gap indicators.
<% endif %>
<% set gap_quenched = fossil.get('correlations', {}).get('Delta_m12_vs_quenched_satellite_fraction', {}) %>
<% if gap_quenched.get('status') == 'ok' %>
The correlation between \(\Delta m_{12}\) and quenched satellite fraction is \(\rho_{\rm S}=<< gap_quenched.get('spearman_rho')|fmt(2) >>\) (<< pholm_text(gap_quenched.get('p_adj')) >>).
<% endif %>
<% endif %>

<% set phase = phase_space_segregation if phase_space_segregation else es.get('phase_space_segregation', es.get('phase_space', {})) %>
<% if phase.get('status') == 'ok' %>
\paragraph{Projected phase space.}
<% set pst = phase.get('text_summary', {}) %>
We ask whether the \CG{}--\RG{} satellite contrasts vary with projected position relative to the BGG. The analysis uses << pst.get('cg_satellite_n') >> \CG{} and << pst.get('rg_satellite_n') >> \RG{} satellites, excludes rank-1 galaxies, ranks satellites by projected BGG-centric distance, and, when available, uses \(|\Delta v_{\rm BGG}|/\sigma_v\) as a projected phase-space diagnostic; such coordinates are statistical proxies for infall state or quenching time, not orbital reconstructions \citep{Mahajan+11,Oman&Hudson16,Wetzel+13}.
<% set inner_early_delta_display = ((pst.get('cg_earlytype_fraction_inner') * 100)|round(1)) - ((pst.get('rg_earlytype_fraction_inner') * 100)|round(1)) %>
In the innermost satellite-distance bin, the quenched fractions are << pst.get('cg_quenched_fraction_inner')|pct(1) >>\% for \CG{} and << pst.get('rg_quenched_fraction_inner')|pct(1) >>\% for \RG, a difference of \(<< pst.get('delta_quenched_fraction_inner')|pct(1) >>\) percentage points; the corresponding early-type fractions are << pst.get('cg_earlytype_fraction_inner')|pct(1) >>\% and << pst.get('rg_earlytype_fraction_inner')|pct(1) >>\% (difference \(<< inner_early_delta_display|fmt(1) >>\) points).
<% if pst.get('quenched_radial_trend') == 'stronger_inner' %>
The quenched-fraction contrast is larger in the inner bin than in the outer bin, favouring a centrally concentrated environmental component.
<% elif pst.get('quenched_radial_trend') == 'stronger_outer' %>
The quenched-fraction contrast is not confined to the inner bin; it is at least as large at larger projected distance, which is more consistent with preprocessing or a global sample difference.
<% elif pst.get('quenched_radial_trend') == 'similar_at_inner_and_outer_ranks' %>
The quenched-fraction contrast is similar between the inner and outer distance ranks, so the data do not isolate the \CG{} offset to the immediate BGG environment.
<% else %>
The radial dependence of the quenched-fraction contrast cannot be determined robustly with the available bin counts.
<% endif %>
<% set quenched_model = phase.get('regression_results', {}).get('quenched_distance', {}) %>
<% if quenched_model.get('status') == 'ok' %>
After adjusting for stellar mass, redshift, distance bin, and available group-scale controls, the quenched-status model gives a \CG{} odds ratio of << pst.get('quenched_model_sample_cg_or')|fmt(2) >> (95\% CI << pst.get('quenched_model_sample_cg_ci_low')|fmt(2) >>--<< pst.get('quenched_model_sample_cg_ci_high')|fmt(2) >>; << pval_text(pst.get('quenched_model_sample_cg_p')) >>)<% endif %><% set early_model = phase.get('regression_results', {}).get('early_type_distance', {}) %><% if early_model.get('status') == 'ok' %>, and the analogous early-type model gives << pst.get('earlytype_model_sample_cg_or')|fmt(2) >> (<< pval_text(pst.get('earlytype_model_sample_cg_p')) >>)<% endif %>.
<% set matched_phase = phase.get('matched_robustness', {}) %>
<% if matched_phase.get('status') == 'ok' %>
A nearest-neighbour mass--redshift matched check on << matched_phase.get('n_cg4_matched') >> \CG{}--\RG{} satellite pairs gives matched quenched- and early-type-fraction differences of \(<< pst.get('matched_delta_quenched')|pct(1) >>\) and \(<< pst.get('matched_delta_earlytype')|pct(1) >>\) percentage points, respectively.
<% endif %>
In compact groups the member counts are small, velocity dispersions are noisy, and projection mixes recently accreted and long-resident satellites, so we treat these bins as an observational diagnostic of where the contrast appears, not as a dynamical clock.
<% endif %>

\paragraph{Optical colours.}
We derive the $(u-r)$, $(u-g)$, $(g-r)$ and $(r-i)$ colours from the matched SDSS photometry and, because stellar mass confounds colour comparisons \citep{Alpaslan+15}, model each colour jointly with $\log(M_\star/M_\odot)$ and redshift, with cluster-robust standard errors. The colour-analysis subset is <% if r.Colour_robustness_matching_biased -%>not representative of the full galaxy catalogues -- matched galaxies are systematically less massive, more actively star-forming, and more frequently satellites than unmatched galaxies -- <% else -%>broadly representative of the full catalogues in the audited quantities, <% endif %>so colours are treated as a secondary diagnostic. In the pooled satellite comparison, <% if r.Colour_robustness_significant_baseline_blue|length == 0 -%>no blue \CG{} coefficient is detected after Holm correction<% else -%>\CG{} satellites are detected as bluer in <% for item in r.Colour_robustness_significant_baseline_blue -%><< item.colour_label >><% if not loop.last %>, <% endif %><% endfor %><% endif %>, and the adjusted coefficients are <% if r.Colour_robustness_summary.control_dependent -%>control-sample dependent<% else -%>consistent across the control catalogues<% endif %>. The data therefore do not establish a robust global blue or red offset for \CG{} satellites; the full colour--mass, model-dependence, and radial diagnostics are given in Appendix~\ref{sec:optical_colours}.

\paragraph{Group-scale diagnostics.}
A broader set of environmental diagnostics is reported in Appendix~\ref{app:env_diagnostics}: <% if r.get('Dist2BGG_significant_count', 0) > 0 %>significant monotonic sSFR--distance trends in individual samples (Appendix~\ref{app:dist2bgg})<% else %>no monotonic sSFR--distance trends<% endif %>; dominance splits whose clearest separation is in group luminosity rather than in any \CG{}-specific property; morphology tests against the Zheng--Shen class, \(\Delta m_{12}\), and the BGG luminosity fraction, none of which survives false-discovery-rate control; and a crossing-time diagnostic that primarily tracks virial mass and mass-to-light ratio.

<% if sz.get('status') == 'ok' %>
\subsection{Galaxy sizes at fixed stellar mass}\label{sec:size_results}

<% set size_avail = sz.get('availability_audit', {}) %>
<% set size_adj = sz.get('adjusted', {}) %>
<% set size_adj_all = size_adj.get('all', {}) %>
<% set size_adj_sat = size_adj.get('satellites', {}) %>
<% set size_adj_bgg = size_adj.get('bgg', {}) %>
<% set size_matched = sz.get('matched', {}) %>
<% set size_m_simard = size_matched.get('effects', {}).get('delta_log_Rchl_r', {}) %>
<% set size_m_petro = size_matched.get('effects', {}).get('delta_log_petroR50', {}) %>
<% set size_m_conc = size_matched.get('effects', {}).get('delta_concentration', {}) %>
<% set size_crowd = sz.get('crowding', {}) %>
<% set size_crowd_excl = size_crowd.get('excluding_close', {}).get('satellites', {}) %>
<% set size_crowd_flag = size_crowd.get('with_close_flag', {}).get('satellites', {}) %>
<% set size_petro_all = sz.get('petrosian', {}).get('all', {}) %>
<% set size_petro_sat = sz.get('petrosian', {}).get('satellites', {}) %>
<% set size_md = sz.get('measure_delta', {}) %>
<% set size_tidal = sz.get('tidal', {}) %>
<% set size_radial_cg = sz.get('radial', {}).get('correlations', {}).get('CG4', {}) %>
<% set size_radial_ctrl = sz.get('radial', {}).get('correlations', {}).get('pooled_controls', {}) %>
<% set size_conc_sat = sz.get('concentration', {}).get('satellites', {}) %>
<% set size_strata = sz.get('morphology_strata', {}) %>
<% set size_percontrol = sz.get('per_control', {}).get('comparisons', {}) %>

We now test whether \CG{} galaxies differ in size at fixed stellar mass, using the seeing-corrected Simard half-light radii as the primary measure and the Petrosian radii as the model-independent cross-check (Sect.~\ref{sec:size_data}). The Simard measure resolves << size_avail.get('cg4_simard_fraction')|pct(1) >>\% of the \CG{} galaxies and << size_avail.get('pooled_control_simard_fraction')|pct(1) >>\% of the pooled control galaxies, while the Petrosian radius is essentially complete in every sample (Appendix Fig.~\ref{fig:data_availability}). <% if size_avail.get('completeness_caveat') %>Because the \CG{}--control difference in Simard completeness exceeds five percentage points, every Simard-based coefficient below carries a differential-completeness caveat and is read together with the Petrosian rerun, which does not share this incompleteness.<% else %>The \CG{} and pooled-control Simard completeness agree to within five percentage points, so differential incompleteness is not a first-order concern for the coefficients below.<% endif %>

<% if size_adj_sat.get('status') == 'ok' %>
For satellite galaxy size, the \CG{} offset at fixed stellar mass, redshift, and available group-scale covariates is \(<< size_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex (\(<< size_adj_sat.get('pct_equivalent')|fmt(1) >>\)\%), with 95\% confidence interval \([<< size_adj_sat.get('ci_low')|fmt(3) >>, << size_adj_sat.get('ci_high')|fmt(3) >>]\) and << pholm_text(size_adj_sat.get('p_holm')) >>, over << size_adj_sat.get('n') >> satellites in << size_adj_sat.get('n_groups') >> physical groups.
<% endif %>
<% if size_adj_all.get('status') == 'ok' %>
The all-galaxy model gives \(<< size_adj_all.get('cg4_delta_dex')|fmt(3) >>\) dex (95\% CI \([<< size_adj_all.get('ci_low')|fmt(3) >>, << size_adj_all.get('ci_high')|fmt(3) >>]\); << pholm_text(size_adj_all.get('p_holm')) >>)<% if size_adj_bgg.get('status') == 'ok' %>, and the BGG-only model gives \(<< size_adj_bgg.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_adj_bgg.get('p_holm')) >>)<% endif %>.
<% endif %>
<% if szv.get('primary_all_significant') or szv.get('primary_satellites_significant') %>
The primary family therefore detects a \CG{} size offset: at fixed stellar mass, \CG{} galaxies are << szv.get('direction') >> than their regular-group counterparts.
<% else %>
No coefficient in this primary family is detected after Holm correction, so the pooled adjusted models do not establish a \CG{} size offset at fixed stellar mass.
<% endif %>
The per-control versions of the fit are descriptive (Appendix Fig.~\ref{fig:size_forest_per_control}); the fitted \CG{} coefficient is <% for control_key, control_label in [('Control4B', '\\CB{}'), ('Control4C', '\\CC{}'), ('RG4', '\\RG{}')] %><% set control_fit = size_percontrol.get(control_key, {}) %><% if control_fit.get('status') == 'ok' %>\(<< control_fit.get('cg4_delta_dex')|fmt(3) >>\) dex against << control_label >><% if not loop.last %>, <% else %> (raw p-values with Benjamini--Hochberg control inside this figure family)<% endif %><% endif %><% endfor %>. The sign and significance of the \CG{} coefficient depend on the adopted control, so we do not interpret any single per-control contrast on its own.

<% if size_matched.get('status') == 'ok' %>
The one-to-one matched comparison re-derives the same << size_matched.get('n_pairs') >> propensity pairs as the matched-control analysis, with a separate Holm family for the three size outcomes so that the published matched-outcome family is untouched. <% if size_m_simard.get('status') == 'ok' %>On the << size_m_simard.get('n_pairs_with_sizes') >> pairs in which both members have valid Simard sizes, the mean \CG{}-minus-control difference in \(\log_{10} R_{\rm chl,r}\) is \(<< size_m_simard.get('mean_delta')|fmt(3) >>\) dex (95\% bootstrap interval \([<< size_m_simard.get('mean_ci95')[0]|fmt(3) >>, << size_m_simard.get('mean_ci95')[1]|fmt(3) >>]\); << pholm_text(size_m_simard.get('p_holm')) >>).<% endif %> <% if size_m_petro.get('status') == 'ok' %>The corresponding Petrosian difference is \(<< size_m_petro.get('mean_delta')|fmt(3) >>\) dex (<< pholm_text(size_m_petro.get('p_holm')) >>)<% endif %><% if size_m_conc.get('status') == 'ok' %>, and the matched concentration difference is \(<< size_m_conc.get('mean_delta')|fmt(3) >>\) (<< pholm_text(size_m_conc.get('p_holm')) >>)<% endif %>.
<% if szv.get('survives_matching') and not szv.get('primary_satellites_significant') %>
The matched pairs therefore prefer << szv.get('direction') >> \CG{} sizes even though the pooled adjusted coefficient is consistent with zero. The matched control set is dominated by the sample whose per-control fit carries the same sign, so we read this as a control-dependent contrast rather than as an independent confirmation of a global offset.
<% endif %>
<% endif %>

<% if size_petro_sat.get('status') == 'ok' %>
The Petrosian rerun, with the field seeing as an additional standardized covariate, gives a satellite coefficient of \(<< size_petro_sat.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_petro_sat.get('p_holm')) >>)<% if size_petro_all.get('status') == 'ok' %> and an all-galaxy coefficient of \(<< size_petro_all.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_petro_all.get('p_holm')) >>)<% endif %>.
<% endif %>
<% if size_conc_sat.get('status') == 'ok' %>
The concentration index \(C = R_{90,r}/R_{50,r}\) provides a model-independent structural proxy: the adjusted satellite coefficient is \(<< size_conc_sat.get('cg4_delta')|fmt(3) >>\) (<< pholm_text(size_conc_sat.get('p_holm')) >>)<% if size_conc_sat.get('p_holm') is not none and size_conc_sat.get('p_holm') < 0.05 %>, a detected concentration difference at fixed mass<% else %>, so no \CG{} concentration-index shift is detected at fixed mass<% endif %>.
<% endif %>
Crowding, measure-difference, tidal-index, radial, and morphology-stratified size diagnostics are given in Appendix~\ref{app:size_details}.
<% endif %>

\section{Discussion: drivers of structural evolution in compact groups}\label{sec:discussion}
<% set discussion_es = extended_specialness if extended_specialness else {} %>
<% set discussion_tidal = discussion_es.get('tidal_indices', {}) %>
<% set discussion_tidal_ell = discussion_tidal.get('models', {}).get('elliptical', {}) %>
<% set discussion_ell_base = discussion_tidal_ell.get('baseline', {}) %>
<% set discussion_ell_tidal = discussion_tidal_ell.get('with_tidal_index', {}) %>
<% set discussion_tidal_term = discussion_ell_tidal.get('terms', {}).get('log_tidal_index', {}) %>
<% set d_mpc = (discussion_es.get('matched_controls', {}) or {}).get('group_level_per_control', {}) %>
<% set d_pc4c = d_mpc.get('Control4C', {}) if d_mpc else {} %>

\subsection{The morphology hierarchy and its estimands}\label{sec:morph_estimands}

The clearest residual difference between \CG{} and the regular-group controls is morphological: a higher Galaxy~Zoo~1 elliptical-vote fraction and a lower spiral-vote fraction, carried primarily by satellites. The inferential framework of Sect.~\ref{sec:results} shows, however, that the strength of this contrast depends on the estimand. At the galaxy level, the per-control adjusted excess is strong against \CB{} and \RG{} but not detected against the projected cores \CC{}. At the group level, the matched elliptical-vote satellite fraction is higher in \CG{} than in every control (Table~\ref{tab:group_match_per_control})<% if d_pc4c.get('status') == 'ok' and d_pc4c.get('p_permutation') is not none and d_pc4c.get('p_permutation') < 0.05 %>, including \CC{} under the stated per-control convention<% endif %>. The galaxy-level matched contrast is directionally consistent but not detected once the four galaxies of a group are resampled together, and the primary within-host stratified estimator of Sect.~\ref{sec:host_controlled} detects no additional residual excess of \CG{} members over co-members of the same host.

These results are not contradictory because the analyses differ in their matched populations, conditioning sets, outcomes, and effect scales. Since each quartet contributes three satellites, the distinction is not simply one of weighting galaxies versus groups. The group-level analysis matches systems on group properties and estimates a difference in satellite composition, whereas the galaxy-level analyses estimate individual conditional odds ratios or matched individual-galaxy differences after adjustment for galaxy- and group-level covariates. The conditional odds-ratio scale is also non-collapsible, and the uncertainty structures differ between clustered model-based inference, paired group resampling, paired sign-flip tests, and within-host conditioning. The observed pattern is therefore best read as estimand dependence: the \CG{} satellite-composition contrast remains visible at the system level, while the adjusted individual-galaxy and shared-host contrasts are weaker or not detected under their own conditioning sets.

The pairwise tidal-index experiment strengthens this interpretation while also narrowing it. <% if discussion_ell_base.get('status') == 'ok' and discussion_ell_tidal.get('status') == 'ok' -%>For elliptical morphology, the pairwise complete-case baseline \CG{} odds ratio is \(<< discussion_ell_base.get('cg4_odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_ell_base.get('cg4_p')) >>). After adding the projected pairwise tidal-index proxy, the \CG{} odds ratio decreases to \(<< discussion_ell_tidal.get('cg4_odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_ell_tidal.get('cg4_p')) >>), while the tidal-index term itself has odds ratio \(<< discussion_tidal_term.get('odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_tidal_term.get('p')) >>).<% else -%>In the available models, adding the projected pairwise tidal-index proxy reduces the \CG{} morphology coefficient while the tidal-index term is itself informative.<% endif %> This behaviour is expected if compact groups are selecting galaxies in locally dense projected configurations where repeated close companions, rather than the catalogue label alone, are linked to the elliptical/early-type-like population.

This proxy uses projected separations, so it may be inflated by line-of-sight alignments and is partly entangled with compact-group selection itself: compact groups are selected to be compact in projection. We therefore read the tidal-index result as a continuous local projected-density or immediate-neighbour proxy, not as a direct time-dependent tidal-field measurement. The \CC{} comparison makes the test deliberately stringent, because \CC{} is also selected as a projected compact core around a BGG. The persistence of a \CG{}--\CC{} morphology contrast in the closest-neighbour exclusion test shows that the \CG{} label is not reduced to the simplest crowding artefact; at the same time, the tidal-index model indicates that the \CG{} coefficient attenuates after conditioning on a more continuous projected-density proxy. We therefore interpret the residual \CG{}--\CC{} contrast as consistent with \CG{} selecting a more extreme part of the local-density distribution, rather than as proof that compact groups define a separate causal channel.

This reading is consistent with compact groups identifying the high-density, high-interaction tail of group environments -- some genuinely compact and dynamically evolved, others dense cores or substructures inside larger haloes -- with the physically relevant quantity being the accumulated local interaction history, whether in the present configuration or through preprocessing. It also matches the construction result that many compact groups are embedded in or associated with larger structures, and the stronger structural signatures reported for non-isolated compact groups in recent S-PLUS work. We note that no harmonised halo-mass estimate is available for these catalogues (Appendix~\ref{app:selection}), so, although the adjusted models control for group luminosity and velocity dispersion, a residual halo-mass contribution to the morphology signal cannot be fully excluded.

\subsection{Elliptical-vote morphologies, S0 degeneracy, and transition galaxies}

The Galaxy Zoo morphology variables used here identify galaxies with high elliptical vote fractions and galaxies with high combined-spiral vote fractions. They do not cleanly distinguish classical ellipticals from S0 galaxies, faded disks, or other bulge-dominated disk systems. The enhanced \CG{} elliptical fraction should therefore be interpreted as a shift toward early-type-like appearances, not as proof that compact groups specifically build classical elliptical galaxies.

This caveat matters when comparing with the recent S-PLUS structural studies of compact-group galaxies by \citet{Montaguth2023,Montaguth2025,Montaguth2026MassSize,Montaguth2026NonIsolated}, which identify transition galaxies and trends in the \(R_{\rm e}\)--S\'ersic-index and mass-size planes, with especially strong signals in non-isolated compact groups and host structures. Their transition population can plausibly overlap with the elliptical-vote class in our catalogue: systems with faded disks, increasing central concentration, or S0-like structure may receive high elliptical vote fractions here even if they are not classical ellipticals. The two approaches are therefore complementary: their structural measurements describe the likely physical form of the transformation, while our regular-group controls show that the strongest residual compact-group signal is the accumulated elliptical/spiral vote-fraction balance.<% if sz.get('status') == 'ok' %> With the Simard structural parameters now attached to this sample, the \(R_{\rm e}\)--S\'ersic-\(n\) plane can be inspected directly for \CG{} and its controls (Appendix Fig.~\ref{fig:size_re_n_plane}), providing contextual structural information rather than a primary inferential test.<% endif %>

\subsection{Why the star-formation and size signals are weaker}\label{sec:decoupling}

The star-formation diagnostics do not follow the morphology signal one-to-one. \CG{} satellites have a lower raw star-forming fraction, especially relative to \RG, but the adjusted evidence is control-dependent (detected only against \CB) and the matched contrasts provide no evidence for a quenched-fraction difference. Among galaxies that remain star-forming, the main-sequence residuals do not show a robust compact-group offset. The colour analysis is control-sensitive and based on a biased colour-matched subset, while AGN-like fractions, magnitude gaps, and the limited \(\mathrm{H}\alpha\) diagnostic do not identify a simple alternative driver.

<% set discussion_selection = discussion_es.get('selection_diagnostics', {}) %>
<% set discussion_angular = discussion_selection.get('nearest_angular_separation', {}) %>
<% set discussion_crowd = discussion_es.get('morphology_robustness', {}) %>
<% set discussion_crowd_rows = discussion_crowd.get('fractions_after_excluding_close', []) %>
<% set discussion_c4c_crowd_list = discussion_crowd_rows|selectattr('sample', 'equalto', 'Control4C')|list %>
<% set discussion_c4c_crowd = discussion_c4c_crowd_list|first if discussion_c4c_crowd_list|length > 0 else {} %>
The star-formation non-detections are also limited by the spectroscopy. <% if discussion_angular.get('status') == 'ok' %>\CG{} has a higher fraction of galaxies with a nearest projected neighbour below the 55-arcsec SDSS fibre-collision scale than the controls (<< discussion_angular.get('cg4_fraction_below_threshold')|pct(1) >>\% versus << discussion_angular.get('control_fraction_below_threshold')|pct(1) >>\%). <% endif %><% if discussion_c4c_crowd.get('close_fraction') is not none %>The control value is pooled over the regular-control comparison; the \CC{}-only exclusion fraction in Sect.~\ref{sec:morph_crowding} is larger (<< discussion_c4c_crowd.get('close_fraction')|pct(1) >>\%) because \CC{} is selected as the compact-core analogue. <% endif %>Spectra are therefore preferentially incomplete in the closest projected pairs, and short-lived interaction-triggered starbursts or post-starburst phases could be underrepresented. The MPA-JHU aperture corrections use photometric information outside the fibre \citep{Brinchmann+04,Kauffmann+03a}; these corrections may be less secure for interacting or blended compact-group members. We therefore treat the sSFR, SFMS, and \(\mathrm{H}\alpha\) results as conservative secondary non-detections, not as proof that compact groups have no star-formation effect. This decoupling suggests that the dominant observable difference is not an instantaneous quenching event caught at the present epoch: a galaxy can lose visible spiral structure, build a more bulge-dominated profile, or fade into an S0-like state on timescales that do not require a simultaneously large sSFR residual in the surviving star-forming subset.

<% if sz.get('status') == 'ok' %>
<% set disc_adj_sat = sz.get('adjusted', {}).get('satellites', {}) %>
<% set disc_m_simard = sz.get('matched', {}).get('effects', {}).get('delta_log_Rchl_r', {}) %>
The size test carries a parallel message. \citet{Coenda+12} compared galaxies in the \citet{McConnachie+09} compact groups with loose-group and field samples using SDSS Petrosian half-light radii and found compact-group galaxies to be smaller at fixed luminosity, with the clearest offset for early types relative to the field, based on approximately 1\(\sigma\) comparisons of fitted size--luminosity relations. The present analysis re-poses that question with the non-split HMCG-based \CG{} sample against three regular-group controls, seeing-corrected model sizes, a Petrosian cross-check, cluster-robust adjusted models, one-to-one matching, crowding tests, and Holm-corrected confidence intervals.
<% if szv.get('primary_satellites_significant') %>
Under this stricter design, the compact-group size deficit survives: at fixed stellar mass, \CG{} satellites are systematically << szv.get('direction') >> (\(<< disc_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex), in qualitative agreement with \citet{Coenda+12} but now against regular-group controls rather than the field.
<% elif szv.get('survives_matching') %>
Under this stricter design, the pooled adjusted models do not confirm a compact-group size offset at fixed stellar mass relative to regular groups (satellite coefficient \(<< disc_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex, Holm-corrected interval consistent with zero). The matched pairs do prefer << szv.get('direction') >> \CG{} sizes (\(<< disc_m_simard.get('mean_delta')|fmt(3) >>\) dex), but that contrast is control-dependent. The comparison with \citet{Coenda+12} is therefore nuanced rather than contradictory: their compact-group versus field and loose-group offset can coexist with a null compact-group versus regular-group-core result, because our controls are themselves dense group environments rather than the field.
<% else %>
Under this stricter design, we find no compact-group size offset at fixed stellar mass relative to regular groups. This does not contradict \citet{Coenda+12} directly, because their comparison was against loose groups and the field, whereas ours is against regular-group cores selected from the same parent catalogues.
<% endif %>
Two interpretation guardrails apply: a negative \(\Delta\) at fixed mass can reflect genuine tidal truncation or stripping, but also a higher bulge dominance in the \CG{} population or residual measurement blending in crowded fields. The morphology-stratified fits address the former, and the Petrosian cross-check, measure-difference diagnostic, and crowding exclusions bound the latter (Appendix~\ref{app:size_details}); any future claim of compact-group size evolution should be read through those blocks together rather than through a single pooled coefficient.
<% endif %>

\section{Conclusion}\label{sec:conclusion}
<% set c_pc4c = ((es.get('matched_controls', {}) or {}).get('group_level_per_control', {}) or {}).get('Control4C', {}) %>

We have tested whether galaxies in \CG{} compact groups remain special after removing split systems, comparing them with three regular-group analogues drawn from the same parent catalogues -- \RG, \CB, and \CC{} -- through complementary individual-galaxy, group-composition, and shared-host contrasts (Table~\ref{tab:inferential_framework}).

The main result is morphological, and it depends on the estimand. \CG{} galaxies -- primarily satellites -- have a higher Galaxy~Zoo~1 elliptical-vote fraction and a lower spiral-vote fraction. The adjusted individual-galaxy excess is strong against the luminous cores of richer ordinary groups (\CB) and against true four-member groups (\RG), but is not detected against BGG-centred projected cores (\CC). At the group level, matched \CG{} systems show positive elliptical-vote satellite-composition contrasts against all three controls under the stated per-control multiplicity convention (Table~\ref{tab:group_match_per_control})<% if c_pc4c.get('status') == 'ok' and c_pc4c.get('p_permutation') is not none and c_pc4c.get('p_permutation') < 0.05 %>, including \CC{}<% endif %>. Under the primary conditional within-host estimator, no additional residual excess is detected for \CG{} members relative to non-\CG{} co-members of the same host. The morphology result is stable to the Galaxy~Zoo~1 vote threshold, is recovered with a threshold-free vote-fraction model and a S\'ersic-index cross-check, and persists after excluding the closest projected pairs; BGG morphology is not detected as different after adjustment. Conditioning on a local projected-density proxy attenuates the galaxy-level coefficient, but this conditional attenuation does not establish mediation.

The star-formation result is weaker. \CG{} satellites have a lower raw star-forming fraction, especially relative to \RG, but the adjusted evidence is control-dependent, the matched contrasts provide no evidence for a quenched-fraction difference, and star-forming \CG{} galaxies show no robust offset from the fitted star-forming main sequence.
<% if sz.get('status') == 'ok' %>
<% set conclusion_adj_sat = sz.get('adjusted', {}).get('satellites', {}) %>
<% set conclusion_m_simard = sz.get('matched', {}).get('effects', {}).get('delta_log_Rchl_r', {}) %>
<% if szv.get('primary_satellites_significant') %>At fixed stellar mass, redshift, and group-scale covariates, \CG{} satellites are systematically << szv.get('direction') >> in seeing-corrected half-light radius (\(<< conclusion_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex; Holm-corrected), a size offset that complements the morphology signal.<% elif szv.get('survives_matching') %>At fixed stellar mass, redshift, and group-scale covariates, the pooled adjusted models show no detected \CG{} offset in seeing-corrected half-light radius (satellite coefficient \(<< conclusion_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex); the one-to-one matched pairs formally prefer << szv.get('direction') >> \CG{} sizes (\(<< conclusion_m_simard.get('mean_delta')|fmt(3) >>\) dex), but the contrast is control-dependent, so we do not claim a robust compact-group size effect.<% else %>At fixed stellar mass, redshift, and group-scale covariates, no \CG{} offset is detected in seeing-corrected half-light radius, in either the adjusted models or the matched pairs.<% endif %>
<% endif %>
The remaining secondary diagnostics do not identify a simple alternative driver: the colour results are control-sensitive and based on a biased matched subset, AGN-like fractions and magnitude gaps are not detected as parallel compact-group offsets, the \(\mathrm{H}\alpha\) diagnostic is limited by the absence of age-sensitive spectral indices from our retrieved catalogues, and projected phase-space trends do not localize the contrast uniquely to the innermost satellites.

We therefore interpret \CG{} systems as hosting a population shifted towards early-type-like morphologies, consistent with accumulated structural transformation in dense group cores and/or preprocessing. This is not proof of a uniquely isolated present-day compact-group quenching mechanism. Explicit E--S0--spiral and transition-galaxy classifications, bulge--disc structure, homogeneous halo masses, and age-sensitive stellar-population diagnostics will be needed to determine whether the elliptical-vote satellite excess reflects disk fading, tidal heating, or merger-driven transformation in dense group cores.

\section*{Data availability}

The input data are drawn from public SDSS spectroscopic and photometric tables, the MPA-JHU
value-added catalogues, Galaxy Zoo classifications, the \citet{Simard2011} structural
catalogue (VizieR \texttt{J/ApJS/196/11}), and the public group and compact-group catalogues cited
in Sect.~2. The derived galaxy and group tables from this analysis will be made available
through the CDS at publication. The full analysis and paper-generation code is available at
\url{https://github.com/MatthieuTricottet/CG_Gals}; the release corresponding to this
article will be archived with a DOI (e.g. Zenodo) at acceptance. Supplementary diagnostic
figures removed from the printed appendices (main-sequence, environmental, phase-space,
colour, and size diagnostics) are included in the archived release. % TODO(MT): mint DOI.

\begin{acknowledgements}
% TODO(MT): personal thanks (colleagues, informal readers).
% TODO(MT): funding — MT: ...; GAM: ...; EDG acknowledges support from CONICET and
% SECyT--UNC, Argentina (grant nos. TODO).
% TODO(MT): paste the official SDSS-IV/DR16 funding acknowledgement verbatim from
% https://www.sdss4.org/collaboration/citing-sdss/ (required by SDSS data policy).
This publication uses data generated via the Galaxy Zoo project; we thank the many
volunteers whose classifications made this analysis possible. This work made use of the
MPA-JHU value-added spectroscopic catalogues, and of the \citet{Simard2011} structural
catalogue obtained through the VizieR catalogue access tool (CDS, Strasbourg; catalogue
\texttt{J/ApJS/196/11}). This research made use of Astropy \citep{Astropy2022}, Astroquery
\citep{Ginsburg2019}, NumPy \citep{Harris2020}, SciPy \citep{Virtanen2020}, pandas
\citep{McKinney2010}, Matplotlib \citep{Hunter2007}, statsmodels \citep{Seabold2010}, and
scikit-learn \citep{Pedregosa2011}.
\end{acknowledgements}

\bibliographystyle{aa}
\bibliography{<<BIB_FILE>>}
\clearpage
\begin{appendix}

\section{SDSS data query}\label{app:sdss_query}

% MOVED: column/join description relocated here from Sect. 2.1.
We specifically extract the columns \texttt{sfr\_tot\_p50}, \texttt{specsfr\_tot\_p50}, and \texttt{lgm\_tot\_p50} from the \texttt{galSpecExtra} table, and \texttt{p\_el\_debiased} and \texttt{p\_cs\_debiased} from the \texttt{zooSpec} table. In the query, \texttt{galSpecExtra}, \texttt{galSpecLine}, and \texttt{zooSpec} are joined to \texttt{SpecObj} by \texttt{specObjID}, while the photometric object is linked through \texttt{SpecObj.bestObjID}=\texttt{PhotoObj.objID}; the queried quantities are then merged onto the \CG{} and control catalogue rows by their stored \texttt{objid}.

The full query is provided in \texttt{src/data\_loader.py} in the repository release (Zenodo DOI; Sect.~Data availability).

\FloatBarrier

\section{sSFR classification details}\label{app:ssfr_method}

We identify BPT-classifiable galaxies by requiring measured values of both emission-line ratios
\[
\log_{10}\!\bigl([\mathrm{N\,II}]\lambda6584/\mathrm{H}\alpha\bigr)
\quad\text{and}\quad
\log_{10}\!\bigl([\mathrm{O\,III}]\lambda5007/\mathrm{H}\beta\bigr).
\]
Among these galaxies, we flag AGN-like systems if they satisfy either
\[
\log_{10}\frac{[\mathrm{N\,II}]}{\mathrm{H}\alpha} > 0,
\]
or if they lie above the empirical demarcation of \citet{Kauffmann+03a}:
\[
\log_{10}\frac{[\mathrm{O\,III}]}{\mathrm{H}\beta}
> \frac{0.61}{\log_{10}([\mathrm{N\,II}]/\mathrm{H}\alpha)-0.05} \;+\; 1.3,
\]
in which case they are excluded from the GMM calibration sample. This calibration flag is deliberately more inclusive than the three-class BPT scheme of Sect.~\ref{sec:agn_env}, in which composite galaxies lie between the \citet{Kauffmann+03a} and \citet{Kewley+01} demarcations and only galaxies above the \citet{Kewley+01} line are labelled AGN-like, because its sole purpose is to keep possible AGN contamination out of the sSFR calibration. Galaxies with missing diagnostic line ratios are BPT-unclassified, not proven non-AGN. They are retained for the broad sSFR status assignment when valid SDSS stellar mass and sSFR estimates are available, while the emission-line/AGN-like analysis treats BPT-unclassified objects separately.
Galaxies with \texttt{specsfr\_tot\_p50} equal to \(-9999\) in the SDSS carry the MPA-JHU missing-value sentinel and have no valid sSFR estimate;
they are treated as missing data throughout: excluded from the GMM calibration, never assigned an sSFR class,
omitted from every sSFR figure, and reported as counts only (Sect.~\ref{sec:ssfr_classification}).
Galaxies with a measured sSFR are divided into quenched and star-forming classes with a two-component Gaussian Mixture Model (GMM) in the \(\log M_\star\)-\(\log\mathrm{sSFR}\) plane \citep{McLachlanPeel2000}. The calibration excludes galaxies flagged as AGN-like where the required BPT line ratios are available, while BPT-unclassified galaxies with valid SDSS stellar mass and sSFR estimates can still receive a broad sSFR class. The quenched/star-forming boundary is the locus where the two component posterior probabilities are equal; the resulting classification for the SDSS selection is shown in Fig.~\ref{fig:sSFR_classification}. A fixed-threshold robustness check confirms that the qualitative hierarchy is not tied to the exact GMM split (Sect.~\ref{sec:secondary}).

% MOVED: fig:sSFR_classification relocated here from Sect. 2.
\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{sSFR_classification.pdf}
     \caption{sSFR versus stellar mass, the star-forming/quenched limit, and Galaxy Zoo morphologies for the SDSS selection,
     excluding galaxies without sSFR estimates.}
     \label{fig:sSFR_classification}
\end{figure}

\FloatBarrier

<% set overlap = r.get('cg4_pc_quartet_overlap', {}) %>
<% if overlap %>
\section{Compact groups as projected cores of ordinary groups}\label{app:cg_core_overlap}

The compact-group exclusion of Sect.~\ref{sec:samples} is not a technicality: for << overlap.get('n_lim_groups') >> of the 765 parent Lim groups, the would-be \CC{} quartet (BGG plus three closest projected companions) contains at least one \CG{} galaxy — << overlap.get('n_galaxies') >> \CG{} galaxies in total. Table~\ref{tab:cg_core_overlap} breaks this overlap down by the \citet{Zheng&Shen21} embeddedness class: essentially every Embedded and Predominant compact group \emph{is} the projected core of an ordinary group. Had these groups not been excluded, the same galaxies would have appeared on both sides of the comparisons, biasing every contrast toward null.

\begin{table}
    \centering
    \caption{\CG{} galaxies inside would-be \CC{} quartets of the parent catalogue, by compact-group class.}
    \label{tab:cg_core_overlap}
    \begin{tabular}{lccc}
        \hline\hline
        \textbf{\CG{} class} & \textbf{\CG{} groups} & \textbf{Lim groups} & \textbf{Galaxies} \\
        \hline
<% for class_name, row in overlap.get('by_class', {}).items() %>
        << lu.display_label(class_name) >> & << row.get('n_cg4_groups') >> / << row.get('n_cg4_groups_total') >> & << row.get('n_lim_groups') >> & << row.get('n_galaxies') >> \\
<% endfor %>
        \hline
    \end{tabular}
    \tablefoot{``\CG{} groups'' counts compact groups with at least one member inside a parent-group quartet, over the total number of compact groups of that class (split groups included, as in the exclusion).}
\end{table}
<% endif %>

\section{Star-forming main-sequence details}\label{app:baseline_details}
% MOVED: SFMS fitting details, per-control residual offsets, class-split
% comparison, and supporting figures relocated here from old Sects. 3.5-3.6.

We model the star-forming main sequence as a function of stellar mass using polynomial orders 1--4; the polynomial-order comparison is provided in the online supplement. The diagnostic shown there is the residual RMS scatter, not a formal reduced \(\chi^2\), because no per-galaxy uncertainty model is used. We adopt order << r.get('Main_Sequence_polyfit_selected_order', 2) >> because it is the lowest-order model with the smallest five-fold cross-validated RMS scatter (<< r.get('Main_Sequence_polyfit_selected_cv_rms', 0)|fmt(5) >> dex, compared with << r.get('Main_Sequence_polyfit_order1_cv_rms', 0)|fmt(5) >> dex for order 1).
We then use order 2 to compute the residual of each star-forming galaxy with respect to the main sequence. A positive residual means higher sSFR at fixed stellar mass relative to the fitted star-forming main sequence.

In the comparisons below, \(\Delta\) is the \CG{}-minus-control median residual; positive values mean that \CG{} star-forming galaxies lie above the fitted main sequence relative to the control. Each quoted interval is the central 68\% (16th--84th percentile) bootstrap interval on the median difference, and each \(p\) is the two-sided bootstrap sign-crossing probability for the same difference (10\,000 resamples). The 68\% interval and the two-sided \(p\) answer slightly different questions and can therefore appear to disagree near the significance boundary: a 68\% interval that excludes zero is compatible with \(p\) slightly above 0.05.

The median main-sequence residual of \CG{} galaxies is:
\begin{itemize}
<% for key, res in build.items() %>
<% if key.startswith("pval_MSresiduals_") %>

<% set delta = res["Δmedian"] %>
<% set ci16 = res["CI_16"] %>
<% set ci84 = res["CI_84"] %>
<% set p = res["p_value"] %>
<% set name = key
    .replace("pval_MSresiduals_", "")
    .replace("_Gals", "") %>

\item <% if p is not none and p < 0.05 %>significantly different<% elif p is not none and p < 0.10 %>marginally different<% else %>not significantly different<% endif %> from that of << name >>,
with a median offset of
$\Delta = << "%.3f"|format(delta) >>$
($<< "%.3f"|format(ci16) >> \le \Delta \le << "%.3f"|format(ci84) >>$, 16th--84th percentile bootstrap interval),
corresponding to << pval_text(p) >>.

<% endif %>
<% endfor %>
\end{itemize}

Because the raw residual comparison and the matched comparison condition on different covariate structures, and because the apparent offset changes in strength after matching, we treat SFMS residuals as a secondary diagnostic rather than as evidence for a robust compact-group star-formation offset (Sect.~\ref{sec:raw}).

<% if r.get('Main_sequence_offset_by_cg_class_significant', []) %>
Within \CG, the class-split comparison of star-forming main-sequence offsets highlights
<% for item in r.get('Main_sequence_offset_by_cg_class_significant', []) %>
a difference between << item.class_a_label|default(item.class_a) >> and << item.class_b_label|default(item.class_b) >>
(medians << item.median_class_a_fmt >> and << item.median_class_b_fmt >>, respectively; << p_from_fmt(item.p_value_fmt) >>)<% if not loop.last %>; <% else %>.<% endif %>
<% endfor %>
<% else %>
Within \CG, the star-forming main-sequence offsets of the Embedded, Isolated and Predominant classes remain statistically consistent with one another.
<% endif %>

\FloatBarrier

\section{Environmental, group-scale, and colour diagnostics}\label{app:env_diagnostics}
% MOVED: old Sect. 4 (environmental and group-scale diagnostics) and the
% full optical-colour analysis relocated here; the main text keeps a
% one-paragraph summary in Sect. 3 (Secondary outcomes).

This appendix collects the group-scale and colour diagnostics summarized in Sect.~\ref{sec:secondary}.

\subsection{Distance to the BGG}\label{app:dist2bgg}
We compare the sSFR of satellite galaxies with their distance to the BGG, expressed both in projected kpc and in units of $\langle R_{ij}\rangle$.
<% if r.get('Dist2BGG_significant_count', 0) > 0 %>
Significant monotonic trends are detected in the following cases.
<% for item in r.get('Dist2BGG_significant_correlations', []) %>
In << item.sample_label >>, sSFR correlates with << item.distance_label >> with Spearman coefficient
$\rho_\mathrm{S} = << item.spearman_rho_fmt >>$ for << item.n >> satellites (<< p_from_fmt(item.spearman_p_fmt) >>) and Pearson coefficient
$r = << item.pearson_r_fmt >>$ (<< p_from_fmt(item.pearson_p_fmt) >>).
<% endfor %>
<% else %>
No significant monotonic trend is detected between sSFR and distance to the BGG in either metric.
<% endif %>
\subsection{Dominance}
<% set dom_summary = r.get('Dom_distribution_summary_by_sample', []) %>
<% set dom_significant = dom_summary|selectattr('has_significant_difference')|list %>
<% set dom_not_significant = dom_summary|rejectattr('has_significant_difference')|list %>
<% set spiral_corr = r.get('Dom_spiral_fraction_significant_correlations', []) %>
<% set c4c_size = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'size_Group_Bary_kpc')|list %>
<% set c4c_vdisp = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'Vdisp')|list %>
<% set c4c_lum = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'Lum_group')|list %>
<% set c4b_corr = spiral_corr|selectattr('sample', 'equalto', 'Control4B')|list %>
<% set cg4_dom_lum = spiral_corr|selectattr('sample', 'equalto', 'CG4')|selectattr('domination', 'equalto', 'Dominated')|selectattr('quantity', 'equalto', 'Lum_group')|list|first %>
<% set cg4_nondom_voffset = spiral_corr|selectattr('sample', 'equalto', 'CG4')|selectattr('domination', 'equalto', 'Non-dominated')|selectattr('quantity', 'equalto', 'Voffset')|list|first %>
We split groups into dominated systems, where the BGG contributes more than << (DOMINATIION_CRITERIA * 100)|round|int >>\% of the group luminosity, and non-dominated systems. This split does not produce a strong compact-group signal by itself: <% if dom_not_significant|length > 0 %><% if dom_not_significant|length == 1 -%><< dom_not_significant[0].sample_label >> shows no significant dominated/non-dominated difference after the Benjamini--Hochberg false-discovery-rate (FDR) correction<% else -%><% for item in dom_not_significant -%><< item.sample_label >><% if loop.revindex0 == 1 %> and <% elif not loop.last %>, <% else %> show no significant dominated/non-dominated difference after the Benjamini--Hochberg false-discovery-rate (FDR) correction<% endif %><% endfor %><% endif %><% else %>all samples show at least one corrected dominated/non-dominated difference<% endif %>. Here \(p_\mathrm{adj}\) denotes the FDR-adjusted p-value, and Cliff's \(\delta\) is a rank-based effect size ranging from -1 to 1.

Across the control and reference samples, the clearest binary separation is instead in group luminosity. Dominated systems have lower \(L_\mathrm{group}\) than their non-dominated counterparts: <% for item in dom_significant -%><% set p_adj_text = item.p_adj_fmt|replace('$', '') -%><< item.sample_label >> gives << item.median_dom_fmt >> versus << item.median_nondom_fmt >><% if lu.formatted_unit(item.quantity) %> << lu.formatted_unit(item.quantity) >><% endif %> (\(p_\mathrm{adj}<% if p_adj_text.startswith('<') %><< p_adj_text >><% else %>=<< p_adj_text >><% endif %>\), \(\delta=<< item.cliffs_delta_fmt >>\))<% if loop.revindex0 == 1 %>; and <% elif not loop.last %>; <% else %>.<% endif %><% endfor %> The negative \(\delta\) values mean that dominated groups tend to occupy the lower-luminosity side of each comparison.

The spiral-fraction correlations show a broader environmental pattern rather than a clean dominated/non-dominated dichotomy. <% if c4c_size|length > 0 %>The most coherent set appears in \CC, where \(S_\mathrm{frac}\) increases with \(\left\langle R_{ij}\right\rangle\) in both dominated and non-dominated groups (<% for item in c4c_size -%><% set p_text = item.p_value_fmt|replace('$', '') -%>\(\rho_\mathrm{S}=<< item.rho_fmt >>\), \(p<% if p_text.startswith('<') %><< p_text >><% else %>=<< p_text >><% endif %>\)<% if loop.revindex0 == 1 %> and <% endif %><% endfor %>)<% if c4c_vdisp|length > 0 or c4c_lum|length > 0 %>, while weaker negative trends are found with <% if c4c_vdisp|length > 0 %>\(\sigma_v\)<% if c4c_lum|length > 0 %> and <% endif %><% endif %><% if c4c_lum|length > 0 %>\(L_\mathrm{group}\)<% endif %><% endif %>.<% endif %> <% if c4b_corr|length > 0 %>\CB{} contributes only weaker non-dominated-group correlations. <% endif %><% if cg4_dom_lum or cg4_nondom_voffset %>In \CG, the significant trends occur in small subsets<% if cg4_dom_lum %>, with \(S_\mathrm{frac}\) decreasing with \(L_\mathrm{group}\) among dominated groups (\(\rho_\mathrm{S}=<< cg4_dom_lum.rho_fmt >>\), << cg4_dom_lum.n_groups >> groups)<% endif %><% if cg4_nondom_voffset %> and with \(\Delta V_\mathrm{BGG}/\sigma_v\) among non-dominated groups (\(\rho_\mathrm{S}=<< cg4_nondom_voffset.rho_fmt >>\), << cg4_nondom_voffset.n_groups >> groups)<% endif %>; we therefore treat these as supporting diagnostics rather than the main dominance result.<% endif %> The full distributional diagnostics are provided in the online supplement.
% CUT: fig:Dom_vs_NonDom_XOR (bootstrap Spearman half-violins) removed to
% respect the 8-page free appendix budget; resurrect from git if a referee asks.

\subsection{Morphology and domination}
<% if r.get('Morphology_domination_effects_count', 0) > 0 %>
When we further split the samples by domination and by the morphology of the BGG, significant effects only appear in the following cases.
\begin{itemize}
<% for item in r.get('Morphology_domination_effects', []) %>
\item << item.sample_label >> << item.domination_label >> groups, where the median satellite spiral fraction is
<< item.group_median_spiral_bgg_fmt >> for spiral BGGs and << item.group_median_elliptical_bgg_fmt >> for elliptical BGGs.
The association between BGG morphology and satellite spiral fraction has a group-level odds ratio of << item.group_or_fmt >> (<< p_from_fmt(item.group_p_fmt) >>) in this sample; the same within-sample contrast under a satellite-level clustered uncertainty treatment gives
an odds ratio of << item.sat_or_fmt >> (<< p_from_fmt(item.sat_p_fmt) >>).
<% endfor %>
\end{itemize}
In each case the group-level and satellite-level clustered treatments share the same point estimate by construction and differ only in the uncertainty model, which is why the odds ratios coincide.
<% else %>
Splitting the samples by both domination and BGG morphology does not reveal any significant modulation of the satellite morphology mix.
<% endif %>

<% set md = es.get('morphology_dominance', {}) %>
<% if md.get('status') == 'ok' %>
\subsection{Morphology versus Zheng--Shen class, magnitude gap, and BGG dominance}

We next test whether the E/Sp morphology mix is associated with three group-level descriptors: the Zheng--Shen compact-group class, the first--second magnitude gap \(\Delta m_{12}=M_{r,2}-M_{r,1}\), and the continuous BGG luminosity fraction \(f_{L,\mathrm{BGG}}=L_\mathrm{BGG}/L_\mathrm{group}\). These tests use only the existing Galaxy Zoo elliptical and spiral classes; uncertain morphologies are excluded from the binary fits. We run the tests separately for all galaxies, BGGs only, and satellites only, so that any trend driven by the BGG itself is not folded into the satellite result. For satellites, \(f_{L,\mathrm{BGG}}\) mechanically includes the satellite luminosity in the group-luminosity denominator, so any satellite-level trend with \(f_{L,\mathrm{BGG}}\) is partly definitional; we therefore report the satellite models with a stellar-mass control.

<% set md_audit = md.get('dominance_audit', {}) %>
The magnitude-gap and BGG-luminosity-fraction quantities are available for all << md_audit.get('n_groups') >> \CG{}+\RG{} groups used in this test. The Zheng--Shen class is available only for compact groups. Split systems are absent from the current \CG{} sample because they are removed before the paper analysis, and no corresponding Zheng--Shen class is defined for the \RG{} catalogue used here. We therefore report the \RG{} class test as unavailable, while retaining \RG{} in the \(\Delta m_{12}\) and \(f_{L,\mathrm{BGG}}\) comparisons.

Overall, these tests provide little evidence that the E/Sp mix is driven by Zheng--Shen class, magnitude gap, or BGG luminosity fraction. The few marginal associations are therefore treated as descriptive diagnostics rather than as robust secondary trends (Tables~\ref{tab:morphology_dominance_class_counts} and \ref{tab:morphology_dominance_models}).

\begin{table}
\centering
\caption{Elliptical and spiral counts by Zheng--Shen class for the \CG{} sample. Uncertain morphologies are excluded. Split systems have zero counts because they were removed by the existing non-split \CG{} sample definition.}
\label{tab:morphology_dominance_class_counts}
\small
\tabcolsep=2pt
\begin{tabular}{lccc}
\hline\hline
\makecell{Zheng--Shen\\class} & \makecell{All \CG{}\\E/Sp} & \makecell{\CG{} BGG\\E/Sp} & \makecell{\CG{} sat.\\E/Sp} \\
\hline
<% set cg_all_class = md.get('class_association', {}).get('CG4', {}).get('all', {}).get('contingency_table', []) %>
<% set cg_bgg_class = md.get('class_association', {}).get('CG4', {}).get('bgg', {}).get('contingency_table', []) %>
<% set cg_sat_class = md.get('class_association', {}).get('CG4', {}).get('satellites', {}).get('contingency_table', []) %>
<% for row in cg_all_class %>
<% set bgg_row = cg_bgg_class|selectattr('class', 'equalto', row.class)|list|first %>
<% set sat_row = cg_sat_class|selectattr('class', 'equalto', row.class)|list|first %>
<< row.class >> & << row.N_E >>/<< row.N_Sp >> & << bgg_row.N_E if bgg_row else 0 >>/<< bgg_row.N_Sp if bgg_row else 0 >> & << sat_row.N_E if sat_row else 0 >>/<< sat_row.N_Sp if sat_row else 0 >> \\
<% endfor %>
\hline
\end{tabular}
\end{table}

For the Zheng--Shen compact-group class, no \CG{} association reaches the conventional 5\% significance threshold. The closest case is the satellite-only test, which is treated as marginal and descriptive. Because the Zheng--Shen class is not defined for the \RG{} catalogue, we do not attempt an \RG{} class comparison.

\begin{table}
\centering
\caption{Tests of the E-vs-Sp morphology mix against compact-group class, magnitude gap, and BGG luminosity fraction. The continuous columns report the available adjusted logistic model for \(P(\mathrm{E})\); all-galaxy and satellite fits use group-clustered standard errors. Odds ratios are reported per magnitude for \(\Delta m_{12}\) and per 0.1 increase in luminosity fraction for \(f_{L,\mathrm{BGG}}\).}
\label{tab:morphology_dominance_models}
\small
\tabcolsep=2pt
\begin{tabular}{lllc}
\hline\hline
Subset & Sample & Test & Result \\
\hline
<% for subset in ['all', 'bgg', 'satellites'] %>
<% for sample in ['CG4', 'RG4'] %>
<% set class_res = md.get('class_association', {}).get(sample, {}).get(subset, {}) %>
<% set class_test = class_res.get('test', {}) %>
<% set gap_models = md.get('continuous_association', {}).get('Delta_m12', {}).get(sample, {}).get(subset, {}).get('models', {}) %>
<% set gap_model = gap_models.get('adjusted', {}) if gap_models.get('adjusted', {}).get('status') == 'ok' else gap_models.get('unadjusted', {}) %>
<% set fl_models = md.get('continuous_association', {}).get('f_L_BGG', {}).get(sample, {}).get(subset, {}).get('models', {}) %>
<% set fl_model = fl_models.get('adjusted', {}) if fl_models.get('adjusted', {}).get('status') == 'ok' else fl_models.get('unadjusted', {}) %>
<< md.get('subsets', {}).get(subset, subset) >> & <% if sample == 'CG4' %>\CG{}<% else %>\RG{}<% endif %> & Class & <% if class_test.get('status') == 'ok' %><< pval_text(class_test.get('p_value')) >><% elif sample == 'RG4' %>not defined<% else %>skipped<% endif %> \\
 & & \(\Delta m_{12}\) & <% if gap_model.get('status') == 'ok' %>OR \(<< gap_model.get('odds_ratio')|fmt(2) >>\), << pval_text(gap_model.get('p_value')) >><% else %>skipped<% endif %> \\
 & & \(f_{L,\mathrm{BGG}}\) & <% if fl_model.get('status') == 'ok' %>OR \(<< fl_model.get('odds_ratio')|fmt(2) >>\), << pval_text(fl_model.get('p_value')) >><% else %>skipped<% endif %> \\
<% endfor %>
<% endfor %>
\hline
\end{tabular}
\end{table}

For \(\Delta m_{12}\), no statistically significant morphology trend is found in \CG{} or \RG{}, whether the fit is run for all galaxies, BGGs only, or satellites only. The same conclusion holds for \(f_{L,\mathrm{BGG}}\): no fitted trend reaches \(p<0.05\). The \CG{}--\RG{} interaction models detect no significant difference in the morphology--dominance relation for either \(\Delta m_{12}\) or \(f_{L,\mathrm{BGG}}\), across all galaxies, BGGs only, and satellites only. A class interaction is not defined because the Zheng--Shen compact-group class has no \RG{} counterpart in the present catalogue. Across the morphology--class, morphology--gap, morphology--luminosity-fraction, and interaction tests, no result remains significant after Benjamini--Hochberg FDR control.
<% endif %>

\subsection{Crossing time}
<% set tcross_corr = r.get('Tcross_global_correlations', []) %>
<% set tcross_m_over_l = tcross_corr|selectattr('y_key', 'equalto', 'lg_M_virial_over_L')|list|first %>
<% set tcross_mass = tcross_corr|selectattr('y_key', 'equalto', 'lg_M_virial')|list|first %>
<% set tcross_lum = tcross_corr|selectattr('y_key', 'equalto', 'lg_Lum_group')|list|first %>
<% set p_m_over_l = tcross_m_over_l.p_value_fmt|replace('$', '') if tcross_m_over_l else '' %>
<% set p_mass = tcross_mass.p_value_fmt|replace('$', '') if tcross_mass else '' %>
<% set p_lum = tcross_lum.p_value_fmt|replace('$', '') if tcross_lum else '' %>
The crossing-time diagnostic mainly separates systems by dynamical scale rather than by luminosity alone. Shorter \(t_\mathrm{cross}\) systems have larger virial mass-to-light ratios and virial masses: the correlations with \(\log(\mathcal{M}_\mathrm{VT}/L_r)\) and \(\log(\mathcal{M}_\mathrm{VT})\) are \(\rho_\mathrm{S}=<< tcross_m_over_l.rho_fmt >>\) (\(p<% if p_m_over_l.startswith('<') %><< p_m_over_l >><% else %>=<< p_m_over_l >><% endif %>\)) and \(\rho_\mathrm{S}=<< tcross_mass.rho_fmt >>\) (\(p<% if p_mass.startswith('<') %><< p_mass >><% else %>=<< p_mass >><% endif %>\)), respectively, over << tcross_mass.n_groups >> groups. By contrast, the relation with total group luminosity is formally detected but very weak (\(\rho_\mathrm{S}=<< tcross_lum.rho_fmt >>\)). We therefore read \(t_\mathrm{cross}\) as a dynamical compactness indicator tied primarily to virial mass and mass-to-light ratio, with little independent leverage from \(L_\mathrm{group}\).
\FloatBarrier
\subsection{Optical colours}\label{sec:optical_colours}
<% set colour_avail_cg4 = es.get('selection_diagnostics', {}).get('availability_counts_by_sample', {}).get('CG4', {}).get('colours', {}) %>
The retained colour-analysis matches include <% for item in r.Colour_matching_summary -%><< item.n_matched >> of << item.n_total >> galaxies in << item.sample_label >><% if not loop.last %>, <% else %>.<% endif %><% endfor %> This stricter subset differs from the availability audit in Fig.~\ref{fig:data_availability}: the figure's colour row only requires the four derived SDSS colour columns to be present in the final catalogue<% if colour_avail_cg4.get('n_available') and colour_avail_cg4.get('n_total') %>, giving << colour_avail_cg4.get('n_available') >> of << colour_avail_cg4.get('n_total') >> \CG{} galaxies<% endif %>, whereas the colour analysis also requires successful matched-photometry retention and the covariates used below. The fractions retained in this stricter colour-analysis subset are <% for item in r.Colour_robustness_audit -%><< (100 * item.n_all_four_colours / item.n_galaxies)|round(1) >>\% in << item.sample >><% if not loop.last %>, <% else %>.<% endif %><% endfor %>

The raw catalogue colour--mass fits indicate that \CG{} does not simply follow the same colour--mass relation as every comparison sample: <% set n_colour_slope_tests = r.Colour_mass_significant_global_tests|length %><% if n_colour_slope_tests == 4 -%>common slopes are rejected in all four colours<% elif n_colour_slope_tests > 0 -%>common slopes are rejected in << n_colour_slope_tests >> of the four colours<% else -%>common slopes are not rejected in the four colours<% endif %><% if r.Colour_mass_significant_pairwise_tests|length > 0 -%>, with the significant directional slope differences concentrated in the bluer colours and dependent on the adopted comparison catalogue<% endif %>. In the satellite-only comparison, the fitted offsets and slopes likewise depend on the control definition. These tests motivate the robustness analysis rather than establishing a stand-alone compact-group colour effect.

We therefore refit the satellite colours with cluster-robust standard errors, using catalogue-qualified group identifiers. In the pooled comparison adjusted only for stellar mass and redshift, <% if r.Colour_robustness_significant_baseline_blue|length == 0 -%>none of the four \CG{} coefficients is significantly negative after Holm correction<% else -%>\CG{} satellites are significantly bluer in <% for item in r.Colour_robustness_significant_baseline_blue -%><< item.colour_label >><% if not loop.last %>, <% else %><% endif %><% endfor %><% endif %>. <% if r.Colour_robustness_significant_full_blue|length > 0 -%>After additionally controlling for morphology and sSFR class, negative coefficients appear in <% for item in r.Colour_robustness_significant_full_blue -%><< item.colour_label >> ($\Delta=<< item.is_CG4_coefficient_fmt >>$ mag, Holm-corrected << p_from_fmt(item.p_holm_fmt) >>)<% if not loop.last %> and <% else %>.<% endif %><% endfor %><% else -%>Adding morphology and sSFR class does not reveal a significant negative coefficient.<% endif %> However, these adjusted coefficients are <% if r.Colour_robustness_summary.control_dependent -%>control-sample dependent<% else -%>consistent across the control catalogues<% endif %>, and <% if r.Colour_robustness_significant_ssfr_splits|length == 0 and r.Colour_robustness_significant_morphology_splits|length == 0 -%>they do not persist within the sSFR or morphology splits<% else -%>their split-sample behaviour is not uniform<% endif %>. The data therefore do not localize the colour effect to the star-forming spiral population expected for a simple interaction-triggered star-formation scenario.

Residual colour trends with projected BGG-centric distance are consistent with stronger processing near the group centre and a less processed outer population, but these trends use the same biased colour subset and are treated as exploratory. The group-level BGG--satellite colour concordance is similarly secondary: redder BGGs tend to have redder mean satellite populations in the combined group sample, but the \CG{} slope is not a robustly distinct population-level signature. Detailed colour--mass, satellite, and radial diagnostics are provided in the online supplement. Figure~\ref{fig:colour_robustness_coefficients} summarizes the model and control-sample dependence that motivates our interpretation: the present data do not establish a robust global blue or red offset for \CG{} satellites.

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< r.Colour_figures.robustness_coefficients >>}
    \caption{Robustness of the satellite colour coefficients. Left: pooled ordinary-group comparison before and after controlling for morphology and sSFR class. Right: mass- and redshift-adjusted comparisons against each control catalogue separately. Points show the \CG{} minus ordinary coefficient and bars show 95\% confidence intervals; negative values indicate bluer \CG{} satellites.}
    \label{fig:colour_robustness_coefficients}
\end{figure*}

\FloatBarrier

\section{Pooled models, matching details, and selection diagnostics}\label{app:pooled_models}
\suppressfloats[t]

<% set matched_app = es.get('matched_controls', {}) %>
<% set group_holm = matched_app.get('group_level_holm_sensitivity', {}) %>
<% if group_holm.get('status') == 'ok' %>
\subsection{Group-level multiplicity sensitivity}

The three per-control group-level satellite-composition matches are treated in the main text as distinct control-definition estimands, and their p-values are not adjusted across \CB{}, \CC{}, and \RG{}. Table~\ref{tab:group_match_holm_sensitivity} reports the Holm sensitivity requested by that alternative convention. The pooled group-level match is excluded because it is a secondary summary of overlapping control catalogues.

\begin{table}
\centering
\caption{Holm sensitivity for the three per-control group-level satellite-composition matches. The adjusted values are computed from the full-precision bootstrap and paired sign-flip permutation p-values.}
\label{tab:group_match_holm_sensitivity}
\small
\tabcolsep=3pt
\begin{tabular}{lrrrr}
\hline\hline
Control & \(p_{\rm perm}\) & Holm \(p_{\rm perm}\) & \(p_{\rm boot}\) & Holm \(p_{\rm boot}\) \\
\hline
<% for row in group_holm.get('rows', []) %>
\<< row.get('control')|replace('Control4B','CB')|replace('Control4C','CC')|replace('RG4','RG') >>{} & \(<< row.get('p_permutation')|fmt(3) >>\) & \(<< row.get('p_permutation_holm')|fmt(3) >>\) & \(<< row.get('p_bootstrap')|fmt(3) >>\) & \(<< row.get('p_bootstrap_holm')|fmt(3) >>\) \\
<% endfor %>
\hline
\end{tabular}
\end{table}
<% endif %>

\subsection{Pooled adjusted models}\label{sec:adjusted_galaxy_properties}
% MOVED: full pooled model family relocated here from old Sect. 5.2; the
% summary sentence and fig:specialness_logistic remain in the main text.
<% set sm = es.get('specialness_models', {}) %>
<% if sm.get('status') == 'ok' %>
As a compact summary across the heterogeneous control definitions we also fit binomial models to the pooled catalogue. Because the control samples overlap heavily (all \RG{} galaxies are also \CB{} members, and \CB{} and \CC{} share their cores), the pooled control side is first deduplicated by SDSS objid — one row per physical galaxy, unweighted, with every source label recorded — and standard errors are clustered by physical Lim group. The common adjustment set includes stellar mass, redshift, satellite/BGG rank, group luminosity and velocity dispersion when sufficiently complete. This pooled family is read as a secondary summary; the per-control contrasts of Sect.~\ref{sec:primary_contrasts} are primary.

<% for key, label in [('quenched_all', 'quenched status'), ('quenched_satellites', 'quenched satellite status'), ('starforming_satellites', 'star-forming satellite status'), ('elliptical_all', 'elliptical morphology'), ('elliptical_satellites', 'elliptical satellite morphology'), ('elliptical_bgg', 'elliptical BGG morphology'), ('spiral_all', 'spiral morphology'), ('spiral_satellites', 'spiral satellite morphology')] %>
<% set model = sm.get(key, {}) %>
<% if model.get('status') == 'ok' %>
For << label >>, the \CG{} odds ratio is \(<< model.get('cg4_odds_ratio')|fmt(2) >>\), with 95\% confidence interval \([<< model.get('cg4_ci95')[0]|fmt(2) >>, << model.get('cg4_ci95')[1]|fmt(2) >>]\) and << pholm_text(model.get('cg4_p_adj')) >>.
<% endif %>
<% endfor %>

<% set sig = sm.get('significant_models', []) %>
<% if ('elliptical_all' in sig) or ('spiral_all' in sig) %>
Compact-group membership therefore remains independently associated with morphology after the available adjustments: \CG{} galaxies have a higher probability of being elliptical and a lower probability of being spiral. In contrast, the adjusted quenched and star-forming satellite coefficients are weaker and do not provide evidence for an instantaneous star-formation difference that remains significant after correction.
<% elif sig %>
Compact-group membership remains independently associated with <% for item in sig -%><< item|replace('_', ' ') >><% if not loop.last %>, <% else %> after the available adjustments.<% endif %><% endfor %>
<% else %>
None of the \CG{} coefficients remains significant after correction across this model family.
<% endif %>
<% endif %>

\subsection{Galaxy-level matching details}
% MOVED: pool composition, provenance, complement outcomes, and the
% effects/balance figures relocated here from old Sect. 5.6.
<% set matched = es.get('matched_controls', {}) %>
<% if matched.get('status') == 'ok' %>
<% set cg4_audit = es.get('selection_diagnostics', {}).get('sample_size_audit', {}).get('CG4', {}) %>
<% set cg4_total_available = cg4_audit.get('total_galaxies', matched.get('n_cg4_matched')) %>
<% set cg4_unmatched = cg4_total_available - matched.get('n_cg4_matched') if cg4_total_available is not none and matched.get('n_cg4_matched') is not none else none %>
The galaxy-level control pool of Sect.~\ref{sec:matched} is deduplicated by SDSS objid before matching: << matched.get('n_control_rows_before_dedup') >> control rows collapse to << matched.get('n_control_galaxies_unique_pool') >> unique galaxies. <% if cg4_unmatched is not none and cg4_unmatched > 0 %>The << cg4_unmatched >> unmatched \CG{} galaxies were excluded because they lay outside the common propensity-score support or exceeded the adopted caliper. <% endif %>By kept label, the matched controls comprise <% for key, value in matched.get('matched_control_counts_by_sample', {}).items() %><< value >> << key >><% if not loop.last %>, <% endif %><% endfor %>; by physical provenance, << matched.get('n_matched_controls_physically_RG4') >> of the << matched.get('n_control_matched') >> matched controls are physically \RG{} galaxies (which also appear in \CB{} through the sample overlap), and the full per-galaxy provenance is recorded in the released \texttt{matched\_control\_provenance.csv}.<% if matched.get('balance_figure') %> The covariate balance before and after matching is shown in Fig.~\ref{fig:matched_control_balance}; the per-outcome matched-effect figure is provided in the online supplement.<% endif %>

For completeness, the complement outcomes of the matched family are as follows.
<% set m_sf = matched.get('effects', {}).get('starforming_fraction', {}) %>
<% if m_sf.get('status') == 'ok' %>
The \CG{}--minus--control difference in star-forming fraction is \(<< m_sf.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_sf.get('ci95')[0]|fmt(3) >>, << m_sf.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_sf.get('p_adj')) >>.
<% endif %>
<% set m_spiral = matched.get('effects', {}).get('spiral_fraction', {}) %>
<% if m_spiral.get('status') == 'ok' %>
The \CG{}--minus--control difference in spiral fraction is \(<< m_spiral.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_spiral.get('ci95')[0]|fmt(3) >>, << m_spiral.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_spiral.get('p_adj')) >>.
<% endif %>

<% if matched.get('balance_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< matched.get('balance_figure') >>}
    \caption{Absolute standardized mean differences before and after matching.}
    \label{fig:matched_control_balance}
\end{figure}
<% endif %>
<% endif %>

\FloatBarrier
\subsection{Selection diagnostics}\label{app:selection}
% MOVED: relocated here from old Sect. 5.12.
<% set selection = es.get('selection_diagnostics', {}) %>
<% if selection.get('status') == 'ok' %>
The fractions with complete measurements for the major derived quantities are shown in Fig.~\ref{fig:data_availability}; this consolidated audit includes the Simard and Petrosian size availability used by Sect.~\ref{sec:size_results}. The colour-matched versus unmatched comparisons quantify shifts in stellar mass, redshift, rank, star-formation class and morphology.

<% set angular = selection.get('nearest_angular_separation', {}) %>
<% if angular.get('status') == 'ok' %>
The median nearest-neighbour angular separation is << angular.get('median_cg4')|fmt(1) >> arcsec in \CG{} and << angular.get('median_control')|fmt(1) >> arcsec in the controls; the fractions below the 55-arcsec SDSS fibre-collision scale are << angular.get('cg4_fraction_below_threshold')|pct(1) >>\% and << angular.get('control_fraction_below_threshold')|pct(1) >>\%, respectively.
<% endif %>

<% if selection.get('strong_selection_bias') %>
Under the availability definitions shown in Fig.~\ref{fig:data_availability}, at least one major availability difference is both statistically significant and larger than 10 percentage points, so affected results are treated as exploratory.
<% else %>
Under the availability definitions shown in Fig.~\ref{fig:data_availability}, no major availability difference meets both criteria of corrected significance and a difference exceeding 10 percentage points.
<% endif %>

<% set avail = selection.get('availability_by_sample', {}) %>
<% set group_audit = selection.get('group_scale_column_audit', {}) %>
<% set missing_group_cols = group_audit.get('missing_or_sparse_columns', []) %>
<% if 'log_group_mass' in missing_group_cols %>
The group-scale availability row is based on the group covariates available for this catalogue: radius scale, velocity dispersion, group luminosity, and BGG luminosity dominance. A harmonized group-mass estimate is not available in these tables, so models that request it omit that covariate.
<% elif avail and avail.get('CG4', {}).get('group_scale_quantities') == 0 %>
The automated availability row for group-scale quantities should be interpreted as an alias audit for a specific set of column names rather than as evidence that no group-scale information is used elsewhere in the analysis.
<% endif %>

The colour-selection mass audit is provided in the online supplement.

<% if selection.get('availability_figure') %>
\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< SUBFIGURES_PATH >><< selection.get('availability_figure') >>}
    \caption{Availability fractions for the major derived quantities in each sample, including the Simard and Petrosian size-availability columns used by the size analysis. Cell labels give the number available over the final per-sample galaxy count and the corresponding percentage. The colour row records completeness of the four derived SDSS colour columns in the final catalogue, rather than the stricter colour-analysis matched subset used in Appendix~\ref{sec:optical_colours}. The Simard-size column reflects the DR7 bridge, redshift, quality-window, blending, and pegged-S\'ersic-index cuts described in Sect.~\ref{sec:size_data}.}
    \label{fig:data_availability}
\end{figure*}
<% endif %>
<% endif %>

\FloatBarrier

<% if sz.get('status') == 'ok' %>
\section{Extended size diagnostics}\label{app:size_details}
% MOVED: crowding, measure-difference, tidal, radial, and morphology-strata
% size diagnostics relocated here from old Sect. 5.11, with the size figures.
% The former size-availability figure is now consolidated into Fig. F.2.
% Start the retained substantive size figures at G.2 for cross-version continuity.
\setcounter{figure}{1}
<% set size_crowd = sz.get('crowding', {}) %>
<% set size_crowd_excl = size_crowd.get('excluding_close', {}).get('satellites', {}) %>
<% set size_crowd_flag = size_crowd.get('with_close_flag', {}).get('satellites', {}) %>
<% set size_md = sz.get('measure_delta', {}) %>
<% set size_tidal = sz.get('tidal', {}) %>
<% set size_radial_cg = sz.get('radial', {}).get('correlations', {}).get('CG4', {}) %>
<% set size_radial_ctrl = sz.get('radial', {}).get('correlations', {}).get('pooled_controls', {}) %>
<% set size_strata = sz.get('morphology_strata', {}) %>

The size-availability counts are included in the consolidated audit of Fig.~\ref{fig:data_availability}. This appendix therefore focuses on the extended size-effect diagnostics and on the contextual \(R_{\rm e}\)--S\'ersic-\(n\) plane; the descriptive mass--size relations and the per-sample sizes predicted at \(\log(M_\star/M_\odot) = << sz.get('mass_size', {}).get('reference_logMstar')|fmt(1) >>\) are provided in the online supplement.

<% if size_crowd_excl.get('status') == 'ok' and size_crowd_flag.get('status') == 'ok' %>
The crowding checks mirror Sect.~\ref{sec:morph_crowding}: excluding galaxies with a projected neighbour within << size_crowd.get('threshold_arcsec')|fmt(0) >> arcsec leaves a satellite coefficient of \(<< size_crowd_excl.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pval_text(size_crowd_excl.get('p')) >>), and adding the close-neighbour flag as a covariate gives \(<< size_crowd_flag.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pval_text(size_crowd_flag.get('p')) >>).
<% endif %>

<% if size_md.get('status') == 'ok' %>
The per-galaxy difference between the two measures, \(d = \log R_{\rm chl,r} - \log R_{50,r}\), averages \(<< size_md.get('mean_delta_dex')|fmt(3) >>\) dex<% if size_md.get('mean_by_morphology', {}).get('elliptical') and size_md.get('mean_by_morphology', {}).get('spiral') %>, and is larger for elliptical galaxies (\(<< size_md.get('mean_by_morphology', {}).get('elliptical', {}).get('mean')|fmt(3) >>\) dex) than for spiral galaxies (\(<< size_md.get('mean_by_morphology', {}).get('spiral', {}).get('mean')|fmt(3) >>\) dex), as expected if the Petrosian aperture preferentially truncates concentrated profiles<% endif %>. <% if size_md.get('spearman_vs_neighbour_separation') %>Its rank correlation with the nearest-neighbour separation is \(\rho_\mathrm{S} = << (size_md.get('spearman_vs_neighbour_separation', {}).get('rho', 0) + 0)|fmt(3) >>\) (<< pval_text(size_md.get('spearman_vs_neighbour_separation', {}).get('p')) >>), so residual blending does not drive a strong measure-dependent size systematic.<% endif %>
<% endif %>

<% if size_tidal.get('status') == 'ok' and size_tidal.get('baseline', {}).get('status') == 'ok' and size_tidal.get('with_tidal_index', {}).get('status') == 'ok' %>
Adding the pairwise projected tidal index of Sect.~\ref{sec:tidal_index} to the all-galaxy size model changes the \CG{} coefficient from \(<< size_tidal.get('baseline', {}).get('cg4_delta_dex')|fmt(3) >>\) dex to \(<< size_tidal.get('with_tidal_index', {}).get('cg4_delta_dex')|fmt(3) >>\) dex on the pairwise complete-case sample<% if szv.get('absorbed_by_tidal_index') %>; the local projected-density proxy attenuates the size coefficient in the same way it attenuates the morphology coefficient<% else %>; with a near-null baseline coefficient, the attenuation diagnostic used for morphology is not informative here<% endif %>.
<% endif %>

<% if size_radial_cg.get('status') == 'ok' and size_radial_ctrl.get('status') == 'ok' %>
The radial diagnostic is exploratory: mass- and redshift-adjusted satellite size residuals correlate with projected BGG-centric distance with \(\rho_\mathrm{S} = << size_radial_cg.get('spearman_rho')|fmt(2) >>\) (<< pval_text(size_radial_cg.get('spearman_p')) >>) in \CG{} and \(\rho_\mathrm{S} = << size_radial_ctrl.get('spearman_rho')|fmt(2) >>\) (<< pval_text(size_radial_ctrl.get('spearman_p')) >>) in the pooled controls. A more negative inner-region residual would be the tidal-truncation expectation; the observed trends are similar in \CG{} and the controls, so the data do not isolate a \CG{}-specific inner-region size suppression.
<% endif %>

<% set size_strata_ell = size_strata.get('elliptical_satellites', {}) %>
<% set size_strata_spi = size_strata.get('spiral_satellites', {}) %>
<% if size_strata_ell.get('status') == 'ok' and size_strata_spi.get('status') == 'ok' %>
Within morphology classes, the satellite-only fits give \(<< size_strata_ell.get('cg4_delta_dex')|fmt(3) >>\) dex for elliptical satellites (<< pholm_text(size_strata_ell.get('p_holm')) >>) and \(<< size_strata_spi.get('cg4_delta_dex')|fmt(3) >>\) dex for spiral satellites (<< pholm_text(size_strata_spi.get('p_holm')) >>)<% set size_strata_int = size_strata.get('interaction', {}).get('interaction_term') %><% if size_strata_int %>, and the \CG{}\(\times\)morphology interaction is \(<< size_strata_int.get('coefficient')|fmt(3) >>\) dex (<< pval_text(size_strata_int.get('p')) >>)<% endif %>. These strata are secondary diagnostics; they test whether any size offset is independent information beyond the morphology signal rather than a restatement of it.
<% endif %>

<% if sz.get('per_control', {}).get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('per_control', {}).get('figure') >>}
    \caption{Mass- and redshift-adjusted \CG{} size coefficients against the pooled controls and against each control catalogue separately. Bars show 95\% confidence intervals; negative values mean smaller \CG{} sizes at fixed mass.}
    \label{fig:size_forest_per_control}
\end{figure}
<% endif %>

<% if sz.get('re_n_plane_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('re_n_plane_figure') >>}
    \caption{Half-light radius versus S\'ersic index for \CG{} and the pooled controls, shown for galaxies with both valid Simard size and stellar mass in the mass--size analysis. The pooled controls are shown as a grey logarithmic hexbin density, while \CG{} galaxies are overplotted as foreground points; lines show binned medians where bins are sufficiently populated. This is the plane used by the S-PLUS transition-galaxy studies.}
    \label{fig:size_re_n_plane}
\end{figure}
<% endif %>

<% endif %>

\FloatBarrier
\end{appendix}
\clearpage

\end{document}
