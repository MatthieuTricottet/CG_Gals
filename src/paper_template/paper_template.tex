\documentclass{aa}
%
\usepackage{graphicx}
\usepackage[varg]{txfonts}
\usepackage{natbib,twoopt}
\usepackage[breaklinks=true]{hyperref} %% to avoid \citeads line fills
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
    pdftitle={Are galaxies in compact groups special?},
    pdfpagemode=FullScreen,
    }

\bibpunct{(}{)}{;}{a}{}{,}             %% natbib format for A&A and ApJ

\makeatletter
  \newcommandtwoopt{\citeads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citealp[#1][#2]{#3}}}
  \newcommandtwoopt{\citepads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citep[#1][#2]{#3}}}
  \newcommandtwoopt{\citetads}[3][][]{\href{http://adsabs.harvard.edu/abs/#3}%
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citet[#1][#2]{#3}}}
  \newcommandtwoopt{\citeyearads}[3][][]%
    {\href{http://adsabs.harvard.edu/abs/#3}
    {\def\hyper@linkstart##1##2{}%
     \let\hyper@linkend\@empty\citeyear[#1][#2]{#3}}}
\makeatother

% To add links in your PDF file, use the package "hyperref"
% with options according to your LaTeX or PDFLaTeX drivers.
%

\usepackage{graphicx}	% Including figure files
\usepackage{color}
\usepackage{array}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{ae,aecompl}
\usepackage[utf8]{inputenc}
\usepackage{url}
% \usepackage{autonum} % only cited equations are numbered

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{placeins}


\usepackage{listings}
\usepackage{xcolor}

\newcommand{\diazg}{D\'{\i}az-Gim\'enez}



\newcommand{\CB}{Control\textsubscript{4B}}
\newcommand{\CC}{Control\textsubscript{4C}}
\newcommand{\CG}{CG\textsubscript{4}}
\newcommand{\RG}{RG\textsubscript{4}}
\newcommand{\kms}{\,\mathrm{km\,s}^{-1}}
\newcommand{\gap}{$\Delta R_{1,2}$}
\newcommand{\msun}{\mathrm{M}_\odot}

\usepackage{color}
\usepackage{array}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{ae,aecompl}
\usepackage[utf8]{inputenc}
\usepackage{url}
% \usepackage{autonum} % only cited equations are numbered

\definecolor{darkgreen}{rgb}{0.0,0.5,0.0}
\definecolor{darkred}{rgb}{0.75,0.,0.2}
\definecolor{magenta}{rgb}{0.8,0,0.8}
\definecolor{purple}{rgb}{0.5,0,0.5}
\definecolor{gray}{rgb}{0.5,0.6,0.7}
\newcommand{\hide}[1]{\textcolor{gray}{#1}}
\newcommand{\red}[1]{\textcolor{red}{#1}}
\newcommand{\blue}[1]{\textcolor{blue}{#1}}
\newcommand\boldblue[1]{\textcolor{blue}{\mathbf{#1}}}
\newcommand\boldred[1]{\textcolor{darkred}{\mathbf{#1}}}
\newcommand{\gam}[1]{\textcolor{darkred}{\bf Gary: #1}}
\newcommand{\gm}[1]{\textcolor{darkred}{#1}}
\newcommand{\referee}[1]{\textcolor{red}{\bf Referee: #1}}
\newcommand{\MT}[1]{\textcolor{darkgreen}{\bf Matthieu: #1}}
\newcommand{\mt}[1]{\textcolor{darkgreen}{#1}}



\lstset{
  language=SQL,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue}\bfseries,
  stringstyle=\color{red},
  commentstyle=\color{gray}\itshape,
  morekeywords={SpecObj,PhotoObj,galSpecExtra,galSpecLine},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=5pt,
  showstringspaces=false,
  tabsize=4,
  breaklines=true,
  frame=single,
  captionpos=b
}

\graphicspath{{<<SUBFIGURES_PATH>>}}


\begin{document}
<% set sz = extended_specialness.get('size_analysis', {}) %>
<% set szv = sz.get('verdicts', {}) %>
\nolinenumbers
\title{Are galaxies in compact groups special?}

   \author{Matthieu Tricottet\inst{1}\thanks{ORCID: \url{https://orcid.org/0009-0008-0062-4450}}
           \and Gary A. Mamon\inst{2}\thanks{ORCID: \url{https://orcid.org/0000-0001-8956-5953}}
           \and Eugenia \diazg\inst{3,4}\thanks{ORCID: \url{https://orcid.org/0000-0001-5374-4810}}
           }

   \institute{
                80, rue d'Al\'esia, 75014 Paris, France\\
                \email{matthieu.tricottet@gmail.com}
                \and Institut d'Astrophysique de Paris (UMR 7095: CNRS \& Sorbonne Universit\'e), 98 bis boulevard Arago, 75014 Paris, France
                \and CONICET. Instituto de Astronom\'ia Te\'orica y Experimental (IATE), Laprida 854, X5000BGR, C\'ordoba, Argentina
                \and Universidad Nacional de C\'ordoba (UNC). Observatorio Astron\'omico de C\'ordoba (OAC), Laprida 854, X5000BGR, C\'ordoba, Argentina
            }

  \date{}

<# The size sentence in the Results block is static text asserting the
   current size-analysis verdicts (no pooled adjusted offset; matched pairs
   prefer smaller sizes, control-dependent). If the size analysis changes,
   revisit the abstract. #>
  \abstract
  {Compact groups combine high projected densities with low velocity dispersions, but many are embedded in larger structures. Their significance depends on whether their galaxies remain distinct from comparable regular-group members, not only from the field.}
  {We test whether galaxies in compact groups of four members (\CG) remain special against regular groups of four members (\RG), the four brightest galaxies of richer regular groups (\CB), and projected compact cores formed by the brightest group galaxy and its three nearest projected companions (\CC).}
  {From our previous samples, we retain <<CG4_Groups_nonsplit_N>> \CG{} systems after removing split systems. We combine Galaxy Zoo~1 vote fractions, Sloan Digital Sky Survey stellar masses and specific star-formation rates, optical colours, emission-line diagnostics, and seeing-corrected half-light radii, analysed with covariate-adjusted, group-clustered models and matched controls.}
  {\CG{} galaxies, especially satellites, have a higher elliptical-vote fraction and a lower spiral-vote fraction than their regular-group analogues. Per-control models show a strong signal against \CB{} and \RG{}, but not against the projected cores \CC. Matched \CG{} groups have higher elliptical-vote satellite fractions than each control in group-level matches, while the galaxy-level matched contrast is not significant once group membership is respected. The signal is stable to the vote-fraction threshold and a S\'ersic-index cross-check. Under the primary stratified within-host estimator, \CG{} members show no significant morphology excess over other members of the same host groups. Star-formation, colour, active-galactic-nucleus, magnitude-gap, \(\mathrm{H}\alpha\), and fixed-mass size diagnostics do not show comparably robust \CG{} offsets. A projected pairwise tidal index attenuates the morphology signal, consistent with a contrast concentrated in the most extreme local projected configurations.}
  {Compact groups host a more morphologically evolved population, consistent with accumulated transformation in dense group cores and/or preprocessing rather than a uniquely isolated present-day quenching mode.}

   \keywords{galaxies: groups: general -- galaxies: evolution -- galaxies: interactions -- galaxies: star formation -- galaxies: elliptical and lenticular, cD -- methods: statistical}

   \maketitle

<% macro p_from_fmt(fmt, label='p') -%><% set text = (fmt|string)|replace('$', '')|trim -%>\(<< label >><% if text.startswith('<') %><< text >><% else %> = << text >><% endif %>\)<%- endmacro %>
<% macro pval_text(x, nd=3) -%><% if x is none -%>\(p\) unavailable<% elif x < 0.0001 -%>\(p<10^{-4}\)<% elif x < 0.001 -%>\(p = << gu.latex_number(x, precision=2, math_mode=False) >>\)<% else -%>\(p = << x|fmt(nd) >>\)<% endif -%><% endmacro %>
<% macro pholm_text(x, nd=3) -%><% if x is none -%>Holm-corrected \(p\) unavailable<% elif x < 0.0001 -%>Holm-corrected \(p<10^{-4}\)<% elif x < 0.001 -%>Holm-corrected \(p = << gu.latex_number(x, precision=2, math_mode=False) >>\)<% else -%>Holm-corrected \(p = << x|fmt(nd) >>\)<% endif -%><% endmacro %>
<% macro qlabel_short(q) -%><% if q.key == 'sSFR' -%>\(\log_{10}\mathrm{sSFR}\)<% elif q.key == 'M_r' -%>\(M_r\)<% else -%>\(\log M_\star\)<% endif -%><% endmacro %>

\section{Introduction}\label{sec:intro}

Given their high apparent density, compact groups appear to be the ideal laboratories for frequent galaxy interactions, which alter galaxy masses, luminosities, sizes, morphologies and star formation rates. 
Moreover, their low velocity dispersions \citep{Hickson+92} make them ideal sites of mergers, which also affect these attributes, 
in particular their morphological classes (e.g., \citealt{Mamon92}). \citet{MendesdeOliveira&Hickson94} provided the classic report of an excess of early-type galaxies in compact groups relative to the field. In such configurations, repeated tidal encounters, dynamical friction, mergers, gas stripping or consumption, and disk heating can plausibly accelerate the transformation of galaxies compared with more diffuse environments \citep{Barnes85,Barnes89,Verdes-Montenegro+01}. This expectation is closely connected to the broader morphology-density and star-formation-environment relation, in which galaxy structure and star-formation activity vary systematically with local density \citep{Dressler80}.
Multi-wavelength studies of compact-group galaxies likewise report altered infrared, UV, star-formation, and population properties relative to looser environments \citep{Johnson+07,Walker+10,Tzanavaris+10,Bitsakis+10,Bitsakis+11,Coenda+12,Plauchu-Frayn+12}.

The interpretation of compact groups is nevertheless ambiguous. Projected selection can include chance alignments, and many Hickson-like systems need not be isolated bound entities. Simulations and redshift-space analyses have long shown that apparent compact groups may be transient alignments, dense substructures, or cores embedded in looser groups and richer haloes \citep{Mamon86,DiazGimenez&Mamon10,DiazGimenez+12,DiazGimenez+20,Zheng&Shen21,Taverna+23}. The relevant observational question is therefore not only whether compact-group galaxies differ from field galaxies, but whether they remain different from galaxies occupying regular-group analogues selected with comparable luminosity, redshift, and membership constraints.

Recent structural work strengthens this motivation. Using S-PLUS imaging and single-S\'ersic measurements, \citet{Montaguth2023,Montaguth2025,Montaguth2026MassSize,Montaguth2026NonIsolated} identified transition galaxies and trends in the \(R_{\rm e}\)--S\'ersic-index plane that point to structural transformation in compact groups, with stronger signatures in non-isolated compact groups and host structures. These studies suggest that compact groups can mark sites of cumulative structural processing. They do not, however, by themselves answer whether compact-group galaxies remain distinct after comparison with regular-group cores selected from the same parent catalogues.

Our previous work \citep{Tricottet+25} addressed the question of whether compact groups are special as dynamical systems. We found that, once the direct consequences of the compact-group selection are accounted for, compact groups of four galaxies have group-scale properties broadly similar to those of regular groups of four galaxies and to the cores of richer regular groups. This motivates the complementary question addressed here: even if compact groups are not strongly distinct as systems, are their member galaxies individually different? We therefore use the same compact-group and regular-group framework to compare galaxies rather than groups, controlling for stellar mass, redshift, galaxy rank, and available group-scale properties. We then identify which observable, if any, carries the residual compact-group signal: star-formation class, Galaxy Zoo morphology, optical colour, AGN-like activity, fossil-like magnitude gaps, projected phase-space position, or local projected tidal density.

The paper is organized as follows. Section~2 describes the samples, the sSFR and morphology classifications, and the statistical hierarchy. Sections~3 and 4 present baseline population differences and group-scale diagnostics. Section~5 tests which signals survive adjusted and matched comparisons and examines secondary diagnostics. Section~6 discusses the physical interpretation of the morphology signal, and Section~7 summarizes the conclusions.

\section{Data and classifications}
\subsection{Samples}\label{sec:samples}
We use the compact-group and control samples constructed in our previous study \citep{Tricottet+25}.
We briefly recall their construction here. Galaxy properties are taken from the SDSS DR12 catalogue
of \citet{Tempel+17}, while regular groups are drawn from the SDSS DR7 group catalogue of \citet{Lim+17}; the two
catalogues were matched through SDSS object identifiers to define the Lim--Tempel working catalogue. Compact groups
are selected from the modified Hickson compact-group catalogue of \citet{DiazGimenez+18}. Starting from the reliable
HMCGs with exactly four members, we retain only systems in which all galaxies are present in the Lim--Tempel catalogue and
impose the volume- and luminosity-complete limits $0.005 \le z_{\rm BGG} \le 0.0452$ and $M_{r,\rm BGG}\leq -21.81$, with
all 4 galaxies lying within 3 magnitudes of the brightest group galaxy. This defines the parent \CG{} sample of
78 compact groups. The regular-group comparison samples are built from Lim--Tempel groups satisfying the same redshift
and BGG-luminosity limits, with at least 4 members and at least 3 satellites within 3 magnitudes of the BGG.
From this parent control catalogue, we define \RG{} groups as regular groups with exactly four members, excluding
systems identical to a \CG, and we additionally use the \CB{} and \CC{} samples, formed respectively from
the 4 brightest galaxies and from the BGG plus its 3 closest projected companions in each parent regular group.
As in our previous paper, every control group containing at least one \CG{} galaxy is excluded from the control samples, so no galaxy appears on both sides of any comparison. The \CC{} sample is regenerated for this paper directly from the 765 parent groups (BGG plus the three closest projected companions, followed by the compact-group exclusion, which removes 60 groups)\footnote{Our previous paper quotes 61 excluded projected-core groups; the archived parent catalogue reproducibly yields 60. The difference traces to an earlier revision of the parent catalogue and does not affect any comparison in this paper.}. After the explicit Lim group 3688 removal described below, the control samples contain << Control4B_Groups_N >> \CB{}, << Control4C_Groups_N >> \CC{}, and << RG4_Groups_N >> \RG{} groups (<< Control4B_Gals_N >>, << Control4C_Gals_N >>, and << RG4_Gals_N >> galaxies). The \CB{} and \CC{} counts differ because the luminosity-ranked and projected-distance-ranked core definitions select different galaxies and therefore retain different Lim groups after the compact-group exclusion.
In contrast to our previous study, we remove CGs classified as \emph{split} following \citet{Zheng&Shen21}.
This reduces the compact-group sample from <<CG4_Groups_withsplit_N>> to
<<CG4_Groups_nonsplit_N>> groups. We also remove the systems associated with Lim group 3688 from our \CB{} and \CC{} samples. This group contains 6 galaxies
with redshifts between 0.032 and 0.034, and 1 with a redshift of 0.048. This group appears to be spurious, and the
outlying galaxy, although not the BGG, is selected in both our original \CB{} and \CC{} samples, significantly affecting the group velocity
dispersion ($1979$ and $1981\, \kms$, respectively).

The three regular-group controls serve different physical purposes. \RG{} contains true regular groups with exactly four selected members and is the most direct four-member regular-group comparison. \CB{} contains the four brightest galaxies in richer regular groups and tests whether the \CG{} population resembles the luminous core of an ordinary group. \CC{} contains the BGG plus the three closest projected companions and is the most compact-core-like ordinary-group analogue. \CG{} is therefore tested against both equal-membership groups and regular-group cores, rather than against a single control definition.

We use SDSS DR<<DATA_RELEASE>> \citep{Ahumada+20} to retrieve stellar masses, star formation rates \citep[SFRs;][]{Brinchmann+04,Kauffmann+03b}, and Galaxy Zoo morphologies
\citep{Lintott+08,GalaxyZoo1}. We specifically extract the columns \texttt{sfr\_tot\_p50},
\texttt{specsfr\_tot\_p50}, and \texttt{lgm\_tot\_p50} from the \texttt{galSpecExtra} table, and
\texttt{p\_el\_debiased} and \texttt{p\_cs\_debiased} from the \texttt{zooSpec} table. In the SDSS query, \texttt{galSpecExtra}, \texttt{galSpecLine}, and \texttt{zooSpec} are joined to \texttt{SpecObj} by \texttt{specObjID}, while the photometric object is linked through \texttt{SpecObj.bestObjID}=\texttt{PhotoObj.objID}; the queried quantities are then merged onto the CG and control catalogue rows by their stored \texttt{objid}.

Throughout, `SDSS selection' denotes the reference sample returned by the query of Appendix~\ref{app:sdss_query}: all spectroscopic galaxies with \(<<Z_MIN>> \le z \le <<Z_MAX>>\), extinction-corrected Petrosian \(r \le <<R_MAX>>\), a valid MPA-JHU stellar mass, and Galaxy Zoo classifications. It serves as a field-dominated reference population, not as a control sample.

All projected distances and derived quantities adopt the Planck 2015 cosmology \citep{Planck15}, consistently with our previous paper \citep{Tricottet+25}.


\subsection{sSFR classification}\label{sec:ssfr_classification}
We classify galaxies from SDSS total sSFRs using the query reported in Appendix~\ref{app:sdss_query} (Listing~\ref{lst:query}).
In the MPA-JHU catalogue, values of \(-9999\) in the sSFR fields are sentinel values indicating that no valid sSFR estimate is available, rather than physical sSFR measurements; the same missing-value convention is used for MPA-JHU DR8 target quantities by \citet{Zeraatgari+24}. For the total-sSFR field used here, we therefore treat \texttt{specsfr\_tot\_p50} \(= -9999\) as \emph{missing data}: such galaxies are excluded from the star-formation classification, from every sSFR-based figure and fraction, and are reported as counts only (Table~\ref{tab:sSFR_status}). All quenched and star-forming fractions quoted in this paper are computed among classified galaxies only. The missing-sSFR incidence is markedly higher in \CG{} (<< CG4_Gals_NNosSFR_pc|fmt(0) >>\% of members, and << CG4_BGG_NNosSFR_pc|fmt(0) >>\% of BGGs) than in the field selection (<< SDSS_NNosSFR_pc|fmt(0) >>\%), plausibly reflecting the crowded-field spectroscopy and deblending issues discussed in Sect.~\ref{sec:decoupling} rather than a physical population. Galaxies with a measured sSFR are split into quenched and star-forming classes with a two-component GMM in the \(\log M_\star\)-\(\log\mathrm{sSFR}\) plane.
BPT/AGN handling and the exact GMM boundary definition are given in Appendix~\ref{app:ssfr_method}.

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{sSFR_classification.pdf}
     \caption{sSFR versus stellar mass, the star-forming/quenched limit, and Galaxy Zoo morphologies for the SDSS selection,
     excluding galaxies without sSFR estimates.}
     \label{fig:sSFR_classification}
\end{figure}

\subsection{Morphology classification}
Morphologies are drawn from Galaxy Zoo 1 \citep{Lintott+08,GalaxyZoo1}, a single-question classification in which volunteers assigned each galaxy image to one of several morphological categories. The resulting debiased vote fractions are provided in the \texttt{zooSpec} table of the SDSS database through the columns \texttt{p\_el\_debiased} (fraction voting ``elliptical'') and \texttt{p\_cs\_debiased} (fraction voting combined-spiral; Lintott et al.\ 2011). We classify a galaxy as elliptical if \texttt{p\_el\_debiased} is greater than 0.5, and as spiral if \texttt{p\_cs\_debiased} is greater than 0.5. All other cases are classified as ``uncertain''. These are vote-fraction classes, not a full structural decomposition. In particular, the catalogue does not provide a clean E/S0 separation, so references below to elliptical or early-type galaxies should be read as Galaxy Zoo 1 elliptical-vote proxies for early-type morphology rather than as a bulge-disc structural classification.

<% if sz.get('status') == 'ok' %>
<% set size_cuts = sz.get('quality_cuts', {}) %>
<% set size_avail = sz.get('availability_audit', {}) %>
<% set size_totals = size_avail.get('totals', {}) %>
\subsection{Galaxy sizes}\label{sec:size_data}

Our primary galaxy-size measure is the circular half-light radius of the pure S\'ersic fit in the $r$ band, \(R_{\rm chl,r}\), from the bulge+disk decomposition catalogue of \citet{Simard2011}. These are PSF-convolved GIM2D fits with re-derived sky levels and deblending designed with crowded fields in mind, which is the relevant regime for compact-group photometry, so the fitted sizes are corrected for seeing. The catalogue is keyed to SDSS DR7 identifiers; we therefore bridge our DR<<DATA_RELEASE>> objects to their DR7 counterparts through the SkyServer \texttt{<< r.get('size_dr7_bridge', 'SpecDR7') >>} cross-match table. Two guards protect this bridge: rows in which the Simard redshift differs from the catalogue redshift by more than << size_cuts.get('z_match_tolerance')|fmt(3) >> are treated as wrong matches and their structural values are dropped (<< size_totals.get('z_mismatch') >> rows), and rows in which two distinct catalogue galaxies of the same group resolve to a single DR7 identifier are treated as blended DR7 detections and dropped (<< size_totals.get('shred_merge') >> rows). The Simard sizes are tabulated in kpc under the catalogue's own cosmology; we convert them back to angular sizes with the catalogue plate scale and then to proper kpc with the same Planck 2015 conversion as every other projected quantity in this paper (Sect.~\ref{sec:samples}).

As a secondary size measure we use the SDSS Petrosian radii \texttt{petroR50\_r} and \texttt{petroR90\_r}, which also define the concentration index \(C = R_{90,r}/R_{50,r}\). Petrosian radii are not corrected for seeing, and the SDSS Petrosian aperture misses a profile-dependent fraction of the flux, roughly 20\% for an \(n=4\) profile, which biases \(R_{50,r}\) low for concentrated galaxies \citep{Blanton2001,Graham2005} -- precisely the elliptical/early-type-like population in which \CG{} is enhanced. The Petrosian measure is nevertheless retained because it is model-independent, essentially fully available, and provides the like-for-like bridge to the compact-group size study of \citet{Coenda+12}. All sizes are required to lie between << size_cuts.get('size_min_kpc')|fmt(1) >> and << size_cuts.get('size_max_kpc')|fmt(0) >> kpc after re-conversion; S\'ersic fits pegged at the GIM2D bounds (\(n_g \le << size_cuts.get('ng_peg_low')|fmt(2) >>\) or \(n_g \ge << size_cuts.get('ng_peg_high')|fmt(1) >>\); << size_totals.get('n_pegged') >> galaxies) are excluded from the primary sample, and Petrosian entries must satisfy \(R_{50,r}>0\), \(R_{90,r}>R_{50,r}\), and a finite positive uncertainty. The completeness of both measures, and its difference between \CG{} and the controls, is audited in Sect.~\ref{sec:size_results}.
<% endif %>

\subsection{Statistical strategy}

The analyses are organized hierarchically. Raw tables and exact tests provide descriptive baseline comparisons. Fisher or Barnard exact tests are used for two-sample categorical comparisons depending on the table structure, while rank-sum or bootstrap comparisons are used for distributional and median differences. Spearman and Pearson correlations are used only as diagnostics of monotonic or linear trends; when the coefficients are small, we interpret them as weak even if the p-values are formally small. Holm or Benjamini--Hochberg corrections are applied where multiple related tests are performed.

The primary inference layer consists of mass-, rank-, and environment-adjusted binomial models fit \emph{separately} for each \CG{}--control pair (Sect.~\ref{sec:primary_contrasts}), and of a group-level matched comparison in which groups, not galaxies, are the units. Throughout, the independence unit is the \emph{physical} group: standard errors are clustered by Lim group (unifying the overlapping control definitions, with \CG{} systems assigned to their host Lim group), and all resampling is blocked at the group level, since the four galaxies of one group are not independent observations. Pooled models across the three controls are reported as secondary summaries only, after deduplicating the overlapping control samples to one row per physical galaxy. Bootstrap p-values use the add-one empirical convention \(p = (k+1)/(B+1)\) with the number of resamples \(B\) reported, so no resampling p-value can fall below its Monte-Carlo resolution; displayed p-values are floored at \(10^{-4}\). Colours, AGN-like fractions, \(\mathrm{H}\alpha\) equivalent widths, magnitude gaps, crossing-time trends, projected phase space, and the pairwise tidal index are secondary diagnostics. The main conclusions below rely on signals that survive the per-control adjusted and matched analyses, not on isolated exploratory tests.

\section{Baseline population differences}
\subsection{Raw sSFR fractions}

The sSFR status for each sample is shown in Table~\ref{tab:sSFR_status}. In the raw all-galaxy comparison, Fisher's exact test gives star-forming-fraction p-values of
<<p_from_fmt(pval_Control4B_Starforming_vs_CG)>> versus the \CB{} sample,
 <<p_from_fmt(pval_Control4C_Starforming_vs_CG)>> versus \CC, and  <<p_from_fmt(pval_RG4_Starforming_vs_CG)>> versus \RG. We treat these raw tests as descriptive because they do not adjust for stellar mass, rank, or group-scale differences.

 The same comparison restricted to BGGs and satellites is also shown in Table~\ref{tab:sSFR_status}.
For BGGs, the p-values obtained with <<BGG_sSFR_tests>> are
  <<p_from_fmt(BGG_star_forming_pvalue_Control4B_vs_CG4)>> against \CB,
  <<p_from_fmt(BGG_star_forming_pvalue_Control4C_vs_CG4)>> against \CC{} and
  <<p_from_fmt(BGG_star_forming_pvalue_RG4_vs_CG4)>>  against \RG.
For satellites, the corresponding p-values are
  <<p_from_fmt(Sat_star_forming_pvalue_Control4B_vs_CG4)>> against \CB,
  <<p_from_fmt(Sat_star_forming_pvalue_Control4C_vs_CG4)>> against \CC{} and
  <<p_from_fmt(Sat_star_forming_pvalue_RG4_vs_CG4)>> against \RG. The satellite deficit is clearest in the raw comparison with \RG{} and is revisited below with adjusted and matched analyses.

\begin{table}
    \centering
    \caption{Number of galaxies in each sSFR class for each sample.
    Galaxies without a valid sSFR estimate (`No sSFR') are excluded from the
    classification: their percentages are quoted with respect to all galaxies
    of the (sub)sample, while the quenched and star-forming percentages are
    quoted with respect to classified galaxies only.}
    \label{tab:sSFR_status}
    \tabcolsep=3pt
    \begin{tabular}{lccc}
        \hline\hline
            \textbf{Sample} & \textbf{  No sSFR  } & \textbf{  Quenched  } & \textbf{Star-forming} \\
            \hline
<% for tex, s in [('\\CG', 'CG4'), ('\\CB', 'Control4B'), ('\\CC', 'Control4C'), ('\\RG', 'RG4')] %>
<% set all_M = r[s ~ '_BGG_NNosSFR'] + r[s ~ '_Sat_NNosSFR'] %>
<% set all_Q = r[s ~ '_BGG_NQuenched'] + r[s ~ '_Sat_NQuenched'] %>
<% set all_SF = r[s ~ '_BGG_NStarforming'] + r[s ~ '_Sat_NStarforming'] %>
<% set all_C = all_Q + all_SF %>
<% set all_N = all_C + all_M %>
            << tex >> &  <<all_M>>  (<< (100 * all_M / all_N)|fmt(0) >>\%) &  <<all_Q>>  (<< (100 * all_Q / all_C)|fmt(0) >>\%) &  <<all_SF>>  (<< (100 * all_SF / all_C)|fmt(0) >>\%) \\
             \quad \emph{BGG} &  \emph{<< r[s ~ '_BGG_NNosSFR'] >>  (<< r[s ~ '_BGG_NNosSFR_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_BGG_NQuenched'] >>  (<< r[s ~ '_BGG_NQuenched_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_BGG_NStarforming'] >>  (<< r[s ~ '_BGG_NStarforming_pc']|fmt(0) >>\%)} \\
             \quad \emph{Satellites} &  \emph{<< r[s ~ '_Sat_NNosSFR'] >>  (<< r[s ~ '_Sat_NNosSFR_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_Sat_NQuenched'] >>  (<< r[s ~ '_Sat_NQuenched_pc']|fmt(0) >>\%)} &  \emph{<< r[s ~ '_Sat_NStarforming'] >>  (<< r[s ~ '_Sat_NStarforming_pc']|fmt(0) >>\%)} \\
<% endfor %>
         \hline
            \emph{SDSS selection} &  \emph{<<SDSS_NNosSFR>> (<<SDSS_NNosSFR_pc|fmt(0)>>\%)} &  \emph{<<SDSS_NQuenched>>  (<<SDSS_NQuenched_pc|fmt(0)>>\%)} &  \emph{<<SDSS_NStarforming>>  (<<SDSS_NStarforming_pc|fmt(0)>>\%)}\\
        \hline
    \end{tabular}
\end{table}

\subsection{Raw morphology fractions}

The morphologies obtained for each sample are shown in Table~\ref{tab:morphologies}.
Barnard's two-sided exact tests were used to compare the fraction of each morphology between \CG{} and each control sample. The resulting p-values are
<<p_from_fmt(pval_Control4B_Elliptical_vs_CG_pc)>> for ellipticals and <<p_from_fmt(pval_Control4B_Spiral_vs_CG_pc)>> for spirals versus the \CB{} sample,
 <<p_from_fmt(pval_Control4C_Elliptical_vs_CG_pc)>> and <<p_from_fmt(pval_Control4C_Spiral_vs_CG_pc)>> versus \CC{} and <<p_from_fmt(pval_RG4_Elliptical_vs_CG_pc)>>
 and <<p_from_fmt(pval_RG4_Spiral_vs_CG_pc)>> versus \RG. The galaxies in \CG{} systems, which we identified as cores of richer groups in
 \citet{Tricottet+25}, show a robust raw excess of elliptical classifications and a deficit of spiral classifications. Figure~\ref{fig:galaxies_morph_sfr} displays sSFR versus stellar mass for galaxies in the SDSS selection and in \CG.


\begin{table}
    \centering
    \caption{Number of galaxies in each Galaxy Zoo vote-fraction morphology proxy for each sample.}
    \label{tab:morphologies}
    \small
    \tabcolsep=2pt
    \begin{tabular}{lccc}
        \hline\hline
            \textbf{Sample} & \textbf{Elliptical} & \textbf{Spiral} & \textbf{Uncertain} \\
            \hline
            \CG &  <<CG4_Gals_N_Elliptical>>  (<<CG4_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<CG4_Gals_N_Spiral>>  (<<CG4_Gals_fraction_Spiral_pc|fmt(0)>>\%)&  <<CG4_Gals_N_Uncertain>>  (<<CG4_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \CB &  <<Control4B_Gals_N_Elliptical>>  (<<Control4B_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<Control4B_Gals_N_Spiral>>  (<<Control4B_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<Control4B_Gals_N_Uncertain>>  (<<Control4B_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \CC &  <<Control4C_Gals_N_Elliptical>>  (<<Control4C_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<Control4C_Gals_N_Spiral>>  (<<Control4C_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<Control4C_Gals_N_Uncertain>>  (<<Control4C_Gals_fraction_Uncertain_pc|fmt(0)>>\%) \\
            \RG &  <<RG4_Gals_N_Elliptical>>  (<<RG4_Gals_fraction_Elliptical_pc|fmt(0)>>\%) &  <<RG4_Gals_N_Spiral>>  (<<RG4_Gals_fraction_Spiral_pc|fmt(0)>>\%) &  <<RG4_Gals_N_Uncertain>>  (<<RG4_Gals_fraction_Uncertain_pc|fmt(0)>>\%)\\
            \emph{SDSS selection} &  \emph{<<SDSS_N_Elliptical>> (<<SDSS_fraction_Elliptical_pc|fmt(0)>>\%)} &  \emph{<<SDSS_N_Spiral>>  (<<SDSS_fraction_Spiral_pc|fmt(0)>>\%)} &  \emph{<<SDSS_N_Uncertain>>  (<<SDSS_fraction_Uncertain_pc|fmt(0)>>\%)}\\
        \hline
    \end{tabular}
\end{table}


\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{galaxies_sfr.pdf}
     \caption{sSFR and morphologies of galaxies in \CG{} and our SDSS selection.
     Galaxies without a valid sSFR estimate are excluded from this figure.}
     \label{fig:galaxies_morph_sfr}
\end{figure}

\subsection{Morphology versus sSFR}



Among star-forming \CG{} galaxies, <<CG_Nb_Spiral_Starforming>> are classified as spiral
(<<CG_fracpc_Spiral_Starforming>>\% after removing galaxies with uncertain morphology) and
<<CG_Nb_Elliptical_Starforming>> are classified as elliptical (<<CG_fracpc_Elliptical_Starforming>>\% after removing galaxies with uncertain morphology).
The corresponding numbers are <<Control4B_Nb_Spiral_Starforming>> spiral galaxies (<<Control4B_fracpc_Spiral_Starforming>>\%)
and <<Control4B_Nb_Elliptical_Starforming>> elliptical galaxies (<<Control4B_fracpc_Elliptical_Starforming>>\%) for \CB,
<<Control4C_Nb_Spiral_Starforming>> spiral galaxies (<<Control4C_fracpc_Spiral_Starforming>>\%)
and <<Control4C_Nb_Elliptical_Starforming>> elliptical galaxies (<<Control4C_fracpc_Elliptical_Starforming>>\%) for \CC,
and <<RG4_Nb_Spiral_Starforming>> spiral galaxies (<<RG4_fracpc_Spiral_Starforming>>\%)
and <<RG4_Nb_Elliptical_Starforming>> elliptical galaxies (<<RG4_fracpc_Elliptical_Starforming>>\%) for \RG.
The probabilities that these fractions are drawn from
the same distribution, estimated with <<Morph_sSFR_test>>, are <<p_from_fmt(pval_Control4B_Starforming_vs_CG_pc)>> against \CB,
<<p_from_fmt(pval_Control4C_Starforming_vs_CG_pc)>> against \CC, and <<p_from_fmt(pval_RG4_Starforming_vs_CG_pc)>> against \RG. We therefore find
neither a significant excess nor a deficit of
star-forming elliptical galaxies in \CG{} relative to any control sample.

When restricting the comparison to BGGs, we find <<CG4_BGG_N_Elliptical>> elliptical BGGs (<<CG4_BGG_fracpc_Elliptical>>\% of the BGGs) and
<<CG4_BGG_N_Spiral>> spiral BGGs (<<CG4_BGG_fracpc_Spiral>>\%)
 in \CG.
  The corresponding numbers are
 <<Control4B_BGG_N_Elliptical>> elliptical BGGs (<<Control4B_BGG_fracpc_Elliptical>>\%) and
 <<Control4B_BGG_N_Spiral>> spiral BGGs (<<Control4B_BGG_fracpc_Spiral>>\%) in \CB,
 <<Control4C_BGG_N_Elliptical>> elliptical BGGs (<<Control4C_BGG_fracpc_Elliptical>>\%) and
 <<Control4C_BGG_N_Spiral>> spiral BGGs (<<Control4C_BGG_fracpc_Spiral>>\%) in \CC,
  and  <<RG4_BGG_N_Elliptical>> elliptical BGGs (<<RG4_BGG_fracpc_Elliptical>>\%) and
  <<RG4_BGG_N_Spiral>> spiral BGGs (<<RG4_BGG_fracpc_Spiral>>\%)
  in \RG.
The p-values obtained with <<BGG_morph_tests>> are
  <<BGG_morph_pvalue_Control4B_vs_CG4>> against \CB,
  <<BGG_morph_pvalue_Control4C_vs_CG4>> against \CC{} and
  <<BGG_morph_pvalue_RG4_vs_CG4>>  against \RG.
Thus, BGG morphologies differ between \CG{} and \RG: \CG{} BGGs have a higher elliptical fraction than \RG{} BGGs.

\subsection{Population medians by sSFR class}

Tables~\ref{tab:quenched_starforming_medians}--\ref{tab:split_BGG_ssfr_class_properties_Sat} summarize the typical stellar masses, absolute magnitudes, and sSFRs of quenched and star-forming galaxies, including splits by the sSFR class of the BGG. These medians are descriptive and mainly show that star-formation class is entangled with stellar mass and luminosity, motivating the adjusted models below.

\begin{table}
\centering
\caption{Median values of galaxy properties for quenched and star-forming populations in each sample.
Uncertainties are estimated by bootstrapping (10\,000 iterations). sSFR is in yr\(^{-1}\), and stellar mass is in \(M_\odot\).
}
\label{tab:quenched_starforming_medians}
\small
\tabcolsep=2pt
\begin{tabular}{llcc}
\hline\hline
Sample & Quantity & Quenched & Star-forming\\
\hline

<% for sample in samples %>
<% for q in qdefs %>
<% if loop.first %><< sample >><% endif %> & << qlabel_short(q) >> & << r[sample ~ '_Quenched_' ~ q.key ~ '_median'] >> $\pm$ << r[sample ~ '_Quenched_' ~ q.key ~ '_median_err'] >> & << r[sample ~ '_Starforming_' ~ q.key ~ '_median'] >> $\pm$ << r[sample ~ '_Starforming_' ~ q.key ~ '_median_err'] >>\\
<% endfor %>
<% endfor %>

\hline
\end{tabular}
\end{table}


\begin{table}
    \centering
    \caption{Median values of various quantities for galaxies according to the sSFR class of their BGG (BGG included).
Uncertainties are estimated by bootstrapping (10\,000 iterations). sSFR is in yr\(^{-1}\), and stellar mass is in \(M_\odot\).}
    \label{tab:split_BGG_ssfr_class_properties}
    \small
    \tabcolsep=2pt
    \begin{tabular}{llcc}
\hline\hline
Sample & Quantity & BGG quenched & BGG SF\\
\hline

<% for sample in samples %>
<% for q in qdefs %>
<% if loop.first %><< sample >><% endif %> & << qlabel_short(q) >> & << r[sample ~ "_BGG_Quenched_" ~ q.key ~ "_median"] >> $\pm$ << r[sample ~ "_BGG_Quenched_" ~ q.key ~ "_median_err"] >> & << r[sample ~ "_BGG_Starforming_" ~ q.key ~ "_median"] >> $\pm$ << r[sample ~ "_BGG_Starforming_" ~ q.key ~ "_median_err"] >>\\
<% endfor %>
<% endfor %>


\hline
\end{tabular}
\end{table}

\begin{table}
    \centering
    \caption{Median values of various quantities for galaxies according to the sSFR class of their BGG (satellites only).
Uncertainties are estimated by bootstrapping (10\,000 iterations). sSFR is in yr\(^{-1}\), and stellar mass is in \(M_\odot\).}
    \label{tab:split_BGG_ssfr_class_properties_Sat}
    \small
    \tabcolsep=2pt
    \begin{tabular}{llcc}
\hline\hline
Sample & Quantity & BGG quenched & BGG SF\\
\hline

<% for sample in samples %>
<% for q in qdefs %>
<% if loop.first %><< sample >><% endif %> & << qlabel_short(q) >> & << r[sample ~ "_Sat_BGG_Quenched_" ~ q.key ~ "_median"] >> $\pm$ << r[sample ~ "_Sat_BGG_Quenched_" ~ q.key ~ "_median_err"] >> & << r[sample ~ "_Sat_BGG_Starforming_" ~ q.key ~ "_median"] >> $\pm$ << r[sample ~ "_Sat_BGG_Starforming_" ~ q.key ~ "_median_err"] >>\\
<% endfor %>
<% endfor %>


\hline
\end{tabular}
\end{table}


\subsection{Main sequence}
We model the star-forming main sequence as a function of stellar mass using polynomial orders 1--4; the polynomial-order comparison is shown in Appendix Fig.~\ref{fig:Main_Sequence_polyfits}. The diagnostic shown there is the residual RMS scatter, not a formal reduced \(\chi^2\), because no per-galaxy uncertainty model is used. We adopt order << r.get('Main_Sequence_polyfit_selected_order', 2) >> because it is the lowest-order model with the smallest five-fold cross-validated RMS scatter (<< r.get('Main_Sequence_polyfit_selected_cv_rms', 0)|fmt(5) >> dex, compared with << r.get('Main_Sequence_polyfit_order1_cv_rms', 0)|fmt(5) >> dex for order 1).
We then use order 2 to compute the residual of each star-forming galaxy with respect to the main sequence,
and show their distributions in Fig.~\ref{fig:main_sequence_residuals}. A positive residual means higher sSFR at fixed stellar mass relative to the fitted star-forming main sequence.

In the comparisons below, \(\Delta\) is the \CG{}-minus-control median residual; positive values mean that \CG{} star-forming galaxies lie above the fitted main sequence relative to the control. Each quoted interval is the central 68\% (16th--84th percentile) bootstrap interval on the median difference, and each \(p\) is the two-sided bootstrap sign-crossing probability for the same difference (10\,000 resamples). The 68\% interval and the two-sided \(p\) answer slightly different questions and can therefore appear to disagree near the significance boundary: a 68\% interval that excludes zero is compatible with \(p\) slightly above 0.05, as for \CB{} below.

The median main-sequence residual of \CG{} galaxies is:
\begin{itemize}
<% for key, res in build.items() %>
<% if key.startswith("pval_MSresiduals_") %>

<% set delta = res["Δmedian"] %>
<% set ci16 = res["CI_16"] %>
<% set ci84 = res["CI_84"] %>
<% set p = res["p_value"] %>
<% set name = key
    .replace("pval_MSresiduals_", "")
    .replace("_Gals", "") %>

\item <% if p is not none and p < 0.05 %>significantly different<% elif p is not none and p < 0.10 %>marginally different<% else %>not significantly different<% endif %> from that of << name >>,
with a median offset of
$\Delta = << "%.3f"|format(delta) >>$
($<< "%.3f"|format(ci16) >> \le \Delta \le << "%.3f"|format(ci84) >>$, 16th--84th percentile bootstrap interval),
corresponding to << pval_text(p) >>.

<% endif %>
<% endfor %>
\end{itemize}

Because the raw residual comparison and the matched comparison below condition on different covariate structures, and because the apparent offset changes in strength after matching, we treat SFMS residuals as a secondary diagnostic rather than as evidence for a robust compact-group star-formation offset.

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{main_sequence_residuals.pdf}
     \caption{Main-sequence residual distributions for the star-forming galaxies in each sample.}
     \label{fig:main_sequence_residuals}
\end{figure}

<% if r.get('Main_sequence_offset_by_cg_class_significant', []) %>
Within \CG, the class-split comparison of star-forming main-sequence offsets highlights
<% for item in r.get('Main_sequence_offset_by_cg_class_significant', []) %>
a difference between << item.class_a_label|default(item.class_a) >> and << item.class_b_label|default(item.class_b) >>
(medians << item.median_class_a_fmt >> and << item.median_class_b_fmt >>, respectively; << p_from_fmt(item.p_value_fmt) >>)<% if not loop.last %>; <% else %>.<% endif %>
<% endfor %>
<% else %>
Within \CG, the star-forming main-sequence offsets of the Embedded, Isolated and Predominant classes remain statistically consistent with one another.
<% endif %>

\section{Environmental and group-scale diagnostics}
\subsection{Distance to the BGG}
We also compare the sSFR of satellite galaxies with their distance to the BGG, expressed both in projected kpc and in units of $\langle R_{ij}\rangle$.
<% if r.get('Dist2BGG_significant_count', 0) > 0 %>
Significant monotonic trends are detected in the following cases.
<% for item in r.get('Dist2BGG_significant_correlations', []) %>
In << item.sample_label >>, sSFR correlates with << item.distance_label >> with Spearman coefficient
$\rho_\mathrm{S} = << item.spearman_rho_fmt >>$ for << item.n >> satellites (<< p_from_fmt(item.spearman_p_fmt) >>) and Pearson coefficient
$r = << item.pearson_r_fmt >>$ (<< p_from_fmt(item.pearson_p_fmt) >>).
<% endfor %>
<% else %>
No significant monotonic trend is detected between sSFR and distance to the BGG in either metric.
<% endif %>
The supporting distance plots are shown in Appendix Figs.~\ref{fig:dist2BGG_kpc_vs_sSFR} and \ref{fig:dist2BGG_norm_vs_sSFR}.

\subsection{Dominance}
<% set dom_summary = r.get('Dom_distribution_summary_by_sample', []) %>
<% set dom_significant = dom_summary|selectattr('has_significant_difference')|list %>
<% set dom_not_significant = dom_summary|rejectattr('has_significant_difference')|list %>
<% set spiral_corr = r.get('Dom_spiral_fraction_significant_correlations', []) %>
<% set c4c_size = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'size_Group_Bary_kpc')|list %>
<% set c4c_vdisp = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'Vdisp')|list %>
<% set c4c_lum = spiral_corr|selectattr('sample', 'equalto', 'Control4C')|selectattr('quantity', 'equalto', 'Lum_group')|list %>
<% set c4b_corr = spiral_corr|selectattr('sample', 'equalto', 'Control4B')|list %>
<% set cg4_dom_lum = spiral_corr|selectattr('sample', 'equalto', 'CG4')|selectattr('domination', 'equalto', 'Dominated')|selectattr('quantity', 'equalto', 'Lum_group')|list|first %>
<% set cg4_nondom_voffset = spiral_corr|selectattr('sample', 'equalto', 'CG4')|selectattr('domination', 'equalto', 'Non-dominated')|selectattr('quantity', 'equalto', 'Voffset')|list|first %>
We split groups into dominated systems, where the BGG contributes more than << (DOMINATIION_CRITERIA * 100)|round|int >>\% of the group luminosity, and non-dominated systems. This split does not produce a strong compact-group signal by itself: <% if dom_not_significant|length > 0 %><% if dom_not_significant|length == 1 -%><< dom_not_significant[0].sample_label >> shows no significant dominated/non-dominated difference after the Benjamini--Hochberg false-discovery-rate (FDR) correction<% else -%><% for item in dom_not_significant -%><< item.sample_label >><% if loop.revindex0 == 1 %> and <% elif not loop.last %>, <% else %> show no significant dominated/non-dominated difference after the Benjamini--Hochberg false-discovery-rate (FDR) correction<% endif %><% endfor %><% endif %><% else %>all samples show at least one corrected dominated/non-dominated difference<% endif %>. Here \(p_\mathrm{adj}\) denotes the FDR-adjusted p-value, and Cliff's \(\delta\) is a rank-based effect size ranging from -1 to 1.

Across the control and reference samples, the clearest binary separation is instead in group luminosity. Dominated systems have lower \(L_\mathrm{group}\) than their non-dominated counterparts: <% for item in dom_significant -%><% set p_adj_text = item.p_adj_fmt|replace('$', '') -%><< item.sample_label >> gives << item.median_dom_fmt >> versus << item.median_nondom_fmt >><% if lu.formatted_unit(item.quantity) %> << lu.formatted_unit(item.quantity) >><% endif %> (\(p_\mathrm{adj}<% if p_adj_text.startswith('<') %><< p_adj_text >><% else %>=<< p_adj_text >><% endif %>\), \(\delta=<< item.cliffs_delta_fmt >>\))<% if loop.revindex0 == 1 %>; and <% elif not loop.last %>; <% else %>.<% endif %><% endfor %> The negative \(\delta\) values mean that dominated groups tend to occupy the lower-luminosity side of each comparison.

The spiral-fraction correlations show a broader environmental pattern rather than a clean dominated/non-dominated dichotomy. <% if c4c_size|length > 0 %>The most coherent set appears in \CC, where \(S_\mathrm{frac}\) increases with \(\left\langle R_{ij}\right\rangle\) in both dominated and non-dominated groups (<% for item in c4c_size -%><% set p_text = item.p_value_fmt|replace('$', '') -%>\(\rho_\mathrm{S}=<< item.rho_fmt >>\), \(p<% if p_text.startswith('<') %><< p_text >><% else %>=<< p_text >><% endif %>\)<% if loop.revindex0 == 1 %> and <% endif %><% endfor %>)<% if c4c_vdisp|length > 0 or c4c_lum|length > 0 %>, while weaker negative trends are found with <% if c4c_vdisp|length > 0 %>\(\sigma_v\)<% if c4c_lum|length > 0 %> and <% endif %><% endif %><% if c4c_lum|length > 0 %>\(L_\mathrm{group}\)<% endif %><% endif %>.<% endif %> <% if c4b_corr|length > 0 %>\CB{} contributes only weaker non-dominated-group correlations. <% endif %><% if cg4_dom_lum or cg4_nondom_voffset %>In \CG, the significant trends occur in small subsets<% if cg4_dom_lum %>, with \(S_\mathrm{frac}\) decreasing with \(L_\mathrm{group}\) among dominated groups (\(\rho_\mathrm{S}=<< cg4_dom_lum.rho_fmt >>\), << cg4_dom_lum.n_groups >> groups)<% endif %><% if cg4_nondom_voffset %> and with \(\Delta V_\mathrm{BGG}/\sigma_v\) among non-dominated groups (\(\rho_\mathrm{S}=<< cg4_nondom_voffset.rho_fmt >>\), << cg4_nondom_voffset.n_groups >> groups)<% endif %>; we therefore treat these as supporting diagnostics rather than the main dominance result.<% endif %> The full distributional and correlation diagnostics are shown in Appendix Figs.~\ref{fig:Dom_vs_NonDom} and \ref{fig:Dom_vs_NonDom_XOR}.

\subsection{Morphology and domination}
<% if r.get('Morphology_domination_effects_count', 0) > 0 %>
When we further split the samples by domination and by the morphology of the BGG, significant effects only appear in the following cases.
\begin{itemize}
<% for item in r.get('Morphology_domination_effects', []) %>
\item << item.sample_label >> << item.domination_label >> groups, where the median satellite spiral fraction is
<< item.group_median_spiral_bgg_fmt >> for spiral BGGs and << item.group_median_elliptical_bgg_fmt >> for elliptical BGGs.
The association between BGG morphology and satellite spiral fraction has a group-level odds ratio of << item.group_or_fmt >> (<< p_from_fmt(item.group_p_fmt) >>) in this sample; the same within-sample contrast under a satellite-level clustered uncertainty treatment gives
an odds ratio of << item.sat_or_fmt >> (<< p_from_fmt(item.sat_p_fmt) >>).
<% endfor %>
\end{itemize}
In each case the group-level and satellite-level clustered treatments share the same point estimate by construction and differ only in the uncertainty model, which is why the odds ratios coincide.
<% else %>
Splitting the samples by both domination and BGG morphology does not reveal any significant modulation of the satellite morphology mix.
<% endif %>

<% set md = extended_specialness.get('morphology_dominance', {}) %>
<% if md.get('status') == 'ok' %>
\subsection{Morphology versus Zheng--Shen class, magnitude gap, and BGG dominance}

We next test whether the E/Sp morphology mix is associated with three group-level descriptors: the Zheng--Shen compact-group class, the first--second magnitude gap \(\Delta m_{12}=M_{r,2}-M_{r,1}\), and the continuous BGG luminosity fraction \(f_{L,\mathrm{BGG}}=L_\mathrm{BGG}/L_\mathrm{group}\). These tests use only the existing Galaxy Zoo elliptical and spiral classes; uncertain morphologies are excluded from the binary fits. We run the tests separately for all galaxies, BGGs only, and satellites only, so that any trend driven by the BGG itself is not folded into the satellite result. For satellites, \(f_{L,\mathrm{BGG}}\) mechanically includes the satellite luminosity in the group-luminosity denominator, so any satellite-level trend with \(f_{L,\mathrm{BGG}}\) is partly definitional; we therefore report the satellite models with a stellar-mass control.

<% set md_audit = md.get('dominance_audit', {}) %>
The magnitude-gap and BGG-luminosity-fraction quantities are available for all << md_audit.get('n_groups') >> \CG{}+\RG{} groups used in this test. The Zheng--Shen class is available only for compact groups. Split systems are absent from the current \CG{} sample because they are removed before the paper analysis, and no corresponding Zheng--Shen class is defined for the \RG{} catalogue used here. We therefore report the \RG{} class test as unavailable, while retaining \RG{} in the \(\Delta m_{12}\) and \(f_{L,\mathrm{BGG}}\) comparisons.

Overall, these tests provide little evidence that the E/Sp mix is driven by Zheng--Shen class, magnitude gap, or BGG luminosity fraction. The few marginal associations are therefore treated as descriptive diagnostics rather than as robust secondary trends.

\begin{table}
\centering
\caption{Elliptical and spiral counts by Zheng--Shen class for the \CG{} sample. Uncertain morphologies are excluded. Split systems have zero counts because they were removed by the existing non-split \CG{} sample definition.}
\label{tab:morphology_dominance_class_counts}
\small
\tabcolsep=2pt
\begin{tabular}{lccc}
\hline\hline
\makecell{Zheng--Shen\\class} & \makecell{All \CG{}\\E/Sp} & \makecell{\CG{} BGG\\E/Sp} & \makecell{\CG{} sat.\\E/Sp} \\
\hline
<% set cg_all_class = md.get('class_association', {}).get('CG4', {}).get('all', {}).get('contingency_table', []) %>
<% set cg_bgg_class = md.get('class_association', {}).get('CG4', {}).get('bgg', {}).get('contingency_table', []) %>
<% set cg_sat_class = md.get('class_association', {}).get('CG4', {}).get('satellites', {}).get('contingency_table', []) %>
<% for row in cg_all_class %>
<% set bgg_row = cg_bgg_class|selectattr('class', 'equalto', row.class)|list|first %>
<% set sat_row = cg_sat_class|selectattr('class', 'equalto', row.class)|list|first %>
<< row.class >> & << row.N_E >>/<< row.N_Sp >> & << bgg_row.N_E if bgg_row else 0 >>/<< bgg_row.N_Sp if bgg_row else 0 >> & << sat_row.N_E if sat_row else 0 >>/<< sat_row.N_Sp if sat_row else 0 >> \\
<% endfor %>
\hline
\end{tabular}
\end{table}

For the Zheng--Shen compact-group class, no \CG{} association reaches the conventional 5\% significance threshold. The closest case is the satellite-only test, which is treated as marginal and descriptive. Because the Zheng--Shen class is not defined for the \RG{} catalogue, we do not attempt an \RG{} class comparison.

\begin{table}
\centering
\caption{Tests of the E-vs-Sp morphology mix against compact-group class, magnitude gap, and BGG luminosity fraction. The continuous columns report the available adjusted logistic model for \(P(\mathrm{E})\); all-galaxy and satellite fits use group-clustered standard errors. Odds ratios are reported per magnitude for \(\Delta m_{12}\) and per 0.1 increase in luminosity fraction for \(f_{L,\mathrm{BGG}}\).}
\label{tab:morphology_dominance_models}
\small
\tabcolsep=2pt
\begin{tabular}{lllc}
\hline\hline
Subset & Sample & Test & Result \\
\hline
<% for subset in ['all', 'bgg', 'satellites'] %>
<% for sample in ['CG4', 'RG4'] %>
<% set class_res = md.get('class_association', {}).get(sample, {}).get(subset, {}) %>
<% set class_test = class_res.get('test', {}) %>
<% set gap_models = md.get('continuous_association', {}).get('Delta_m12', {}).get(sample, {}).get(subset, {}).get('models', {}) %>
<% set gap_model = gap_models.get('adjusted', {}) if gap_models.get('adjusted', {}).get('status') == 'ok' else gap_models.get('unadjusted', {}) %>
<% set fl_models = md.get('continuous_association', {}).get('f_L_BGG', {}).get(sample, {}).get(subset, {}).get('models', {}) %>
<% set fl_model = fl_models.get('adjusted', {}) if fl_models.get('adjusted', {}).get('status') == 'ok' else fl_models.get('unadjusted', {}) %>
<< md.get('subsets', {}).get(subset, subset) >> & <% if sample == 'CG4' %>\CG{}<% else %>\RG{}<% endif %> & Class & <% if class_test.get('status') == 'ok' %><< pval_text(class_test.get('p_value')) >><% elif sample == 'RG4' %>not defined<% else %>skipped<% endif %> \\
 & & \(\Delta m_{12}\) & <% if gap_model.get('status') == 'ok' %>OR \(<< gap_model.get('odds_ratio')|fmt(2) >>\), << pval_text(gap_model.get('p_value')) >><% else %>skipped<% endif %> \\
 & & \(f_{L,\mathrm{BGG}}\) & <% if fl_model.get('status') == 'ok' %>OR \(<< fl_model.get('odds_ratio')|fmt(2) >>\), << pval_text(fl_model.get('p_value')) >><% else %>skipped<% endif %> \\
<% endfor %>
<% endfor %>
\hline
\end{tabular}
\end{table}

For \(\Delta m_{12}\), no statistically significant morphology trend is found in \CG{} or \RG{}, whether the fit is run for all galaxies, BGGs only, or satellites only. The same conclusion holds for \(f_{L,\mathrm{BGG}}\): no fitted trend reaches \(p<0.05\). The \CG{}--\RG{} interaction models detect no significant difference in the morphology--dominance relation for either \(\Delta m_{12}\) or \(f_{L,\mathrm{BGG}}\), across all galaxies, BGGs only, and satellites only. A class interaction is not defined because the Zheng--Shen compact-group class has no \RG{} counterpart in the present catalogue. Across the morphology--class, morphology--gap, morphology--luminosity-fraction, and interaction tests, no result remains significant after Benjamini--Hochberg FDR control.
<% endif %>

\subsection{Crossing time}
<% set tcross_corr = r.get('Tcross_global_correlations', []) %>
<% set tcross_m_over_l = tcross_corr|selectattr('y_key', 'equalto', 'lg_M_virial_over_L')|list|first %>
<% set tcross_mass = tcross_corr|selectattr('y_key', 'equalto', 'lg_M_virial')|list|first %>
<% set tcross_lum = tcross_corr|selectattr('y_key', 'equalto', 'lg_Lum_group')|list|first %>
<% set p_m_over_l = tcross_m_over_l.p_value_fmt|replace('$', '') if tcross_m_over_l else '' %>
<% set p_mass = tcross_mass.p_value_fmt|replace('$', '') if tcross_mass else '' %>
<% set p_lum = tcross_lum.p_value_fmt|replace('$', '') if tcross_lum else '' %>
The crossing-time diagnostic mainly separates systems by dynamical scale rather than by luminosity alone. Shorter \(t_\mathrm{cross}\) systems have larger virial mass-to-light ratios and virial masses: the correlations with \(\log(\mathcal{M}_\mathrm{VT}/L_r)\) and \(\log(\mathcal{M}_\mathrm{VT})\) are \(\rho_\mathrm{S}=<< tcross_m_over_l.rho_fmt >>\) (\(p<% if p_m_over_l.startswith('<') %><< p_m_over_l >><% else %>=<< p_m_over_l >><% endif %>\)) and \(\rho_\mathrm{S}=<< tcross_mass.rho_fmt >>\) (\(p<% if p_mass.startswith('<') %><< p_mass >><% else %>=<< p_mass >><% endif %>\)), respectively, over << tcross_mass.n_groups >> groups. By contrast, the relation with total group luminosity is formally detected but very weak (\(\rho_\mathrm{S}=<< tcross_lum.rho_fmt >>\)). We therefore read \(t_\mathrm{cross}\) as a dynamical compactness indicator tied primarily to virial mass and mass-to-light ratio, with little independent leverage from \(L_\mathrm{group}\). Appendix Fig.~\ref{fig:tcr_vs_mvirial_over_l} shows the corresponding projected trend.


\subsection{Optical colours}\label{sec:optical_colours}
<% set colour_avail_cg4 = extended_specialness.get('selection_diagnostics', {}).get('availability_counts_by_sample', {}).get('CG4', {}).get('colours', {}) %>
We derive the $(u-r)$, $(u-g)$, $(g-r)$ and $(r-i)$ colours from the matched SDSS photometry. The retained colour-analysis matches include <% for item in r.Colour_matching_summary -%><< item.n_matched >> of << item.n_total >> galaxies in << item.sample_label >><% if not loop.last %>, <% else %>.<% endif %><% endfor %> This stricter subset differs from the availability audit in Fig.~\ref{fig:data_availability}: the figure's colour row only requires the four derived SDSS colour columns to be present in the final catalogue<% if colour_avail_cg4.get('n_available') and colour_avail_cg4.get('n_total') %>, giving << colour_avail_cg4.get('n_available') >> of << colour_avail_cg4.get('n_total') >> \CG{} galaxies<% endif %>, whereas the colour analysis here also requires successful matched-photometry retention and the covariates used below. Stellar mass is a confounding variable in colour comparisons \citep{Alpaslan+15}; we therefore model each colour jointly with $\log(M_\star/M_\odot)$ rather than comparing unadjusted colour distributions.

The colour-analysis subset is <% if r.Colour_robustness_matching_biased -%>not representative of the full galaxy catalogues: matched galaxies are systematically less massive, more actively star-forming, and more frequently satellites than unmatched galaxies<% else -%>broadly representative of the full catalogues in the audited quantities<% endif %>. The fractions retained in this stricter colour-analysis subset are <% for item in r.Colour_robustness_audit -%><< (100 * item.n_all_four_colours / item.n_galaxies)|round(1) >>\% in << item.sample >><% if not loop.last %>, <% else %>.<% endif %><% endfor %> The colour analysis is therefore useful as a secondary diagnostic, but it cannot carry the main interpretation by itself.

The raw catalogue colour--mass fits indicate that \CG{} does not simply follow the same colour--mass relation as every comparison sample: <% set n_colour_slope_tests = r.Colour_mass_significant_global_tests|length %><% if n_colour_slope_tests == 4 -%>common slopes are rejected in all four colours<% elif n_colour_slope_tests > 0 -%>common slopes are rejected in << n_colour_slope_tests >> of the four colours<% else -%>common slopes are not rejected in the four colours<% endif %><% if r.Colour_mass_significant_pairwise_tests|length > 0 -%>, with the significant directional slope differences concentrated in the bluer colours and dependent on the adopted comparison catalogue<% endif %>. In the satellite-only comparison, the fitted offsets and slopes likewise depend on the control definition. These tests motivate the robustness analysis rather than establishing a stand-alone compact-group colour effect.

We therefore refit the satellite colours with cluster-robust standard errors, using catalogue-qualified group identifiers. In the pooled comparison adjusted only for stellar mass and redshift, <% if r.Colour_robustness_significant_baseline_blue|length == 0 -%>none of the four \CG{} coefficients is significantly negative after Holm correction<% else -%>\CG{} satellites are significantly bluer in <% for item in r.Colour_robustness_significant_baseline_blue -%><< item.colour_label >><% if not loop.last %>, <% else %><% endif %><% endfor %><% endif %>. <% if r.Colour_robustness_significant_full_blue|length > 0 -%>After additionally controlling for morphology and sSFR class, negative coefficients appear in <% for item in r.Colour_robustness_significant_full_blue -%><< item.colour_label >> ($\Delta=<< item.is_CG4_coefficient_fmt >>$ mag, Holm-corrected << p_from_fmt(item.p_holm_fmt) >>)<% if not loop.last %> and <% else %>.<% endif %><% endfor %><% else -%>Adding morphology and sSFR class does not reveal a significant negative coefficient.<% endif %> However, these adjusted coefficients are <% if r.Colour_robustness_summary.control_dependent -%>control-sample dependent<% else -%>consistent across the control catalogues<% endif %>, and <% if r.Colour_robustness_significant_ssfr_splits|length == 0 and r.Colour_robustness_significant_morphology_splits|length == 0 -%>they do not persist within the sSFR or morphology splits<% else -%>their split-sample behaviour is not uniform<% endif %>. The data therefore do not localize the colour effect to the star-forming spiral population expected for a simple interaction-triggered star-formation scenario.

Residual colour trends with projected BGG-centric distance are consistent with stronger processing near the group centre and a less processed outer population, but these trends use the same biased colour subset and are treated as exploratory. The group-level BGG--satellite colour concordance is similarly secondary: redder BGGs tend to have redder mean satellite populations in the combined group sample, but the \CG{} slope is not a robustly distinct population-level signature. Detailed colour--mass, satellite, and radial diagnostics are shown in Appendix Figs.~\ref{fig:color_mass}--\ref{fig:colour_residual_distance}. Figure~\ref{fig:colour_robustness_coefficients} summarizes the model and control-sample dependence that motivates our interpretation: the present data do not establish a robust global blue or red offset for \CG{} satellites.

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< r.Colour_figures.robustness_coefficients >>}
    \caption{Robustness of the satellite colour coefficients. Left: pooled ordinary-group comparison before and after controlling for morphology and sSFR class. Right: mass- and redshift-adjusted comparisons against each control catalogue separately. Points show the \CG{} minus ordinary coefficient and bars show 95\% confidence intervals; negative values indicate bluer \CG{} satellites.}
    \label{fig:colour_robustness_coefficients}
\end{figure*}

<% set es = extended_specialness %>
<% if es and es.get('status') == 'ok' %>
\section{Robustness: what drives the compact-group differences?}

The previous sections establish that \CG{} galaxies differ from the control catalogues in their population mix, especially in morphology and in the star-forming fraction of satellites. We now ask whether these differences survive after controlling for stellar mass, rank and available group-scale quantities, and whether they are better interpreted as compact-group-specific evolution, preprocessing in dense halo cores, or catalogue-selection effects.

<% set sm = es.get('specialness_models', {}) %>
<% if sm.get('status') == 'ok' %>
<% set pcx = es.get('primary_contrasts', {}) %>
<% if pcx.get('status') == 'ok' %>
\subsection{Per-control adjusted contrasts (primary)}\label{sec:primary_contrasts}

The three control samples answer different questions, so our primary inference fits \CG{} against each control \emph{separately}: against \CB{} (is the \CG{} population special compared with the luminous population of richer ordinary groups?), against \CC{} (compared with BGG-centred projected cores?), and against \RG{} (compared with true four-member ordinary groups?). Each contrast fits the same family of adjusted binomial models (stellar mass, redshift, rank, group luminosity, and velocity dispersion where sufficiently complete), with standard errors clustered by \emph{physical} group: the Lim group for controls, and for \CG{} systems their host Lim group where one exists, so that the same physical system never enters as more than one cluster. Holm correction is applied within each contrast across its model family.

<% for control, tex in [('Control4B', '\\CB'), ('Control4C', '\\CC'), ('RG4', '\\RG')] %>
<% set contrast = pcx.get('contrasts', {}).get(control, {}) %>
<% set ell = contrast.get('elliptical_all', {}) %>
<% set qnc = contrast.get('quenched_all', {}) %>
<% if ell.get('status') == 'ok' %>
Against << tex >>{}, the elliptical odds ratio is \(<< ell.get('cg4_odds_ratio')|fmt(2) >>\) (95\% CI \([<< ell.get('cg4_ci95')[0]|fmt(2) >>, << ell.get('cg4_ci95')[1]|fmt(2) >>]\); << pholm_text(ell.get('cg4_p_adj')) >>)<% if qnc.get('status') == 'ok' %>, and the quenched odds ratio is \(<< qnc.get('cg4_odds_ratio')|fmt(2) >>\) (<< pholm_text(qnc.get('cg4_p_adj')) >>)<% endif %>.
<% endif %>
<% endfor %>

The asymmetry between the contrasts is itself informative: the \CG{} morphology excess is strong against the luminosity-selected cores of richer groups and against true four-member groups, but weak and non-significant against the BGG-centred \emph{projected} cores of ordinary groups, which are the control definition that most closely mimics a compact projected configuration. This pattern anticipates the local projected-density interpretation of Sect.~\ref{sec:tidal_index}.

<% if pcx.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< pcx.get('figure') >>}
    \caption{Adjusted \CG{} odds ratios from the three separate per-control contrasts (colour-coded by control sample) for the all-member elliptical, spiral and quenched outcomes. Bars show 95\% confidence intervals; the vertical line marks equal odds.}
    \label{fig:primary_contrasts}
\end{figure}
<% endif %>
<% endif %>

\subsection{Pooled adjusted models (secondary)}
\label{sec:adjusted_galaxy_properties}

As a compact summary across the heterogeneous control definitions we also fit binomial models to the pooled catalogue. Because the control samples overlap heavily (all \RG{} galaxies are also \CB{} members, and \CB{} and \CC{} share their cores), the pooled control side is first deduplicated by SDSS objid — one row per physical galaxy, unweighted, with every source label recorded — and standard errors are clustered by physical Lim group. The common adjustment set includes stellar mass, redshift, satellite/BGG rank, group luminosity and velocity dispersion when sufficiently complete. This pooled family is read as a secondary summary; the per-control contrasts above are primary.

<% for key, label in [('quenched_all', 'quenched status'), ('quenched_satellites', 'quenched satellite status'), ('starforming_satellites', 'star-forming satellite status'), ('elliptical_all', 'elliptical morphology'), ('elliptical_satellites', 'elliptical satellite morphology'), ('elliptical_bgg', 'elliptical BGG morphology'), ('spiral_all', 'spiral morphology'), ('spiral_satellites', 'spiral satellite morphology')] %>
<% set model = sm.get(key, {}) %>
<% if model.get('status') == 'ok' %>
For << label >>, the \CG{} odds ratio is \(<< model.get('cg4_odds_ratio')|fmt(2) >>\), with 95\% confidence interval \([<< model.get('cg4_ci95')[0]|fmt(2) >>, << model.get('cg4_ci95')[1]|fmt(2) >>]\) and << pholm_text(model.get('cg4_p_adj')) >>.
<% endif %>
<% endfor %>

<% set sig = sm.get('significant_models', []) %>
<% if ('elliptical_all' in sig) or ('spiral_all' in sig) %>
Compact-group membership therefore remains independently associated with morphology after the available adjustments: \CG{} galaxies have a higher probability of being elliptical and a lower probability of being spiral. In contrast, the adjusted quenched and star-forming satellite coefficients are weaker and do not provide evidence for an instantaneous star-formation difference that remains significant after correction.
<% elif sig %>
Compact-group membership remains independently associated with <% for item in sig -%><< item|replace('_', ' ') >><% if not loop.last %>, <% else %> after the available adjustments.<% endif %><% endfor %>
<% else %>
None of the \CG{} coefficients remains significant after correction across this model family.
<% endif %>

<% set ell_sat = sm.get('elliptical_satellites', {}) %>
<% set spi_sat = sm.get('spiral_satellites', {}) %>
<% set ell_bgg = sm.get('elliptical_bgg', {}) %>
<% if ell_sat.get('status') == 'ok' and spi_sat.get('status') == 'ok' %>
The adjusted morphology signal is primarily carried by satellites rather than BGGs: the satellite-only models give a elliptical odds ratio of \(<< ell_sat.get('cg4_odds_ratio')|fmt(2) >>\) and a spiral odds ratio of \(<< spi_sat.get('cg4_odds_ratio')|fmt(2) >>\), whereas <% if ell_bgg.get('status') == 'ok' %>the BGG elliptical coefficient is not significant after adjustment (OR \(<< ell_bgg.get('cg4_odds_ratio')|fmt(2) >>\); << pholm_text(ell_bgg.get('cg4_p_adj')) >>)<% else %>the BGG morphology model does not provide a significant adjusted counterpart<% endif %>.
<% endif %>

<% if sm.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sm.get('figure') >>}
    \caption{Adjusted \CG{} odds ratios for star-formation and morphology diagnostics, including all-galaxy, satellite-only, and BGG-only morphology rows. Bars show 95\% confidence intervals; the vertical line marks equal odds.}
    \label{fig:specialness_logistic}
\end{figure}
<% endif %>
<% endif %>

<% set crowd = es.get('morphology_robustness', {}) %>
<% if crowd.get('status') == 'ok' %>
\subsection{Morphology robustness to projected crowding}\label{sec:morph_crowding}

<% set crowd_rows = crowd.get('fractions_after_excluding_close', []) %>
<% set crowd_cg_list = crowd_rows|selectattr('sample', 'equalto', 'CG4')|list %>
<% set crowd_c4c_list = crowd_rows|selectattr('sample', 'equalto', 'Control4C')|list %>
<% set crowd_cg = crowd_cg_list|first if crowd_cg_list|length > 0 else {} %>
<% set crowd_c4c = crowd_c4c_list|first if crowd_c4c_list|length > 0 else {} %>
<% set crowd_tests = crowd.get('exact_tests_after_excluding_close', []) %>
<% set crowd_c4c_ell_list = crowd_tests|selectattr('control', 'equalto', 'Control4C')|selectattr('outcome', 'equalto', 'elliptical')|list %>
<% set crowd_c4c_ell = crowd_c4c_ell_list|first if crowd_c4c_ell_list|length > 0 else {} %>
<% set crowd_model_ell = crowd.get('adjusted_models_with_close_flag', {}).get('elliptical', {}) %>
<% set crowd_close_term = crowd_model_ell.get('terms', {}).get('close_neighbour_55arcsec', {}) %>
Because Galaxy Zoo morphology is image-based, the \CG{} elliptical excess must be checked against projected crowding and deblending. We therefore repeat the morphology comparison after excluding galaxies whose nearest projected neighbour within the catalogue group lies within << crowd.get('threshold_arcsec')|fmt(0) >> arcsec. The retained samples contain << crowd_cg.get('n_after_excluding_close') >> \CG{} galaxies and << crowd_c4c.get('n_after_excluding_close') >> \CC{} galaxies. After this exclusion, the \CG{} elliptical fraction is << crowd_cg.get('elliptical_fraction_after_excluding_close')|pct(1) >>\%, compared with << crowd_c4c.get('elliptical_fraction_after_excluding_close')|pct(1) >>\% in \CC{}; the \CG{}--\CC{} exact-test result remains significant (<< pval_text(crowd_c4c_ell.get('p_value')) >>). The \CG{} elliptical fraction rises relative to the raw full-sample value in Table~\ref{tab:morphologies}, rather than falling after the closest projected pairs are removed. This argues against the simplest interpretation that the \CG{} elliptical excess is produced solely by crowding in the closest pairs.

The complementary close-neighbour-flag model reaches the same qualitative conclusion. <% if crowd_model_ell.get('status') == 'ok' %>After adjusting for the standard covariates and a \(<55\)-arcsec neighbour flag, the \CG{} elliptical odds ratio is \(<< crowd_model_ell.get('cg4_odds_ratio')|fmt(2) >>\) (95\% CI \([<< crowd_model_ell.get('cg4_ci95')[0]|fmt(2) >>, << crowd_model_ell.get('cg4_ci95')[1]|fmt(2) >>]\); << pval_text(crowd_model_ell.get('cg4_p')) >>).<% endif %> <% if crowd_close_term %>The close-neighbour flag is itself associated with elliptical-vote morphology (OR \(<< crowd_close_term.get('odds_ratio')|fmt(2) >>\); << pval_text(crowd_close_term.get('p')) >>), so this test does not prove that all Galaxy Zoo classifications are immune to crowding. It does rule out the simplest closest-pair artefact as the sole origin of the morphology result.<% endif %> Figure~\ref{fig:morphology_crowding_robustness} shows the corresponding post-exclusion morphology fractions.

<% if crowd.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< crowd.get('figure') >>}
    \caption{Galaxy Zoo morphology fractions after excluding galaxies with a nearest projected neighbour within 55 arcsec. The \CG{} elliptical excess persists after this closest-neighbour exclusion.}
    \label{fig:morphology_crowding_robustness}
\end{figure}
<% endif %>
<% endif %>

<% set sweep = es.get('morphology_threshold_sweep', {}) %>
<% if sweep.get('status') == 'ok' %>
\subsection{Morphology robustness to the classification threshold}\label{sec:morph_threshold}

The adopted Galaxy Zoo classification (debiased vote fraction \(> 0.5\)) is one of several defensible cuts, so we repeat the adjusted all-member elliptical model on the deduplicated pooled sample at thresholds of 0.4, 0.5, 0.6 and 0.8. The \CG{} odds ratio varies from << sweep.get('or_range')[0]|fmt(2) >> to << sweep.get('or_range')[1]|fmt(2) >> across this range<% if sweep.get('qualitatively_stable') %> and never changes sign, so the morphology contrast is not tied to the adopted threshold<% endif %> (the unclassified fraction grows from << sweep.get('thresholds', {}).get('0.4', {}).get('fraction_unclassified')|pct(0) >>\% to << sweep.get('thresholds', {}).get('0.8', {}).get('fraction_unclassified')|pct(0) >>\% over the sweep). A threshold-free check models the debiased elliptical vote fraction \(p_E\) itself: the adjusted \CG{} offset is \(<< sweep.get('continuous', {}).get('cg4_coefficient')|fmt(3) >>\) (95\% CI \([<< sweep.get('continuous', {}).get('cg4_ci95')[0]|fmt(3) >>, << sweep.get('continuous', {}).get('cg4_ci95')[1]|fmt(3) >>]\); << pval_text(sweep.get('continuous', {}).get('cg4_p')) >>).
<% set sersic = sweep.get('sersic_early_late', {}) %>
<% if sersic.get('cg4_odds_ratio') %>
A structural cross-check independent of Galaxy Zoo uses the \citet{Simard2011} pure-S\'ersic index (early type: \(n_g > 2.5\); << sersic.get('coverage')|pct(0) >>\% coverage): the adjusted \CG{} odds ratio is \(<< sersic.get('cg4_odds_ratio')|fmt(2) >>\) (<< pval_text(sersic.get('cg4_p')) >>), consistent in direction with the vote-fraction results, subject to the differential Simard completeness discussed in Sect.~\ref{sec:size_results}.
<% endif %>
<% endif %>

<% set matched = es.get('matched_controls', {}) %>
<% if matched.get('status') == 'ok' %>
\subsection{Matched-control comparison}

<% set cg4_audit = es.get('selection_diagnostics', {}).get('sample_size_audit', {}).get('CG4', {}) %>
<% set cg4_total_available = cg4_audit.get('total_galaxies', matched.get('n_cg4_matched')) %>
<% set cg4_unmatched = cg4_total_available - matched.get('n_cg4_matched') if cg4_total_available is not none and matched.get('n_cg4_matched') is not none else none %>
We matched << matched.get('n_cg4_matched') >> of the << cg4_total_available >> available \CG{} galaxies one-to-one to << matched.get('n_control_matched') >> ordinary-group galaxies <% if not matched.get('replacement') %>without<% else %>with<% endif %> replacement, requiring common propensity-score support, a << matched.get('propensity_caliper_logit_sd')|fmt(1) >>-standard-deviation caliper and exact rank strata. <% if cg4_unmatched is not none and cg4_unmatched > 0 %>The remaining << cg4_unmatched >> \CG{} galaxies were excluded because they lay outside the common propensity-score support or exceeded the adopted caliper. <% endif %>The control pool is first deduplicated by SDSS objid (<< matched.get('n_control_rows_before_dedup') >> control rows collapse to << matched.get('n_control_galaxies_unique_pool') >> unique galaxies), and two hard constraints are enforced: no control may share an objid with a \CG{} galaxy, and no control galaxy is used more than once. By kept label, the matched controls comprise <% for key, value in matched.get('matched_control_counts_by_sample', {}).items() %><< value >> << key >><% if not loop.last %>, <% endif %><% endfor %>; by physical provenance, << matched.get('n_matched_controls_physically_RG4') >> of the << matched.get('n_control_matched') >> matched controls are physically \RG{} galaxies (which also appear in \CB{} through the sample overlap), and the full per-galaxy provenance is recorded in the released \texttt{matched\_control\_provenance.csv}. The propensity model uses \(\log M_\star\), redshift, rank, \(\log L_\mathrm{group}\), and \(\sigma_v\). Projected distance is retained as a phase-space and interaction diagnostic rather than matched away because it partly defines compactness and has limited overlap. The largest absolute standardized mean difference changed from << matched.get('max_abs_smd_before')|fmt(2) >> before matching to << matched.get('max_abs_smd_after')|fmt(2) >> afterwards. The residual imbalance is carried by the group-luminosity covariate (\(\log L_\mathrm{group}\)); it lies slightly above the conventional 0.1 threshold, which is why we read the matched contrasts together with the adjusted models rather than in isolation.

Matched differences below are quoted with group-blocked bootstrap intervals: pairs are resampled by the treated galaxy's compact group (never galaxy by galaxy), with \(B = << matched.get('effects', {}).get('elliptical_fraction', {}).get('n_boot', 9999) >>\) resamples and add-one empirical p-values, so the smallest reportable p-value is \(2/(B+1)\).

<% set m_group = matched.get('group_level', {}) %>
<% set m_pc = matched.get('group_level_per_control', {}) %>
<% if m_group.get('status') == 'ok' %>
\paragraph{Group-level matched contrast.} Because the four galaxies of a compact group are not independent, the cleanest matched estimand is at the \emph{group} level: for each group we count its elliptical-vote satellites (\(N_{\rm ell,sat} \in \{0,1,2,3\}\)) and match \CG{} groups one-to-one to control groups on redshift, BGG stellar mass, total quartet luminosity, and velocity dispersion (propensity caliper 0.2\,SD of the logit). We run the matching three times with the pool restricted to a single control type (one quartet per physical Lim group within each type), and once with the pool drawn from all three types combined. The p-values are an add-one bootstrap sign-crossing probability and a paired sign-flip permutation p (9999 resamples, add-one convention).

<% set comp = m_group.get('matched_control_composition', {}) %>
The pooled control match covers << m_group.get('n_cg4_groups_available') >> \CG{} groups and << m_group.get('n_control_groups_available') >> control groups, yielding << m_group.get('n_matched_groups') >> matched pairs. The matched controls are drawn predominantly from \CB{} (<% for key, val in comp.items() %><< val >> << key >><% if not loop.last %>, <% endif %><% endfor %>, reflecting the pool composition after deduplication). The elliptical-vote satellite fraction difference is \(\Delta = << m_group.get('delta_smooth_satellite_fraction')|fmt(3) >>\) (95\% bootstrap interval \([<< m_group.get('ci95')[0]|fmt(3) >>, << m_group.get('ci95')[1]|fmt(3) >>]\); bootstrap << pval_text(m_group.get('p')) >><% if m_group.get('p_permutation') is not none %>, permutation \(p = << m_group.get('p_permutation')|fmt(3) >>\)<% endif %>; \(B = << m_group.get('n_boot') >>\)).

The per-control matches (Table~\ref{tab:group_match_per_control}) give a consistent picture: the \CG{}--minus--control fraction difference is positive against all three control types. \CC{} is the physically most stringent comparison (projected-core definition), as it most closely mirrors the \CG{} compactness criterion.
<% if m_pc.get('Control4C', {}).get('status') == 'ok' %>
<% set pc4c = m_pc.get('Control4C', {}) %>
Against \CC{}, the match yields << pc4c.get('n_matched_groups') >> pairs, \(\Delta = << pc4c.get('delta_smooth_satellite_fraction')|fmt(3) >>\) (95\% CI \([<< pc4c.get('ci95')[0]|fmt(3) >>, << pc4c.get('ci95')[1]|fmt(3) >>]\); bootstrap << pval_text(pc4c.get('p')) >><% if pc4c.get('p_permutation') is not none %>, permutation \(p = << pc4c.get('p_permutation')|fmt(3) >>\)<% endif %>).
<% endif %>
The galaxy-level contrasts below are read as secondary checks of this group-level result.

\begin{table}[h]
\centering
\caption{Per-control group-level matched contrasts. \(\Delta\) is the matched mean difference in elliptical-vote satellite fraction (CG4 minus control). Both p-values use an add-one convention with \(B = 9999\) resamples.}
\label{tab:group_match_per_control}
\begin{tabular}{lrrrr}
\hline\hline
Control & Pairs & \(\Delta\) & 95\% CI & \(p_{\rm boot}\) / \(p_{\rm perm}\) \\
\hline
<% for ctrl_label in ['Control4B', 'Control4C', 'RG4'] %>
<% set pc = m_pc.get(ctrl_label, {}) %>
<% if pc.get('status') == 'ok' %>
\<< ctrl_label.replace('Control4B','CB').replace('Control4C','CC').replace('RG4','RG') >>{} & << pc.get('n_matched_groups') >> & \(<< pc.get('delta_smooth_satellite_fraction')|fmt(3) >>\) & \([<< pc.get('ci95')[0]|fmt(3) >>, << pc.get('ci95')[1]|fmt(3) >>]\) & \(<< pc.get('p')|fmt(3) >>\) / \(<< pc.get('p_permutation')|fmt(3) >>\) \\
<% else %>
\<< ctrl_label.replace('Control4B','CB').replace('Control4C','CC').replace('RG4','RG') >>{} & --- & --- & --- & --- \\
<% endif %>
<% endfor %>
Pooled & << m_group.get('n_matched_groups') >> & \(<< m_group.get('delta_smooth_satellite_fraction')|fmt(3) >>\) & \([<< m_group.get('ci95')[0]|fmt(3) >>, << m_group.get('ci95')[1]|fmt(3) >>]\) & \(<< m_group.get('p')|fmt(3) >>\) / \(<< m_group.get('p_permutation')|fmt(3) >>\) \\
\hline
\end{tabular}
\end{table}
<% endif %>

<% set m_quenched = matched.get('effects', {}).get('quenched_fraction', {}) %>
<% if m_quenched.get('status') == 'ok' %>
The matched \CG{}--minus--control difference in quenched fraction is \(<< m_quenched.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_quenched.get('ci95')[0]|fmt(3) >>, << m_quenched.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_quenched.get('p_adj')) >>.
<% if m_quenched.get('p_adj') is not none and m_quenched.get('p_adj') < 0.05 %>
This supports an enhanced quenched fraction in \CG{} after matching.
<% elif m_quenched.get('p_adj') is not none and m_quenched.get('p_adj') < 0.10 %>
This is suggestive but not formally significant after correction, so we treat the quenched-fraction excess as borderline.
<% else %>
After matching, this does not provide evidence for an enhanced quenched fraction in \CG{} that remains significant after correction.
<% endif %>
<% endif %>

<% set m_sf = matched.get('effects', {}).get('starforming_fraction', {}) %>
<% if m_sf.get('status') == 'ok' %>
The corresponding \CG{}--minus--control difference in star-forming fraction is \(<< m_sf.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_sf.get('ci95')[0]|fmt(3) >>, << m_sf.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_sf.get('p_adj')) >>.
<% endif %>

<% set m_ell = matched.get('effects', {}).get('elliptical_fraction', {}) %>
<% if m_ell.get('status') == 'ok' %>
The matched \CG{}--minus--control difference in elliptical fraction is \(<< m_ell.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_ell.get('ci95')[0]|fmt(3) >>, << m_ell.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_ell.get('p_adj')) >>.
<% endif %>

<% set m_spiral = matched.get('effects', {}).get('spiral_fraction', {}) %>
<% if m_spiral.get('status') == 'ok' %>
The matched \CG{}--minus--control difference in spiral fraction is \(<< m_spiral.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_spiral.get('ci95')[0]|fmt(3) >>, << m_spiral.get('ci95')[1]|fmt(3) >>]\) and << pholm_text(m_spiral.get('p_adj')) >>.
<% endif %>

<% set m_resid = matched.get('effects', {}).get('residual_sSFR_starforming', {}) %>
<% if m_resid.get('status') == 'ok' %>
Among matched star-forming galaxies, the residual-sSFR difference is \(<< m_resid.get('delta_cg4_minus_control')|fmt(3) >>\), with 95\% bootstrap interval \([<< m_resid.get('ci95')[0]|fmt(3) >>, << m_resid.get('ci95')[1]|fmt(3) >>]\) and << pval_text(m_resid.get('p')) >>.
<% endif %>

<% if matched.get('holm_correction_note') %>
The matched-outcome Holm correction is intentionally conservative: quenched/star-forming and elliptical/spiral diagnostics are retained in the same family even though the audit finds that they are exact complements on their complete-case subsets. Using a non-redundant family would not weaken the morphology conclusion.
<% endif %>

<% set ssfr_alt = r.get('sSFR_robustness', {}) %>
<% if ssfr_alt.get('status') == 'ok' and ssfr_alt.get('primary_comparison') %>
The main interpretation does not rely on the exact GMM division of the sSFR plane. Repeating the key satellite comparison with a fixed threshold, << ssfr_alt.get('threshold_label') >>, gives \CG{} and \RG{} satellite star-forming fractions of << ssfr_alt.get('cg4', {}).get('starforming_fraction_fmt') >>\% and << ssfr_alt.get('primary_comparison', {}).get('control', {}).get('starforming_fraction_fmt') >>\%, respectively, a \CG{}--\RG{} difference of \(<< ssfr_alt.get('primary_comparison', {}).get('delta_starforming_fraction_pct_fmt') >>\) percentage points (<< p_from_fmt(ssfr_alt.get('primary_comparison', {}).get('fisher_p_fmt')) >>). The qualitative hierarchy is unchanged: the morphology result is independent of the sSFR threshold, whereas the quenched/star-forming contrast remains more sensitive to modelling choices.
<% endif %>

The matched comparison is therefore consistent with the adjusted models: the most robust compact-group signal is morphological, while the quenched and star-forming-fraction signals are more model-dependent.

<% if matched.get('effects_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< matched.get('effects_figure') >>}
    \caption{\CG{}--minus--control effects after one-to-one matching. Bars show 95\% bootstrap confidence intervals.}
    \label{fig:matched_control_effects}
\end{figure}
<% endif %>
<% if matched.get('balance_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< matched.get('balance_figure') >>}
    \caption{Absolute standardized mean differences before and after matching.}
    \label{fig:matched_control_balance}
\end{figure}
<% endif %>
<% endif %>

<% set phase = phase_space_segregation if phase_space_segregation else es.get('phase_space_segregation', es.get('phase_space', {})) %>
<% if phase.get('status') == 'ok' %>
<% set host = es.get('host_controlled', {}) %>
<% if host.get('status') == 'ok' %>
\subsection{Compact-group members versus other members of the same host group}\label{sec:host_controlled}

For the Embedded and Predominant \CG{} systems, whose quartets live inside richer Lim parent groups, the sharpest test of ``compact subconfiguration versus shared host environment'' compares the \CG{} member galaxies with the \emph{non-\CG{} members of the same host group}. The primary estimator is conditional logistic regression stratified by host group (conditional logit): the stratum-level intercept is conditioned out in the partial likelihood, so every host-level confounder shared by all members of the same host (halo mass, richness, redshift, large-scale environment) is removed by design. As a robustness check we also fit a binomial GLM with host-group fixed-effect dummies and cluster-robust standard errors by host (FE-GLM). The original pooled binomial GLM with cluster-robust standard errors by host is retained for comparison; unlike the stratified estimators, it cannot remove host-level confounders. Over << host.get('n_hosts') >> host groups (<< host.get('n_members') >> member galaxies, of which << host.get('n_cg_members') >> belong to a \CG), the models include stellar mass, host luminosity rank, and projected host-centric radius as galaxy-level covariates.
<% set host_ell = host.get('models', {}).get('elliptical', {}) %>
<% set host_qnc = host.get('models', {}).get('quenched', {}) %>
<% set host_ell_cl = host_ell.get('conditional_logit', {}) %>
<% set host_ell_fe = host_ell.get('fe_glm', {}) %>
<% set host_qnc_cl = host_qnc.get('conditional_logit', {}) %>
<% if host_ell.get('status') == 'ok' %>
For the conditional logit,<% if host_ell_cl.get('n_concordant_strata', 0) > 0 %> << host_ell_cl.get('n_concordant_strata') >> concordant strata (all-0 or all-1 outcomes within one host) are automatically excluded, leaving << host_ell_cl.get('n_informative_strata') >> informative strata;<% else %> all << host_ell_cl.get('n_informative_strata', host.get('n_hosts')) >> host strata are informative (no concordant strata);<% endif %> the elliptical-vote odds ratio for \CG{} membership is \(<< host_ell.get('cg_member_odds_ratio')|fmt(2) >>\) (95\% CI \([<< host_ell.get('cg_member_ci95')[0]|fmt(2) >>, << host_ell.get('cg_member_ci95')[1]|fmt(2) >>]\); << pholm_text(host_ell.get('cg_member_p_adj')) >>)<% if host_qnc.get('status') == 'ok' %>, and the quenched odds ratio is \(<< host_qnc.get('cg_member_odds_ratio')|fmt(2) >>\) (<< pholm_text(host_qnc.get('cg_member_p_adj')) >>)<% endif %>.<% if host_ell_fe.get('status') == 'ok' %> The FE-GLM gives a consistent elliptical-vote odds ratio of \(<< host_ell_fe.get('is_CG_member_odds_ratio')|fmt(2) >>\) (\(p = << host_ell_fe.get('is_CG_member_p')|fmt(3) >>\))<% if host_ell_fe.get('separation_flag') %>, though partial separation is flagged<% endif %>.<% endif %>
<% if host_ell.get('cg_member_p_adj') is not none and host_ell.get('cg_member_p_adj') >= 0.05 %>
At fixed stellar mass, rank, and host-centric radius, the stratified estimator finds no detectable morphology excess for compact-subconfiguration membership \emph{within} a shared host. This is consistent with the per-control asymmetry of Sect.~\ref{sec:primary_contrasts}: the \CG{} excess appears against galaxies of \emph{other} groups, not against co-members of the same host, pointing to position within the host (its dense projected core) rather than compact-group membership per se.
<% endif %>
<% endif %>
<% endif %>

\subsection{Satellite segregation around the brightest group galaxy}

<% set pst = phase.get('text_summary', {}) %>
The previous sections show global differences between \CG{} and \RG{} satellites. We now ask whether these differences are concentrated near the BGG, as expected for accelerated evolution in the dense inner group, or whether they persist across the satellite population. The analysis uses << pst.get('cg_satellite_n') >> \CG{} and << pst.get('rg_satellite_n') >> \RG{} satellites, excludes rank-1 galaxies, ranks satellites by projected BGG-centric distance, and measures quenched and early-type fractions with group-cluster bootstrap uncertainties. When available, we also use \(|\Delta v_{\rm BGG}|/\sigma_v\), with \(\Delta v_{\rm BGG}=c(z_{\rm gal}-z_{\rm BGG})/(1+z_{\rm group})\), as a projected phase-space diagnostic. Similar projected phase-space coordinates are often used as observational proxies for satellite infall state or quenching time, but only statistically \citep{Mahajan+11,Oman&Hudson16,Wetzel+13}.

<% set inner_early_delta_display = ((pst.get('cg_earlytype_fraction_inner') * 100)|round(1)) - ((pst.get('rg_earlytype_fraction_inner') * 100)|round(1)) %>
In the innermost satellite-distance bin, the quenched fractions are << pst.get('cg_quenched_fraction_inner')|pct(1) >>\% for \CG{} and << pst.get('rg_quenched_fraction_inner')|pct(1) >>\% for \RG, a \CG{}--\RG{} difference of \(<< pst.get('delta_quenched_fraction_inner')|pct(1) >>\) percentage points. The corresponding early-type fractions are << pst.get('cg_earlytype_fraction_inner')|pct(1) >>\% and << pst.get('rg_earlytype_fraction_inner')|pct(1) >>\%, with a difference of \(<< inner_early_delta_display|fmt(1) >>\) percentage points.
<% if pst.get('quenched_radial_trend') == 'stronger_inner' %>
The quenched-fraction contrast is larger in the inner bin than in the outer bin, favouring a centrally concentrated environmental component.
<% elif pst.get('quenched_radial_trend') == 'stronger_outer' %>
The quenched-fraction contrast is not confined to the inner bin; it is at least as large at larger projected distance, which is more consistent with preprocessing or a global sample difference.
<% elif pst.get('quenched_radial_trend') == 'similar_at_inner_and_outer_ranks' %>
The quenched-fraction contrast is similar between the inner and outer distance ranks, so the data do not isolate the \CG{} offset to the immediate BGG environment.
<% else %>
The radial dependence of the quenched-fraction contrast cannot be determined robustly with the available bin counts.
<% endif %>

<% set quenched_model = phase.get('regression_results', {}).get('quenched_distance', {}) %>
<% if quenched_model.get('status') == 'ok' %>
After adjusting for stellar mass, redshift, distance bin, and available group-scale controls, the quenched-status model gives a \CG{} odds ratio of << pst.get('quenched_model_sample_cg_or')|fmt(2) >> (95\% CI << pst.get('quenched_model_sample_cg_ci_low')|fmt(2) >>--<< pst.get('quenched_model_sample_cg_ci_high')|fmt(2) >>; << pval_text(pst.get('quenched_model_sample_cg_p')) >>).
<% endif %>
<% set early_model = phase.get('regression_results', {}).get('early_type_distance', {}) %>
<% if early_model.get('status') == 'ok' %>
The analogous early-type model gives a \CG{} odds ratio of << pst.get('earlytype_model_sample_cg_or')|fmt(2) >> (<< pval_text(pst.get('earlytype_model_sample_cg_p')) >>).
<% endif %>
<% set matched_phase = phase.get('matched_robustness', {}) %>
<% if matched_phase.get('status') == 'ok' %>
A nearest-neighbour mass--redshift matched check uses << matched_phase.get('n_cg4_matched') >> \CG{}--\RG{} satellite pairs. The matched quenched-fraction difference is \(<< pst.get('matched_delta_quenched')|pct(1) >>\) percentage points, and the matched early-type difference is \(<< pst.get('matched_delta_earlytype')|pct(1) >>\) percentage points.
<% endif %>

Projected phase-space coordinates should not be read as orbital reconstructions for individual galaxies. In compact groups the member counts are small, velocity dispersions are noisy, and projection mixes recently accreted and long-resident satellites. We therefore treat these bins as an observational diagnostic of where the \CG{}--\RG{} population contrast appears, rather than as a direct dynamical clock.

<% set pfigs = phase.get('figure_paths', {}) %>
<% if pfigs.get('quenched_fraction_by_distance') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< pfigs.get('quenched_fraction_by_distance') >>}
    \caption{Quenched satellite fraction as a function of projected-distance rank from the BGG. Points show \CG{} and \RG{} fractions; bars show group-cluster bootstrap 16th--84th percentile intervals.}
    \label{fig:phase_space_quenched_distance}
\end{figure}
<% endif %>
<% if pfigs.get('earlytype_fraction_by_distance') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< pfigs.get('earlytype_fraction_by_distance') >>}
    \caption{Early-type satellite fraction as a function of projected-distance rank from the BGG. Because the catalogue separates ellipticals, spirals, and uncertain morphologies but not S0 galaxies, early type is equivalent to elliptical in this diagnostic.}
    \label{fig:phase_space_earlytype_distance}
\end{figure}
<% endif %>
<% endif %>

<% set fossil = es.get('fossilness', {}) %>
<% if fossil.get('status') == 'ok' %>
\subsection{Magnitude gaps and fossil-like assembly}

We define \(\Delta m_{12}=M_{r,2}-M_{r,1}\) and \(\Delta m_{14}=M_{r,4}-M_{r,1}\), so that a more dominant BGG has a positive, larger gap.

<% set dm12 = fossil.get('sample_comparisons', {}).get('Delta_m12', {}) %>
<% if dm12.get('status') == 'ok' %>
For \(\Delta m_{12}\), the \CG{} and control medians are \(<< dm12.get('median_cg4')|fmt(2) >>\) and \(<< dm12.get('median_control')|fmt(2) >>\) mag, respectively (<< pholm_text(dm12.get('p_adj')) >>).
<% endif %>

<% set dm14 = fossil.get('sample_comparisons', {}).get('Delta_m14', {}) %>
<% if dm14.get('status') == 'ok' %>
For \(\Delta m_{14}\), the \CG{} and control medians are \(<< dm14.get('median_cg4')|fmt(2) >>\) and \(<< dm14.get('median_control')|fmt(2) >>\) mag, respectively (<< pholm_text(dm14.get('p_adj')) >>).
<% endif %>

<% if not fossil.get('magnitude_gap_significant') %>
The compact-group morphology signal is therefore not accompanied by a significant excess in these simple fossil-like magnitude-gap indicators.
<% endif %>

<% set gap_quenched = fossil.get('correlations', {}).get('Delta_m12_vs_quenched_satellite_fraction', {}) %>
<% if gap_quenched.get('status') == 'ok' %>
The correlation between \(\Delta m_{12}\) and quenched satellite fraction is \(\rho_{\rm S}=<< gap_quenched.get('spearman_rho')|fmt(2) >>\) (<< pholm_text(gap_quenched.get('p_adj')) >>).
<% endif %>

The supporting magnitude-gap distributions are shown in Appendix Figs.~\ref{fig:magnitude_gap} and \ref{fig:magnitude_gap_quenched_fraction}.
<% endif %>

<% set recent = es.get('recent_quenching', {}) %>
<% if recent.get('status') in ['ok', 'limited'] %>
\subsection{Recently quenched candidates}
<% set strong_halpha_threshold = recent.get('classification_thresholds', {}).get('strong_halpha_emission')|replace('_', '\\_')|replace('\\,\\AA', '\\,\\text{\\AA}') %>

<% if recent.get('status') == 'limited' %>
Our retrieved catalogues do not include the age-sensitive indices \(D_n4000\) and H\(\delta_A\), which are available in the MPA-JHU \texttt{galSpecIndx} product, so we defer a post-starburst classification to follow-up work. We therefore perform only a limited \(\mathrm{H}\alpha\)-equivalent-width comparison, using the SDSS convention in which negative equivalent width denotes emission. We classify strong \(\mathrm{H}\alpha\) emission using \(<< strong_halpha_threshold >>\).
<% elif recent.get('status') == 'ok' %>
The available \(D_n4000\), H\(\delta_A\), and \(\mathrm{H}\alpha\) measurements permit a direct recently-quenched classification.
<% endif %>

<% if recent.get('status') == 'limited' %>
The strong-emission fraction is <% for sample, item in recent.get('fractions_by_sample', {}).items() -%><< item.get('strong_emission_fraction')|pct(1) >>\% in << sample|replace('CG4', '\CG{}')|replace('Control4B', '\CB{}')|replace('Control4C', '\CC{}')|replace('RG4', '\RG{}') >><% if not loop.last %>, <% else %>.<% endif %><% endfor %>
<% for comp_name, comp in recent.get('comparisons', {}).items() %>
<% if comp.get('status') == 'ok' %>
Relative to << comp_name|replace('CG4_vs_', '')|replace('Control4B', '\CB{}')|replace('Control4C', '\CC{}')|replace('RG4', '\RG{}') >>, the \CG{} strong-emission fraction differs by \(<< comp.get('strong_emission_fraction_difference')|fmt(3) >>\) (<< pholm_text(comp.get('p_adj')) >>).
<% endif %>
<% endfor %>
These limited \(\mathrm{H}\alpha\) results indicate that \CG{} galaxies have a lower strong-emission fraction than in each control sample in the current catalogue, but they do not provide a direct post-starburst or recently-quenched classification.
<% endif %>

The \(\mathrm{H}\alpha\) equivalent-width distributions are shown in Appendix Fig.~\ref{fig:recent_quenching}.
<% endif %>

<% set agn = es.get('agn_environment', {}) %>
<% if agn.get('status') in ['ok', 'limited'] %>
\subsection{Emission-line and AGN-like populations}\label{sec:agn_env}

The complementary emission-line analysis classifies BPT star-forming, composite, and AGN-like populations for galaxies with the required diagnostic line ratios: composite galaxies lie between the \citet{Kauffmann+03a} and \citet{Kewley+01} demarcations in the [N\,II] BPT plane, and AGN-like galaxies lie above the \citet{Kewley+01} line; galaxies with a pre-existing AGN flag but no BPT line ratios are also counted as AGN-like. BPT-unclassified galaxies are excluded from the AGN-like fraction denominator rather than treated as confirmed non-AGN. Among BPT-classified \CG{} galaxies, the AGN-like fraction is << agn.get('fractions_by_sample', {}).get('CG4', {}).get('agn_like_fraction')|pct(1) >>\%.

<% set agn_model = agn.get('mass_adjusted_models', {}).get('all', {}) %>
<% if agn_model.get('status') == 'ok' %>
After mass and environment adjustment, the \CG{} AGN-like odds ratio is << agn_model.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(agn_model.get('cg4_p')) >>).
<% if agn_model.get('cg4_p') is not none and agn_model.get('cg4_p') < 0.05 %>
Thus, AGN-like activity remains associated with compact-group membership after adjustment.
<% else %>
Thus, AGN-like activity does not provide a robust independent compact-group discriminator in this analysis.
<% endif %>
<% endif %>

The AGN-like fractions are shown in Appendix Fig.~\ref{fig:agn_fraction}.
<% endif %>

<% set tidal = es.get('tidal_indices', {}) %>
<% if tidal.get('status') == 'ok' %>
\subsection{Pairwise tidal index}\label{sec:tidal_index}

The pairwise analysis reconstructs projected separations and velocity differences for << tidal.get('n_galaxies_with_pairs') >> galaxies and refits the adjusted models with and without the pairwise tidal index \(\sum_j M_{\star,j}/R_{ij}^3\) (in \(\msun\,\mathrm{kpc}^{-3}\)). The two model variants estimate \emph{different quantities}, and we state both estimands explicitly. The model without the index estimates the \emph{total} association between compact-group membership and the outcome at fixed stellar mass, redshift, rank, and group-scale covariates. The model with the index estimates the association \emph{at fixed projected local density}. Because the index is built from \(R^{-3}\)-weighted projected separations, it is mathematically entangled with the compact-group selection itself (compact groups are selected to have small projected separations); conditioning on it therefore partially conditions on the exposure. A reduction of the \CG{} coefficient when the index is added is thus \emph{conditional attenuation}, not mediation: it does not show that local density accounts for the morphology contrast, and it cannot, because the causal ordering of selection, density, and morphology is not identified by projected snapshots. The baseline models in this subsection are refit on the pairwise-analysis complete-case sample, so their coefficients need not be numerically identical to the corresponding coefficients in Sect.~\ref{sec:adjusted_galaxy_properties}. Because \(R_{ij}\) is projected, the term is a local projected-density and immediate-neighbour proxy rather than a direct tidal-history measurement.

<% set tidal_quenched = tidal.get('models', {}).get('quenched', {}) %>
<% set quenched_base = tidal_quenched.get('baseline', {}) %>
<% set quenched_tidal = tidal_quenched.get('with_tidal_index', {}) %>
<% if quenched_base.get('status') == 'ok' and quenched_tidal.get('status') == 'ok' %>
For quenched status, the baseline \CG{} odds ratio is << quenched_base.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(quenched_base.get('cg4_p')) >>), while the model including the tidal index gives a \CG{} odds ratio of << quenched_tidal.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(quenched_tidal.get('cg4_p')) >>). The tidal-index term itself has odds ratio << quenched_tidal.get('terms', {}).get('log_tidal_index', {}).get('odds_ratio')|fmt(2) >> (<< pval_text(quenched_tidal.get('terms', {}).get('log_tidal_index', {}).get('p')) >>).
<% endif %>

<% set tidal_ell = tidal.get('models', {}).get('elliptical', {}) %>
<% set ell_base = tidal_ell.get('baseline', {}) %>
<% set ell_tidal = tidal_ell.get('with_tidal_index', {}) %>
<% if ell_base.get('status') == 'ok' and ell_tidal.get('status') == 'ok' %>
For elliptical morphology, the baseline \CG{} odds ratio is << ell_base.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(ell_base.get('cg4_p')) >>), whereas the model including the tidal index gives a \CG{} odds ratio of << ell_tidal.get('cg4_odds_ratio')|fmt(2) >> (<< pval_text(ell_tidal.get('cg4_p')) >>). This baseline is the pairwise complete-case refit, not the full adjusted model in Sect.~\ref{sec:adjusted_galaxy_properties}. The tidal-index term has odds ratio << ell_tidal.get('terms', {}).get('log_tidal_index', {}).get('odds_ratio')|fmt(2) >> (<< pval_text(ell_tidal.get('terms', {}).get('log_tidal_index', {}).get('p')) >>). The projected tidal-density proxy therefore \emph{attenuates} the \CG{} coefficient, consistent with a morphology contrast concentrated in the most extreme local projected configurations; given the selection entanglement described above, this conditional attenuation must not be read as the index accounting for the effect, and the result remains an exploratory projected-pair diagnostic.
<% endif %>

<% if tidal.get('figure') %>
\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< SUBFIGURES_PATH >><< tidal.get('figure') >>}
    \caption{Quenched status and elliptical morphology as functions of the reconstructed pairwise tidal index. Colours distinguish \CG{} from control galaxies.}
    \label{fig:tidal_index_outcomes}
\end{figure*}
<% endif %>
<% endif %>

<% if sz.get('status') == 'ok' %>
\subsection{Galaxy sizes at fixed stellar mass}\label{sec:size_results}

<% set size_avail = sz.get('availability_audit', {}) %>
<% set size_adj = sz.get('adjusted', {}) %>
<% set size_adj_all = size_adj.get('all', {}) %>
<% set size_adj_sat = size_adj.get('satellites', {}) %>
<% set size_adj_bgg = size_adj.get('bgg', {}) %>
<% set size_matched = sz.get('matched', {}) %>
<% set size_m_simard = size_matched.get('effects', {}).get('delta_log_Rchl_r', {}) %>
<% set size_m_petro = size_matched.get('effects', {}).get('delta_log_petroR50', {}) %>
<% set size_m_conc = size_matched.get('effects', {}).get('delta_concentration', {}) %>
<% set size_crowd = sz.get('crowding', {}) %>
<% set size_crowd_excl = size_crowd.get('excluding_close', {}).get('satellites', {}) %>
<% set size_crowd_flag = size_crowd.get('with_close_flag', {}).get('satellites', {}) %>
<% set size_petro_all = sz.get('petrosian', {}).get('all', {}) %>
<% set size_petro_sat = sz.get('petrosian', {}).get('satellites', {}) %>
<% set size_md = sz.get('measure_delta', {}) %>
<% set size_tidal = sz.get('tidal', {}) %>
<% set size_radial_cg = sz.get('radial', {}).get('correlations', {}).get('CG4', {}) %>
<% set size_radial_ctrl = sz.get('radial', {}).get('correlations', {}).get('pooled_controls', {}) %>
<% set size_conc_sat = sz.get('concentration', {}).get('satellites', {}) %>
<% set size_strata = sz.get('morphology_strata', {}) %>
<% set size_percontrol = sz.get('per_control', {}).get('comparisons', {}) %>

We now test whether \CG{} galaxies differ in size at fixed stellar mass, using the seeing-corrected Simard half-light radii as the primary measure and the Petrosian radii as the model-independent cross-check (Sect.~\ref{sec:size_data}). The Simard measure resolves << size_avail.get('cg4_simard_fraction')|pct(1) >>\% of the \CG{} galaxies and << size_avail.get('pooled_control_simard_fraction')|pct(1) >>\% of the pooled control galaxies, while the Petrosian radius is essentially complete in every sample (Appendix Fig.~\ref{fig:size_availability}). <% if size_avail.get('completeness_caveat') %>Because the \CG{}--control difference in Simard completeness exceeds five percentage points, every Simard-based coefficient below carries a differential-completeness caveat and is read together with the Petrosian rerun, which does not share this incompleteness.<% else %>The \CG{} and pooled-control Simard completeness agree to within five percentage points, so differential incompleteness is not a first-order concern for the coefficients below.<% endif %> The descriptive mass--size relations and the per-sample sizes predicted at \(\log(M_\star/M_\odot) = << sz.get('mass_size', {}).get('reference_logMstar')|fmt(1) >>\) are shown in Appendix Fig.~\ref{fig:size_mass_relation}.

<% if size_adj_sat.get('status') == 'ok' %>
For satellite galaxy size, the \CG{} offset at fixed stellar mass, redshift, and available group-scale covariates is \(<< size_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex (\(<< size_adj_sat.get('pct_equivalent')|fmt(1) >>\)\%), with 95\% confidence interval \([<< size_adj_sat.get('ci_low')|fmt(3) >>, << size_adj_sat.get('ci_high')|fmt(3) >>]\) and << pholm_text(size_adj_sat.get('p_holm')) >>, over << size_adj_sat.get('n') >> satellites in << size_adj_sat.get('n_groups') >> physical groups.
<% endif %>
<% if size_adj_all.get('status') == 'ok' %>
The all-galaxy model gives \(<< size_adj_all.get('cg4_delta_dex')|fmt(3) >>\) dex (95\% CI \([<< size_adj_all.get('ci_low')|fmt(3) >>, << size_adj_all.get('ci_high')|fmt(3) >>]\); << pholm_text(size_adj_all.get('p_holm')) >>)<% if size_adj_bgg.get('status') == 'ok' %>, and the BGG-only model gives \(<< size_adj_bgg.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_adj_bgg.get('p_holm')) >>)<% endif %>.
<% endif %>
<% if szv.get('primary_all_significant') or szv.get('primary_satellites_significant') %>
The primary family therefore indicates a significant \CG{} size offset: at fixed stellar mass, \CG{} galaxies are << szv.get('direction') >> than their regular-group counterparts.
<% else %>
No coefficient in this primary family remains significant after Holm correction, so the pooled adjusted models do not establish a \CG{} size offset at fixed stellar mass.
<% endif %>
The per-control versions of the mass- and redshift-adjusted fit are descriptive and shown in Appendix Fig.~\ref{fig:size_forest_per_control}; the fitted \CG{} coefficient is <% for control_key, control_label in [('Control4B', '\\CB{}'), ('Control4C', '\\CC{}'), ('RG4', '\\RG{}')] %><% set control_fit = size_percontrol.get(control_key, {}) %><% if control_fit.get('status') == 'ok' %>\(<< control_fit.get('cg4_delta_dex')|fmt(3) >>\) dex against << control_label >><% if not loop.last %>, <% else %> (raw p-values with Benjamini--Hochberg control inside this figure family)<% endif %><% endif %><% endfor %>. The sign and significance of the \CG{} coefficient depend on the adopted control, so we do not interpret any single per-control contrast on its own.

<% if size_matched.get('status') == 'ok' %>
The one-to-one matched comparison re-derives the same << size_matched.get('n_pairs') >> propensity pairs as the matched-control analysis, with a separate Holm family for the three size outcomes so that the published matched-outcome family is untouched. <% if size_m_simard.get('status') == 'ok' %>On the << size_m_simard.get('n_pairs_with_sizes') >> pairs in which both members have valid Simard sizes, the mean \CG{}--minus--control difference in \(\log_{10} R_{\rm chl,r}\) is \(<< size_m_simard.get('mean_delta')|fmt(3) >>\) dex (95\% bootstrap interval \([<< size_m_simard.get('mean_ci95')[0]|fmt(3) >>, << size_m_simard.get('mean_ci95')[1]|fmt(3) >>]\); << pholm_text(size_m_simard.get('p_holm')) >>).<% endif %> <% if size_m_petro.get('status') == 'ok' %>The corresponding Petrosian difference is \(<< size_m_petro.get('mean_delta')|fmt(3) >>\) dex (<< pholm_text(size_m_petro.get('p_holm')) >>)<% endif %><% if size_m_conc.get('status') == 'ok' %>, and the matched concentration difference is \(<< size_m_conc.get('mean_delta')|fmt(3) >>\) (<< pholm_text(size_m_conc.get('p_holm')) >>)<% endif %>.
<% if szv.get('survives_matching') and not szv.get('primary_satellites_significant') %>
The matched pairs therefore prefer << szv.get('direction') >> \CG{} sizes even though the pooled adjusted coefficient is consistent with zero. The matched control set is dominated by the sample whose per-control fit carries the same sign, so we read this as a control-dependent contrast rather than as an independent confirmation of a global offset.
<% endif %>
<% endif %>

<% if size_crowd_excl.get('status') == 'ok' and size_crowd_flag.get('status') == 'ok' %>
The crowding checks mirror Sect.~\ref{sec:morph_crowding}: excluding galaxies with a projected neighbour within << size_crowd.get('threshold_arcsec')|fmt(0) >> arcsec leaves a satellite coefficient of \(<< size_crowd_excl.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pval_text(size_crowd_excl.get('p')) >>), and adding the close-neighbour flag as a covariate gives \(<< size_crowd_flag.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pval_text(size_crowd_flag.get('p')) >>).
<% endif %>

<% if size_petro_sat.get('status') == 'ok' %>
The Petrosian rerun, with the field seeing as an additional standardized covariate, gives a satellite coefficient of \(<< size_petro_sat.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_petro_sat.get('p_holm')) >>)<% if size_petro_all.get('status') == 'ok' %> and an all-galaxy coefficient of \(<< size_petro_all.get('cg4_delta_dex')|fmt(3) >>\) dex (<< pholm_text(size_petro_all.get('p_holm')) >>)<% endif %>.
<% endif %>
<% if size_md.get('status') == 'ok' %>
The per-galaxy difference between the two measures, \(d = \log R_{\rm chl,r} - \log R_{50,r}\), averages \(<< size_md.get('mean_delta_dex')|fmt(3) >>\) dex<% if size_md.get('mean_by_morphology', {}).get('elliptical') and size_md.get('mean_by_morphology', {}).get('spiral') %>, and is larger for elliptical galaxies (\(<< size_md.get('mean_by_morphology', {}).get('elliptical', {}).get('mean')|fmt(3) >>\) dex) than for spiral galaxies (\(<< size_md.get('mean_by_morphology', {}).get('spiral', {}).get('mean')|fmt(3) >>\) dex), as expected if the Petrosian aperture preferentially truncates concentrated profiles<% endif %>. <% if size_md.get('spearman_vs_neighbour_separation') %>Its rank correlation with the nearest-neighbour separation is \(\rho_\mathrm{S} = << (size_md.get('spearman_vs_neighbour_separation', {}).get('rho', 0) + 0)|fmt(3) >>\) (<< pval_text(size_md.get('spearman_vs_neighbour_separation', {}).get('p')) >>), so residual blending does not drive a strong measure-dependent size systematic (Appendix Fig.~\ref{fig:size_measure_delta}).<% endif %>
<% endif %>

<% if size_tidal.get('status') == 'ok' and size_tidal.get('baseline', {}).get('status') == 'ok' and size_tidal.get('with_tidal_index', {}).get('status') == 'ok' %>
Adding the pairwise projected tidal index of Sect.~\ref{sec:tidal_index} to the all-galaxy size model changes the \CG{} coefficient from \(<< size_tidal.get('baseline', {}).get('cg4_delta_dex')|fmt(3) >>\) dex to \(<< size_tidal.get('with_tidal_index', {}).get('cg4_delta_dex')|fmt(3) >>\) dex on the pairwise complete-case sample<% if szv.get('absorbed_by_tidal_index') %>; the local projected-density proxy absorbs the size coefficient in the same way it absorbs the morphology coefficient<% else %>; with a near-null baseline coefficient, the attenuation diagnostic used for morphology is not informative here<% endif %>.
<% endif %>

<% if size_radial_cg.get('status') == 'ok' and size_radial_ctrl.get('status') == 'ok' %>
The radial diagnostic is exploratory: mass- and redshift-adjusted satellite size residuals correlate with projected BGG-centric distance with \(\rho_\mathrm{S} = << size_radial_cg.get('spearman_rho')|fmt(2) >>\) (<< pval_text(size_radial_cg.get('spearman_p')) >>) in \CG{} and \(\rho_\mathrm{S} = << size_radial_ctrl.get('spearman_rho')|fmt(2) >>\) (<< pval_text(size_radial_ctrl.get('spearman_p')) >>) in the pooled controls (Appendix Fig.~\ref{fig:size_radial}). A more negative inner-region residual would be the tidal-truncation expectation; the observed trends are similar in \CG{} and the controls, so the data do not isolate a \CG{}-specific inner-region size suppression.
<% endif %>

<% if size_conc_sat.get('status') == 'ok' %>
The concentration index \(C = R_{90,r}/R_{50,r}\) provides a model-independent structural proxy: the adjusted satellite coefficient is \(<< size_conc_sat.get('cg4_delta')|fmt(3) >>\) (<< pholm_text(size_conc_sat.get('p_holm')) >>)<% if size_conc_sat.get('p_holm') is not none and size_conc_sat.get('p_holm') < 0.05 %>, a significant concentration difference at fixed mass<% else %>, so \CG{} membership does not significantly shift the concentration index at fixed mass<% endif %>.
<% endif %>

<% set size_strata_ell = size_strata.get('elliptical_satellites', {}) %>
<% set size_strata_spi = size_strata.get('spiral_satellites', {}) %>
<% if size_strata_ell.get('status') == 'ok' and size_strata_spi.get('status') == 'ok' %>
Within morphology classes, the satellite-only fits give \(<< size_strata_ell.get('cg4_delta_dex')|fmt(3) >>\) dex for elliptical satellites (<< pholm_text(size_strata_ell.get('p_holm')) >>) and \(<< size_strata_spi.get('cg4_delta_dex')|fmt(3) >>\) dex for spiral satellites (<< pholm_text(size_strata_spi.get('p_holm')) >>)<% set size_strata_int = size_strata.get('interaction', {}).get('interaction_term') %><% if size_strata_int %>, and the \CG{}\(\times\)morphology interaction is \(<< size_strata_int.get('coefficient')|fmt(3) >>\) dex (<< pval_text(size_strata_int.get('p')) >>)<% endif %>. These strata are secondary diagnostics; they test whether any size offset is independent information beyond the morphology signal rather than a restatement of it.
<% endif %>
<% endif %>

<% set selection = es.get('selection_diagnostics', {}) %>
<% if selection.get('status') == 'ok' %>
\subsection{Selection diagnostics}\label{sec:selection_diagnostics}

The fractions with complete measurements for the major derived quantities are shown in Appendix Fig.~\ref{fig:data_availability}. The colour-matched versus unmatched comparisons quantify shifts in stellar mass, redshift, rank, star-formation class and morphology.

<% set angular = selection.get('nearest_angular_separation', {}) %>
<% if angular.get('status') == 'ok' %>
The median nearest-neighbour angular separation is << angular.get('median_cg4')|fmt(1) >> arcsec in \CG{} and << angular.get('median_control')|fmt(1) >> arcsec in the controls; the fractions below the 55-arcsec SDSS fibre-collision scale are << angular.get('cg4_fraction_below_threshold')|pct(1) >>\% and << angular.get('control_fraction_below_threshold')|pct(1) >>\%, respectively.
<% endif %>

<% if selection.get('strong_selection_bias') %>
Under the availability definitions shown in Fig.~\ref{fig:data_availability}, at least one major availability difference is both statistically significant and larger than 10 percentage points, so affected results are treated as exploratory.
<% else %>
Under the availability definitions shown in Fig.~\ref{fig:data_availability}, no major availability difference meets both criteria of corrected significance and a difference exceeding 10 percentage points.
<% endif %>

<% set avail = selection.get('availability_by_sample', {}) %>
<% set group_audit = selection.get('group_scale_column_audit', {}) %>
<% set missing_group_cols = group_audit.get('missing_or_sparse_columns', []) %>
<% if 'log_group_mass' in missing_group_cols %>
The group-scale availability row is based on the group covariates available for this catalogue: radius scale, velocity dispersion, group luminosity, and BGG luminosity dominance. A harmonized group-mass estimate is not available in these tables, so models that request it omit that covariate.
<% elif avail and avail.get('CG4', {}).get('group_scale_quantities') == 0 %>
The automated availability row for group-scale quantities should be interpreted as an alias audit for a specific set of column names rather than as evidence that no group-scale information is used elsewhere in the analysis.
<% endif %>

The colour-selection mass audit is shown in Appendix Fig.~\ref{fig:colour_selection_bias}.
<% endif %>

\begin{table*}
\centering
\caption{Result hierarchy. Baseline tests describe where \CG{} differs from the controls; the main interpretation is based on diagnostics that remain after adjusted or matched comparisons.}
\label{tab:result_hierarchy}
\begin{tabular}{p{0.22\textwidth}p{0.23\textwidth}p{0.18\textwidth}p{0.28\textwidth}}
\hline\hline
Diagnostic & Baseline \CG{} signal & Survives adjusted/matched analysis? & Interpretation \\
\hline
Elliptical-vote fraction & Enhanced in \CG{} & Yes & Robust accumulated morphology signal. \\
Spiral-vote fraction & Reduced in \CG{} & Yes & Reduced late-type vote-fraction population. \\
Galaxy Zoo crowding check & Elliptical excess persists after close-neighbour exclusion & Yes, as a limited robustness check & Morphology result not solely produced by the closest projected pairs. \\
Satellite star-forming fraction & Reduced, especially versus \RG{} & Weak or borderline & Partly explained by mass, rank, environment, or preprocessing. \\
Star-forming main-sequence residual & Weak and control-dependent & No robust offset & No strong instantaneous SFR enhancement or suppression among remaining star-forming galaxies. \\
Satellite colours & Colour-slope and residual hints & Control-sensitive & Exploratory; not a simple uniform reddening or blueing of \CG{} satellites. \\
AGN-like fraction & No robust independent signal & No & Not an AGN-driven explanation. \\
Magnitude gaps & No significant excess & No & Not fossil-like under the simple gap diagnostics used here. \\
Pairwise tidal index & Local projected density proxy is informative & Reframes much of the morphology coefficient & Local projected density is linked to the morphology signal. \\
<% if sz.get('status') == 'ok' %>
Galaxy size at fixed mass & Control-dependent, near-zero offsets & <% if szv.get('primary_satellites_significant') and szv.get('survives_matching') %>Yes<% elif szv.get('survives_matching') %>Matched pairs only<% elif szv.get('primary_satellites_significant') %>Adjusted models only<% else %>No<% endif %> & <% if szv.get('primary_satellites_significant') and szv.get('survives_matching') %>Robust size offset at fixed stellar mass.<% elif szv.get('survives_matching') %>Control-dependent; no pooled adjusted offset.<% else %>No compact-group size effect at fixed stellar mass.<% endif %> \\
<% set hierarchy_conc = sz.get('concentration', {}).get('satellites', {}) %>
Concentration index & \(R_{90,r}/R_{50,r}\) structural proxy now measured & <% if hierarchy_conc.get('status') == 'ok' and hierarchy_conc.get('p_holm') is not none and hierarchy_conc.get('p_holm') < 0.05 %>Yes<% else %>No<% endif %> & <% if hierarchy_conc.get('status') == 'ok' and hierarchy_conc.get('p_holm') is not none and hierarchy_conc.get('p_holm') < 0.05 %>Concentration differs at fixed mass; independent structural signal.<% else %>No independent concentration signal at fixed mass.<% endif %> \\
<% endif %>
\hline
\end{tabular}
\end{table*}

<% endif %>

\section{Discussion: drivers of structural evolution in compact groups}
<% set discussion_es = extended_specialness if extended_specialness else {} %>
<% set discussion_tidal = discussion_es.get('tidal_indices', {}) %>
<% set discussion_tidal_ell = discussion_tidal.get('models', {}).get('elliptical', {}) %>
<% set discussion_ell_base = discussion_tidal_ell.get('baseline', {}) %>
<% set discussion_ell_tidal = discussion_tidal_ell.get('with_tidal_index', {}) %>
<% set discussion_tidal_term = discussion_ell_tidal.get('terms', {}).get('log_tidal_index', {}) %>

\subsection{Local projected tidal density and morphology signal}

The most robust residual difference between \CG{} and the regular-group controls is morphological. \CG{} galaxies have a higher Galaxy~Zoo~1 elliptical-vote fraction and a lower spiral-vote fraction. In the per-control adjusted models the excess is strong against \CB{} and \RG{} but not against \CC{}, and it is recovered at the group level in the matched comparison (higher elliptical-vote satellite fraction in matched \CG{} groups across all three per-control comparisons, though individually significant only against \RG{} and the pooled match), while the galaxy-level matched contrast is directionally consistent but not significant once the four galaxies of a group are resampled together. The within-host comparison of Sect.~\ref{sec:host_controlled} finds no significant residual excess of \CG{} members over other members of the same host under the primary stratified estimator. The result hierarchy therefore points to accumulated structural transformation in dense projected cores more strongly than to a present-day star-formation offset or to compact-group membership as such.

The pairwise tidal-index experiment strengthens this interpretation while also narrowing it. <% if discussion_ell_base.get('status') == 'ok' and discussion_ell_tidal.get('status') == 'ok' -%>For elliptical morphology, the pairwise complete-case baseline \CG{} odds ratio is \(<< discussion_ell_base.get('cg4_odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_ell_base.get('cg4_p')) >>), so it is not expected to match exactly the full adjusted-model odds ratio in Sect.~\ref{sec:adjusted_galaxy_properties}. After adding the projected pairwise tidal-index proxy, the \CG{} odds ratio decreases to \(<< discussion_ell_tidal.get('cg4_odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_ell_tidal.get('cg4_p')) >>), while the tidal-index term itself has odds ratio \(<< discussion_tidal_term.get('odds_ratio')|fmt(2) >>\) (<< pval_text(discussion_tidal_term.get('p')) >>).<% else -%>In the available models, adding the projected pairwise tidal-index proxy reduces the \CG{} morphology coefficient while the tidal-index term is itself informative.<% endif %> This behaviour is expected if compact groups are selecting galaxies in locally dense projected configurations where repeated close companions, rather than the catalogue label alone, are linked to the elliptical/early-type-like population.

This proxy uses projected separations, so it may be inflated by line-of-sight alignments and is partly entangled with compact-group selection itself: compact groups are selected to be compact in projection. We therefore read the tidal-index result as a continuous local projected-density or immediate-neighbour proxy, not as a direct time-dependent tidal-field measurement. The \CC{} comparison makes the test deliberately stringent, because \CC{} is also selected as a projected compact core around a BGG. The persistence of a \CG{}--\CC{} morphology contrast in the closest-neighbour exclusion test shows that the \CG{} label is not reduced to the simplest crowding artefact. At the same time, the tidal-index model indicates that a more continuous projected-density proxy absorbs much of the \CG{} coefficient. We therefore interpret the residual \CG{}--\CC{} contrast as consistent with \CG{} selecting a more extreme part of the local-density distribution, rather than as proof that compact groups define a separate causal channel.

<% if sz.get('status') == 'ok' %>
\subsection{Galaxy sizes and comparison with earlier compact-group size studies}

<% set disc_adj_sat = sz.get('adjusted', {}).get('satellites', {}) %>
<% set disc_m_simard = sz.get('matched', {}).get('effects', {}).get('delta_log_Rchl_r', {}) %>
\citet{Coenda+12} compared galaxies in the \citet{McConnachie+09} compact groups with loose-group and field samples using SDSS Petrosian half-light radii and found compact-group galaxies to be smaller at fixed luminosity, with the clearest offset for early types relative to the field. Their assessment rested on approximately 1\(\sigma\) comparisons of fitted size--luminosity relations, without a formal confidence level on the size difference itself. The present analysis re-poses that question with a different control design -- the non-split HMCG-based \CG{} sample against three regular-group controls, including the projected-compact-core analogue \CC{} -- and with seeing-corrected model sizes, a Petrosian cross-check, cluster-robust adjusted models, one-to-one matching, crowding tests, and Holm-corrected confidence intervals.

<% if szv.get('primary_satellites_significant') %>
Under this stricter design, the compact-group size deficit survives: at fixed stellar mass, \CG{} satellites are systematically << szv.get('direction') >> (\(<< disc_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex), in qualitative agreement with \citet{Coenda+12} but now against regular-group controls rather than the field.
<% elif szv.get('survives_matching') %>
Under this stricter design, the pooled adjusted models do not confirm a compact-group size offset at fixed stellar mass relative to regular groups: the satellite coefficient is \(<< disc_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex with a Holm-corrected confidence interval consistent with zero. The matched pairs do prefer << szv.get('direction') >> \CG{} sizes (\(<< disc_m_simard.get('mean_delta')|fmt(3) >>\) dex), but that contrast is control-dependent. The comparison with \citet{Coenda+12} is therefore nuanced rather than contradictory: their compact-group versus field and loose-group offset can coexist with a null compact-group versus regular-group-core result, because our controls are themselves dense group environments rather than the field.
<% else %>
Under this stricter design, we find no compact-group size offset at fixed stellar mass relative to regular groups. This does not contradict \citet{Coenda+12} directly, because their comparison was against loose groups and the field, whereas ours is against regular-group cores selected from the same parent catalogues.
<% endif %>

Three interpretation guardrails apply. A negative \(\Delta\) at fixed mass can reflect genuine tidal truncation or stripping, a higher bulge dominance in the \CG{} population, or residual measurement blending in crowded fields. The morphology-stratified fits address the second possibility, the Petrosian cross-check and the measure-difference diagnostic address the third, and the crowding exclusions bound the sensitivity of all size coefficients to the closest projected pairs. Any future claim of compact-group size evolution should be read through those three blocks together rather than through a single pooled coefficient.
<% endif %>

\subsection{Elliptical-vote morphologies, S0 degeneracy, and transition galaxies}

The Galaxy Zoo morphology variables used here identify galaxies with high elliptical vote fractions and galaxies with high combined-spiral vote fractions. They do not cleanly distinguish classical ellipticals from S0 galaxies, faded disks, or other bulge-dominated disk systems. The enhanced \CG{} elliptical fraction should therefore be interpreted as a shift toward early-type-like appearances, not as proof that compact groups specifically build classical elliptical galaxies.

This caveat is important when comparing with the recent S-PLUS structural studies of compact-group galaxies by \citet{Montaguth2023,Montaguth2025,Montaguth2026MassSize,Montaguth2026NonIsolated}. Those studies identify transition galaxies and trends in the \(R_{\rm e}\)--S\'ersic-index and mass-size planes, with especially strong signals in non-isolated compact groups and host structures. Their transition population can plausibly overlap with the elliptical-vote class in our catalogue: systems with faded disks, increasing central concentration, or S0-like structure may receive high elliptical vote fractions here even if they are not classical ellipticals. The two approaches are therefore complementary. Their structural measurements describe the likely physical form of the transformation, while our regular-group controls show that the strongest residual compact-group signal is the accumulated elliptical/spiral vote-fraction balance.<% if sz.get('status') == 'ok' %> With the Simard structural parameters now attached to this sample, the \(R_{\rm e}\)--S\'ersic-\(n\) plane is measured directly for \CG{} and its controls (Appendix Fig.~\ref{fig:size_re_n_plane}), turning this comparison with the S-PLUS transition-galaxy studies from a qualitative statement into a direct one.<% endif %>

\subsection{Decoupling between morphology and instantaneous star formation}\label{sec:decoupling}

The star-formation diagnostics do not follow the morphology signal one-to-one. \CG{} satellites have a lower raw star-forming fraction, especially relative to \RG, but the adjusted evidence is control-dependent (significant only against \CB) and the matched contrasts provide no evidence for a quenched-fraction difference. Among galaxies that remain star-forming, the main-sequence residuals do not show a robust compact-group offset. The colour analysis is also control-sensitive and based on a biased colour-matched subset, while AGN-like fractions, magnitude gaps, and the limited \(\mathrm{H}\alpha\) diagnostic do not identify a simple alternative driver.

<% set discussion_selection = discussion_es.get('selection_diagnostics', {}) %>
<% set discussion_angular = discussion_selection.get('nearest_angular_separation', {}) %>
<% set discussion_crowd = discussion_es.get('morphology_robustness', {}) %>
<% set discussion_crowd_rows = discussion_crowd.get('fractions_after_excluding_close', []) %>
<% set discussion_c4c_crowd_list = discussion_crowd_rows|selectattr('sample', 'equalto', 'Control4C')|list %>
<% set discussion_c4c_crowd = discussion_c4c_crowd_list|first if discussion_c4c_crowd_list|length > 0 else {} %>
The star-formation non-detections are also limited by the spectroscopy. <% if discussion_angular.get('status') == 'ok' %>\CG{} has a higher fraction of galaxies with a nearest projected neighbour below the 55-arcsec SDSS fibre-collision scale than the controls (<< discussion_angular.get('cg4_fraction_below_threshold')|pct(1) >>\% versus << discussion_angular.get('control_fraction_below_threshold')|pct(1) >>\%). <% endif %><% if discussion_c4c_crowd.get('close_fraction') is not none %>The control value is pooled over the regular-control comparison; the \CC{}-only exclusion fraction in Sect.~\ref{sec:morph_crowding} is larger (<< discussion_c4c_crowd.get('close_fraction')|pct(1) >>\%) because \CC{} is selected as the compact-core analogue. <% endif %>Spectra are therefore preferentially incomplete in the closest projected pairs, and short-lived interaction-triggered starbursts or post-starburst phases could be underrepresented. The MPA-JHU aperture corrections use photometric information outside the fibre \citep{Brinchmann+04,Kauffmann+03a}; these corrections may be less secure for interacting or blended compact-group members. We therefore treat the sSFR, SFMS, and \(\mathrm{H}\alpha\) results as conservative secondary non-detections, not as proof that compact groups have no star-formation effect.

This decoupling suggests that the dominant observable difference is not an instantaneous quenching event caught at the present epoch. A galaxy can lose visible spiral structure, build a more bulge-dominated profile, or fade into an S0-like state on timescales that do not require a simultaneously large sSFR residual in the surviving star-forming subset. The present data therefore favour a cumulative morphology or structure signal over a single ongoing star-formation process.

\subsection{Compact groups as dense cores and preprocessing sites}

The comparison samples were designed to ask whether compact groups remain special relative to ordinary group cores, not merely relative to the field. The answer is mixed but physically useful. \CG{} galaxies retain a robust morphology difference, yet much of the interpretation points toward local density, dense cores, and preprocessing rather than toward an isolated compact-group channel. This is consistent with the construction result that many compact groups are embedded in or associated with larger structures, and with the stronger structural signatures reported for non-isolated compact groups in recent S-PLUS work. We note that no harmonised halo-mass estimate is available for these catalogues (Sect.~\ref{sec:selection_diagnostics}), so, although the adjusted models control for group luminosity and velocity dispersion, a residual halo-mass contribution to the morphology signal cannot be fully excluded.

The most conservative interpretation is that compact groups identify the high-density, high-interaction tail of group environments. Some systems may be genuinely compact and dynamically evolved, while others may be dense cores or substructures inside larger haloes. In both cases, the relevant physical quantity is likely the accumulated local interaction history. Future simulations and direct structural measurements, including \(R_{\rm e}\), S\'ersic index, transition-galaxy classifications, and mass-size residuals, are needed to test whether the signal isolated here is unique to compact configurations or is the high-density limit of more general group preprocessing.

\section{Conclusion}

We have tested whether galaxies in \CG{} compact groups remain special after removing split systems and comparing them with three regular-group analogues: \RG, \CB, and \CC. The comparison was designed to separate compact configurations from ordinary four-member groups, luminous group cores, and projected compact cores around BGGs.

The main result is morphological, and it is control-dependent in an informative way. \CG{} galaxies have an enhanced Galaxy~Zoo~1 elliptical-vote fraction and a reduced spiral-vote fraction. In the per-control adjusted models this signal is strong against the luminous cores of richer ordinary groups (\CB) and against true four-member groups (\RG), but weak and non-significant against BGG-centred projected cores (\CC). At the group level, matched \CG{} groups host a higher elliptical-vote satellite fraction than matched control groups (positive differences against all three per-control comparisons); the galaxy-level matched contrast points in the same direction but is not significant after correction once the four galaxies of a group are treated as one resampling unit. The signal is robust to the Galaxy~Zoo~1 vote threshold, is recovered with a threshold-free vote-fraction model and a S\'ersic-index cross-check, and is primarily carried by satellites; BGG morphology is not significantly different after adjustment. Within shared host groups, \CG{} members show no significant morphology excess over other members of the same host at fixed mass, rank, and host-centric radius under the primary stratified estimator.

The star-formation result is weaker. \CG{} satellites have a lower raw star-forming fraction, especially relative to \RG, but the adjusted evidence is control-dependent (significant only against \CB) and the matched contrasts provide no evidence for a quenched-fraction difference. Among galaxies that remain star-forming, \CG{} galaxies do not show a robust offset from the fitted star-forming main sequence.

<% if sz.get('status') == 'ok' %>
<% set conclusion_adj_sat = sz.get('adjusted', {}).get('satellites', {}) %>
<% set conclusion_m_simard = sz.get('matched', {}).get('effects', {}).get('delta_log_Rchl_r', {}) %>
The galaxy-size test adds a structural dimension to this comparison. <% if szv.get('primary_satellites_significant') %>At fixed stellar mass, redshift, and group-scale covariates, \CG{} satellites are systematically << szv.get('direction') >> in seeing-corrected half-light radius (\(<< conclusion_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex; Holm-corrected), a size offset that complements the morphology signal.<% elif szv.get('survives_matching') %>At fixed stellar mass, redshift, and group-scale covariates, the pooled adjusted models show no significant \CG{} offset in seeing-corrected half-light radius (satellite coefficient \(<< conclusion_adj_sat.get('cg4_delta_dex')|fmt(3) >>\) dex). The one-to-one matched pairs formally prefer << szv.get('direction') >> \CG{} sizes (\(<< conclusion_m_simard.get('mean_delta')|fmt(3) >>\) dex), but the contrast is control-dependent, so we do not claim a robust compact-group size effect.<% else %>At fixed stellar mass, redshift, and group-scale covariates, we find no significant \CG{} offset in seeing-corrected half-light radius, in either the adjusted models or the matched pairs.<% endif %>
<% endif %>

The secondary diagnostics do not identify a simple alternative driver. The colour results are control-sensitive and based on a biased colour-matched subset. AGN-like fractions and magnitude gaps do not explain the morphology signal, the \(\mathrm{H}\alpha\) diagnostic is limited by the absence of age-sensitive spectral indices from our retrieved catalogues, and projected phase-space trends do not localize the quenched difference uniquely to the nearest satellites. The Galaxy Zoo crowding check shows that the elliptical excess persists after excluding the closest projected pairs, so the result is not solely a closest-pair classification artefact.

We therefore interpret \CG{} galaxies as a more morphologically evolved population, consistent with accumulated transformation in dense group cores and/or preprocessing rather than with compact-group membership as such: the excess is no longer statistically detected against projected-core controls or within shared hosts (both point estimates remain positive), and conditioning on a local projected-density proxy attenuates the coefficient (a conditional attenuation that, by construction, cannot establish mediation). The result should not be read as evidence for a uniquely isolated compact-group quenching mechanism.

This paper now connects the regular-group control framework directly to structural measurements -- \(R_{\rm e}\), S\'ersic index, and mass-size residuals through the Simard catalogue -- so the remaining structural future work concerns transition-galaxy classifications and better spectroscopic post-starburst diagnostics. <% if sz.get('status') == 'ok' %><% set final_conc_sat = sz.get('concentration', {}).get('satellites', {}) %>The concentration-index check based on \(R_{90,r}/R_{50,r}\) is likewise measured here<% if final_conc_sat.get('status') == 'ok' %>: the adjusted satellite coefficient is \(<< final_conc_sat.get('cg4_delta')|fmt(3) >>\) (<< pholm_text(final_conc_sat.get('p_holm')) >>), so <% if final_conc_sat.get('p_holm') is not none and final_conc_sat.get('p_holm') < 0.05 %>concentration provides an independent structural signal at fixed mass<% else %>this independent structural proxy shows no significant \CG{} offset at fixed mass<% endif %><% endif %>.<% endif %> Explicit E--S0--spiral and transition-galaxy classifications, combined with bulge--disc structure and age-sensitive stellar-population diagnostics, will be needed to determine whether the elliptical-vote satellite excess reflects disk fading, tidal heating, or merger-driven transformation in dense group cores.

\section*{Data availability}

The input data are drawn from public SDSS spectroscopic and photometric tables, the MPA-JHU
value-added catalogues, Galaxy Zoo classifications, the \citet{Simard2011} structural
catalogue (VizieR \texttt{J/ApJS/196/11}), and the public group and compact-group catalogues cited
in Sect.~2. The derived galaxy and group tables from this analysis will be made available
through the CDS at publication. The full analysis and paper-generation code is available at
\url{https://github.com/MatthieuTricottet/CG_Gals}; the release corresponding to this
article will be archived with a DOI (e.g. Zenodo) at acceptance. % TODO(MT): mint DOI.

\begin{acknowledgements}
% TODO(MT): personal thanks (colleagues, informal readers).
% TODO(MT): funding — MT: ...; GAM: ...; EDG acknowledges support from CONICET and
% SECyT--UNC, Argentina (grant nos. TODO).
% TODO(MT): paste the official SDSS-IV/DR16 funding acknowledgement verbatim from
% https://www.sdss4.org/collaboration/citing-sdss/ (required by SDSS data policy).
This publication uses data generated via the Galaxy Zoo project; we thank the many
volunteers whose classifications made this analysis possible. This work made use of the
MPA-JHU value-added spectroscopic catalogues, and of the \citet{Simard2011} structural
catalogue obtained through the VizieR catalogue access tool (CDS, Strasbourg; catalogue
\texttt{J/ApJS/196/11}). This research made use of Astropy \citep{Astropy2022}, Astroquery
\citep{Ginsburg2019}, NumPy \citep{Harris2020}, SciPy \citep{Virtanen2020}, pandas
\citep{McKinney2010}, Matplotlib \citep{Hunter2007}, statsmodels \citep{Seabold2010}, and
scikit-learn \citep{Pedregosa2011}.
\end{acknowledgements}

\bibliographystyle{aa}
\bibliography{<<BIB_FILE>>}
\clearpage
\begin{appendix}

\section{SDSS data query}\label{app:sdss_query}

The SQL query used to retrieve the spectral and photometric quantities from SDSS is listed below.

\begin{lstlisting}[caption={Query used for selecting spectral \& photometric data from the SDSS},label={lst:query}]
<% for line in SDSS_query.strip('\n').split('\n') %><< line.rstrip() >>
<% endfor %>
\end{lstlisting}

\clearpage

\section{sSFR classification details}\label{app:ssfr_method}

We identify BPT-classifiable galaxies by requiring measured values of both emission-line ratios
\[
\log_{10}\!\bigl([\mathrm{N\,II}]\lambda6584/\mathrm{H}\alpha\bigr)
\quad\text{and}\quad
\log_{10}\!\bigl([\mathrm{O\,III}]\lambda5007/\mathrm{H}\beta\bigr).
\]
Among these galaxies, we flag AGN-like systems if they satisfy either
\[
\log_{10}\frac{[\mathrm{N\,II}]}{\mathrm{H}\alpha} > 0,
\]
or if they lie above the empirical demarcation of \citet{Kauffmann+03a}:
\[
\log_{10}\frac{[\mathrm{O\,III}]}{\mathrm{H}\beta}
> \frac{0.61}{\log_{10}([\mathrm{N\,II}]/\mathrm{H}\alpha)-0.05} \;+\; 1.3,
\]
in which case they are excluded from the GMM calibration sample. This calibration flag is deliberately more inclusive than the three-class BPT scheme of Sect.~\ref{sec:agn_env}, in which composite galaxies lie between the \citet{Kauffmann+03a} and \citet{Kewley+01} demarcations and only galaxies above the \citet{Kewley+01} line are labelled AGN-like, because its sole purpose is to keep possible AGN contamination out of the sSFR calibration. Galaxies with missing diagnostic line ratios are BPT-unclassified, not proven non-AGN. They are retained for the broad sSFR status assignment when valid SDSS stellar mass and sSFR estimates are available, while the emission-line/AGN-like analysis treats BPT-unclassified objects separately. The BPT-classifiable subset is shown in Fig.~\ref{fig:BPT}.

Galaxies with \texttt{specsfr\_tot\_p50} equal to \(-9999\) in the SDSS carry the MPA-JHU missing-value sentinel and have no valid sSFR estimate;
they are treated as missing data throughout: excluded from the GMM calibration, never assigned an sSFR class,
omitted from every sSFR figure, and reported as counts only (Sect.~\ref{sec:ssfr_classification}).
Galaxies with a measured sSFR are divided into quenched and star-forming classes with a two-component Gaussian Mixture Model (GMM) in the \(\log M_\star\)-\(\log\mathrm{sSFR}\) plane \citep{McLachlanPeel2000}. The calibration excludes galaxies flagged as AGN-like where the required BPT line ratios are available, while BPT-unclassified galaxies with valid SDSS stellar mass and sSFR estimates can still receive a broad sSFR class. The quenched/star-forming boundary is the locus where the two component posterior probabilities are equal. A fixed-threshold robustness check confirms that the qualitative hierarchy is not tied to the exact GMM split.

\begin{center}
     \includegraphics[width=\columnwidth]{BPT_diagram.pdf}
     \refstepcounter{figure}\label{fig:BPT}
     \begin{minipage}{\columnwidth}
     \small\textbf{Fig.~\thefigure.} BPT diagram for galaxies with measured diagnostic line ratios. The AGN-like region is excluded from the sSFR calibration sample; galaxies without the required ratios are BPT-unclassified rather than classified as non-AGN by this diagram.
     \end{minipage}
\end{center}

\clearpage

<% set overlap = r.get('cg4_pc_quartet_overlap', {}) %>
<% if overlap %>
\section{Compact groups as projected cores of ordinary groups}\label{app:cg_core_overlap}

The compact-group exclusion of Sect.~\ref{sec:samples} is not a technicality: for << overlap.get('n_lim_groups') >> of the 765 parent Lim groups, the would-be \CC{} quartet (BGG plus three closest projected companions) contains at least one \CG{} galaxy — << overlap.get('n_galaxies') >> \CG{} galaxies in total. Table~\ref{tab:cg_core_overlap} breaks this overlap down by the \citet{Zheng&Shen21} embeddedness class: essentially every Embedded and Predominant compact group \emph{is} the projected core of an ordinary group. Had these groups not been excluded, the same galaxies would have appeared on both sides of the comparisons, biasing every contrast toward null.

\begin{table}
    \centering
    \caption{\CG{} galaxies inside would-be \CC{} quartets of the parent catalogue, by compact-group class.}
    \label{tab:cg_core_overlap}
    \begin{tabular}{lccc}
        \hline\hline
        \textbf{\CG{} class} & \textbf{\CG{} groups} & \textbf{Lim groups} & \textbf{Galaxies} \\
        \hline
<% for class_name, row in overlap.get('by_class', {}).items() %>
        << lu.display_label(class_name) >> & << row.get('n_cg4_groups') >> / << row.get('n_cg4_groups_total') >> & << row.get('n_lim_groups') >> & << row.get('n_galaxies') >> \\
<% endfor %>
        \hline
    \end{tabular}
    \tablefoot{``\CG{} groups'' counts compact groups with at least one member inside a parent-group quartet, over the total number of compact groups of that class (split groups included, as in the exclusion).}
\end{table}
<% endif %>

\section{Supplementary diagnostic figures}
\suppressfloats[t]

This appendix collects the secondary diagnostic figures referenced in the main text.

\subsection{sSFR and main-sequence diagnostics}

Figures~\ref{fig:Residual_sSFR}--\ref{fig:Main_Sequence_polyfits} show the residual-sSFR distribution, the property distributions split by sSFR class (for all galaxies and for satellites by BGG class), and the polynomial-order comparison for the star-forming main sequence.

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{residual_sSFR_histogram.pdf}
     \caption{Distribution of residual sSFR offsets from the adopted star-forming main sequence.}
     \label{fig:Residual_sSFR}
\end{figure}

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{ssfr_class.pdf}
     \caption{Distributions of various quantities for galaxies according to their sSFR class.}
     \label{fig:ssfr_class}
\end{figure}

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{Satellites_by_BGG_ssfr_class.pdf}
     \caption{Distributions of various quantities for satellites according to the BGG sSFR class.}
     \label{fig:Sat_BGG_ssfr_class}
\end{figure}

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{Main_Sequence_polyfits.pdf}
     \caption{Polynomial fits of orders 1--4 to the star-forming main sequence. The legend reports residual RMS and five-fold cross-validated RMS scatter in dex; order 2 is adopted because it is the lowest-order model with the smallest cross-validated RMS scatter.}
     \label{fig:Main_Sequence_polyfits}
\end{figure}

\FloatBarrier
\clearpage
\subsection{Group-scale and radial diagnostics}

Figures~\ref{fig:dist2BGG_kpc_vs_sSFR} and \ref{fig:dist2BGG_norm_vs_sSFR} show the sSFR--distance relations, Figs.~\ref{fig:Dom_vs_NonDom} and \ref{fig:Dom_vs_NonDom_XOR} the domination diagnostics, and Fig.~\ref{fig:tcr_vs_mvirial_over_l} the crossing-time trend.

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{dist2BGG_kpc_vs_sSFR.pdf}
     \caption{sSFR as a function of projected distance to the BGG, in kpc, for satellite galaxies in each sample.}
     \label{fig:dist2BGG_kpc_vs_sSFR}
\end{figure}

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{dist2BGG_norm_vs_sSFR.pdf}
     \caption{sSFR as a function of projected distance to the BGG normalized by $\langle R_{ij}\rangle$ for satellite galaxies in each sample.}
     \label{fig:dist2BGG_norm_vs_sSFR}
\end{figure}

\begin{figure*}
    \centering
     \includegraphics[width=\textwidth]{Dom_vs_NonDom.pdf}
     \caption{Distributions of the main group-scale observables in dominated and non-dominated systems.}
     \label{fig:Dom_vs_NonDom}
\end{figure*}

\begin{figure*}
    \centering
     \includegraphics[width=0.85\textwidth]{Dom_vs_NonDom_XOR_halfviolin_ONEFIG.pdf}
     \caption{Bootstrap distributions of the Spearman coefficients for the group-property pairs selected by the XOR domination criterion, i.e. property pairs whose Spearman correlation, after a per-subsample Benjamini--Hochberg adjustment across pairs, is significant (\(p_\mathrm{adj}<0.10\)) in exactly one of the dominated and non-dominated subsamples of at least one sample.}
     \label{fig:Dom_vs_NonDom_XOR}
\end{figure*}

\begin{figure}
    \centering
     \includegraphics[width=\columnwidth]{tcr_vs_mvirial_over_l.pdf}
     \caption{Crossing time versus virial mass-to-light ratio for the compact groups and their controls.}
     \label{fig:tcr_vs_mvirial_over_l}
\end{figure}

\FloatBarrier
\subsection{Colour diagnostics}

Figures~\ref{fig:color_mass}--\ref{fig:colour_residual_distance} show the colour--mass relations of the catalogues, the satellite colour--mass comparison, and the mass- and redshift-adjusted colour residuals versus BGG-centric distance.

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< r.Colour_figures.colour_mass >>}
    \caption{Optical colour as a function of stellar mass for \CG, the three control catalogues, and the matched SDSS reference sample. Dashed lines show the fitted catalogue relations.}
    \label{fig:color_mass}
\end{figure*}

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< r.Colour_figures.satellite_environment >>}
    \caption{Satellite colour--mass relations in compact and ordinary groups. The fitted lines show both colour offsets at the reference mass and differences in slope.}
    \label{fig:satellite_colour_mass}
\end{figure*}

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< r.Colour_figures.residual_distance >>}
    \caption{Mass- and redshift-adjusted satellite $(u-r)$ and $(u-g)$ residuals as a function of projected distance to the BGG. Lines connect binned medians for \CG{} and the pooled ordinary-group satellites; extreme plotting tails are omitted for readability, while the reported rank correlations use the full matched sample. More negative residuals are bluer than the ordinary-satellite reference relation.}
    \label{fig:colour_residual_distance}
\end{figure*}

<% if sz.get('status') == 'ok' %>
\FloatBarrier
\clearpage
\subsection{Size diagnostics}

Figures~\ref{fig:size_availability}--\ref{fig:size_radial} show the availability audit of the two size measures, the mass--size relations, the per-control size coefficients, the Simard--Petrosian measure difference, the \(R_{\rm e}\)--S\'ersic-\(n\) plane, and the radial size-residual diagnostic.

<% set appendix_size_avail = sz.get('availability_audit', {}) %>
<% if appendix_size_avail.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_size_avail.get('figure') >>}
    \caption{Availability of the seeing-corrected Simard half-light radius and the Petrosian half-light radius in each sample, after the bridge, quality, and blending cuts of Sect.~\ref{sec:size_data}.}
    \label{fig:size_availability}
\end{figure}
<% endif %>

<% if sz.get('mass_size', {}).get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('mass_size', {}).get('figure') >>}
    \caption{Seeing-corrected half-light radius versus stellar mass in each sample. Solid curves show the descriptive quadratic fits used to quote a predicted size at the reference stellar mass.}
    \label{fig:size_mass_relation}
\end{figure}
<% endif %>

<% if sz.get('per_control', {}).get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('per_control', {}).get('figure') >>}
    \caption{Mass- and redshift-adjusted \CG{} size coefficients against the pooled controls and against each control catalogue separately. Bars show 95\% confidence intervals; negative values mean smaller \CG{} sizes at fixed mass.}
    \label{fig:size_forest_per_control}
\end{figure}
<% endif %>

<% if sz.get('measure_delta', {}).get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('measure_delta', {}).get('figure') >>}
    \caption{Per-galaxy difference between the Simard and Petrosian log half-light radii as a function of nearest projected neighbour separation. The dashed line marks the 55-arcsec crowding scale.}
    \label{fig:size_measure_delta}
\end{figure}
<% endif %>

<% if sz.get('re_n_plane_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('re_n_plane_figure') >>}
    \caption{Half-light radius versus S\'ersic index for \CG{} and the pooled controls, shown for galaxies with both valid Simard size and stellar mass in the mass--size analysis. This is the plane used by the S-PLUS transition-galaxy studies.}
    \label{fig:size_re_n_plane}
\end{figure}
<% endif %>

<% if sz.get('radial', {}).get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< sz.get('radial', {}).get('figure') >>}
    \caption{Mass- and redshift-adjusted satellite size residuals versus projected distance to the BGG. Lines connect binned medians for \CG{} and the pooled controls.}
    \label{fig:size_radial}
\end{figure}
<% endif %>
<% endif %>

\FloatBarrier
\clearpage
\subsection{Robustness and selection diagnostics}

Figures~\ref{fig:phase_space_quenched_projected}--\ref{fig:colour_selection_bias} show the projected phase-space bins, the magnitude-gap diagnostics, the \(\mathrm{H}\alpha\) equivalent-width distributions, the AGN-like fractions, the availability audit, and the colour-selection mass audit.

<% set appendix_es = extended_specialness if extended_specialness else {} %>
<% set appendix_phase = phase_space_segregation if phase_space_segregation else appendix_es.get('phase_space_segregation', appendix_es.get('phase_space', {})) %>
<% set appendix_pfigs = appendix_phase.get('figure_paths', {}) %>
<% if appendix_phase.get('status') == 'ok' and appendix_pfigs.get('quenched_fraction_projected_phase_space') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_pfigs.get('quenched_fraction_projected_phase_space') >>}
    \caption{Quenched satellite fraction in coarse projected phase-space bins. Inner and outer refer to projected-distance rank from the BGG; low and high velocity are split at \(|\Delta v_{\rm BGG}|/\sigma_v=1\).}
    \label{fig:phase_space_quenched_projected}
\end{figure}
<% endif %>

<% set appendix_fossil = appendix_es.get('fossilness', {}) %>
<% if appendix_fossil.get('status') == 'ok' and appendix_fossil.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_fossil.get('figure') >>}
    \caption{Distributions of the first--second and first--fourth magnitude gaps in \CG{} and the pooled controls.}
    \label{fig:magnitude_gap}
\end{figure}
<% endif %>
<% if appendix_fossil.get('status') == 'ok' and appendix_fossil.get('quenched_fraction_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_fossil.get('quenched_fraction_figure') >>}
    \caption{Quenched satellite fraction as a function of the first--second magnitude gap.}
    \label{fig:magnitude_gap_quenched_fraction}
\end{figure}
<% endif %>

<% set appendix_recent = appendix_es.get('recent_quenching', {}) %>
<% if appendix_recent.get('status') in ['ok', 'limited'] and appendix_recent.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_recent.get('figure') >>}
    \caption{\(\mathrm{H}\alpha\) equivalent-width distributions used for the limited recent-quenching diagnostic.}
    \label{fig:recent_quenching}
\end{figure}
<% endif %>

<% set appendix_agn = appendix_es.get('agn_environment', {}) %>
<% if appendix_agn.get('status') in ['ok', 'limited'] and appendix_agn.get('figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_agn.get('figure') >>}
    \caption{AGN-like fractions among galaxies with BPT-classifiable emission-line ratios. BPT-unclassified galaxies are not counted as non-AGN in this diagnostic.}
    \label{fig:agn_fraction}
\end{figure}
<% endif %>

<% set appendix_selection = appendix_es.get('selection_diagnostics', {}) %>
<% if appendix_selection.get('status') == 'ok' and appendix_selection.get('availability_figure') %>
\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{<< SUBFIGURES_PATH >><< appendix_selection.get('availability_figure') >>}
    \caption{Availability fractions for the major derived quantities in each sample. The colour row records completeness of the four derived SDSS colour columns in the final catalogue, rather than the stricter colour-analysis matched subset used in Sect.~\ref{sec:optical_colours}.}
    \label{fig:data_availability}
\end{figure*}
<% endif %>
<% if appendix_selection.get('status') == 'ok' and appendix_selection.get('colour_bias_figure') %>
\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{<< SUBFIGURES_PATH >><< appendix_selection.get('colour_bias_figure') >>}
    \caption{Stellar-mass distributions of galaxies with and without complete matched colour measurements.}
    \label{fig:colour_selection_bias}
\end{figure}
<% endif %>

\end{appendix}
\clearpage

\end{document}
